[Ava-1818] #2339: bring-up roster reconcile + restarter hosted guard + backfill majority base

Three 2026-09-02 incident follow-ups in one batch (hosted flip fallout):

1. agent-host bring-up reconcile (services/agent_host/daemon.py): after the
   pidfile claim, the host stops process-form services the roster disables
   for hosted mode (today: restarter) but whose daemon is still running —
   two rollouts restarted the restarter on hosted boxes and it reaped
   healthy hosted rows for minutes before the watchdog round caught up.
   Stop set is derived from ops/spec gate reasons, never hard-coded;
   never raises; 20s grace then hands back to the watchdog.

2. restarter healthcheck hosted guard (services/healthchecks/restarter.py):
   on a hosted runner the whole round is a no-op — the roster disabled the
   daemon, so there is nothing to keep alive, and the stand-in reconcile
   would reap every healthy hosted agent (agent 2986, 2026-09-02).

3. env_registry backfill majority base (shared/env_registry.py): at least
   two keys agreeing on one block base prove it; outliers (a drifted
   hand-set key, unparseable value, legacy pin) no longer abort the whole
   backfill (wsl 2026-09-02: AVA_AGENT_RUNNER_WATCHDOG_HEALTH_PORT=20024).
   Ties stay ambiguous and yield {}.

Tests: bring-up reconcile suite (new), restarter hosted guard, backfill
outlier/tie/unparseable cases. pyright clean.

Co-authored-by: Ava #1818
