# Normalize all text files to LF in the repo (Windows checkouts included).
# Prevents the CRLF churn that bloats diffs when edited on Windows.
* text=auto eol=lf
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
