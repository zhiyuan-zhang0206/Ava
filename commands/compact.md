---
description: Save what matters, then compact your context
instruction-hint: optional focus, e.g. "preserve the latest decision"
---

Compact this context now.

1. Save anything that needs to be kept in the appropriate place, so the summary only has to point back to it, not reproduce it.
2. Write the summary and call `ava.self.compact(summary)` to compact the context. Its docstring contract is the single source of truth for what the summary contains and how to write it.
3. If you are waiting on an external event, set a reminder first.
