---
description: Start the next task in an isolated git worktree (never touch main)
instruction-hint: what to work on
---

Do this in an isolated git worktree — never edit or push the shared main checkout directly. Create a worktree named after your agent id, work on a feature branch, and route every change through a PR.
