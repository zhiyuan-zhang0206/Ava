{
  "openapi": "3.1.0",
  "info": {
    "title": "Ava",
    "version": "0.1.0"
  },
  "paths": {
    "/api/auth/login": {
      "post": {
        "summary": "Login",
        "description": "Authenticate with the cluster secret and receive a session cookie.\n\nRequest body: ``{\"password\": \"<cluster-secret>\"}``\n\nOn success, returns ``{\"ok\": true}`` and sets an HTTP-only session\ncookie whose lifetime is controlled by ``session_ttl_seconds``.\n\nOn failure, returns a typed 401 error envelope with detail ``\"invalid password\"``.\n\nA no-secret cluster (single-box no-auth posture) has no credential: login\nis a no-op 200 success, deliberately BEFORE the rate limiter — there is\nnothing to guess, and no-secret requests must never count into the\nlimiter's failure records.\n\nBrute-force guard: an IP that fails ``MAX_FAILURES`` times in a row is\nlocked for ``LOCKOUT_SECONDS`` (policy + rationale in shared/rate_limit.py).\nWhile locked, the endpoint returns 429 + ``Retry-After`` instead of 401 —\n401 would read as \"wrong password\" and invite exactly the retry loop the\nlockout exists to stop. A successful login resets the IP's counter.",
        "operationId": "login_api_auth_login_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/LoginRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/auth/logout": {
      "post": {
        "summary": "Logout",
        "description": "Revoke and clear the current session cookie; repeated calls are safe.",
        "operationId": "logout_api_auth_logout_post",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          }
        }
      }
    },
    "/api/auth/check": {
      "get": {
        "summary": "Check",
        "description": "Report whether the request carries a valid session cookie.\n\nReturns ``{\"authenticated\": true}`` or ``{\"authenticated\": false}``.\nAlways 200 — the caller uses the body, not the status code.",
        "operationId": "check_api_auth_check_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          }
        }
      }
    },
    "/api/auth/sessions": {
      "get": {
        "summary": "Sessions",
        "description": "List active browser sessions, marking the request's current cookie.\n\nOnly the request's current session keeps its full id; every other row's id\nis masked to its final 8 characters — enough to tell rows apart and to\nrevoke (the revoke endpoint accepts the suffix), without exposing the full\ncredential of sessions the caller does not hold. Managed-browser sessions\nare labeled with ``managed`` so they are not mistaken for the caller's own.",
        "operationId": "sessions_api_auth_sessions_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "additionalProperties": true,
                    "type": "object"
                  },
                  "type": "array",
                  "title": "Response Sessions Api Auth Sessions Get"
                }
              }
            }
          }
        }
      }
    },
    "/api/auth/sessions/{session_id}/revoke": {
      "post": {
        "summary": "Revoke Other Session",
        "description": "Revoke a non-current browser session.\n\nAccepts either the full session id or the 8-character masked suffix shown\nin the sessions list. The suffix fallback only triggers for the exact\nmasked form (8 characters) — any other shape is a full id and 404s when\nunknown — and never matches the request's own session (whose only\nrevocation path is logout). An ambiguous suffix (more than one other\nactive session ends with it) is refused rather than revoked wholesale.",
        "operationId": "revoke_other_session_api_auth_sessions__session_id__revoke_post",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Session Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/bootstrap": {
      "get": {
        "summary": "Get Bootstrap",
        "description": "Return cluster-common config ({ENV_ALIAS: value}, unmasked) for an\nagent-runner to load into its environment.\n\n`role=None` and `role=\"runner\"` both serve the least-privilege\n`ava_runner` AVA_DB_URL (the role's own password, carried inside the URL —\nnever a standalone key). The main identity and all admin credentials remain\ngateway-local (see shared.config.bootstrap_config_values).\n\nRaises:\n    HTTPException: 401 when the request does not carry\n        `Authorization: Bearer <cluster secret>` (a no-secret cluster serves\n        without auth — there is no credential to require); 400 for an\n        unknown role value, and when the runner credential is not provisioned.",
        "operationId": "get_bootstrap_api_bootstrap_get",
        "parameters": [
          {
            "name": "role",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Role"
            }
          },
          {
            "name": "authorization",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Authorization"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "additionalProperties": {
                    "type": "string"
                  },
                  "title": "Response Get Bootstrap Api Bootstrap Get"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}": {
      "patch": {
        "summary": "Patch Agent",
        "description": "Manually set / reset an agent label.\n\nbody.label non-empty: UPDATE directly. Empty string (after strip):\nUPDATE label=NULL to reset back to \"not set\"; frontend re-displays\nfallback `#N`. Both cases also set `label_user_set=TRUE` — sticky bit\nso the background LLM CAS no longer overwrites (otherwise after the\nuser resets, the LLM would still match `label IS NULL` and rename it,\ndefeating the reset intent). Both cases publish LabelUpdated so SSE\npushes in real time.\n\n404: agent_id does not exist.",
        "operationId": "patch_agent_api_agents__agent_id__patch",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/LabelPatchRequest"
              }
            }
          }
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      },
      "get": {
        "summary": "Get Agent",
        "description": "Full state of a single agent — spot-check endpoint for frontend / ops.\nPreviously also served SDK `get_status` (removed — agents no longer query\npeer status; FleetView uses its own API, self-evo uses `list_agents()`).\n\nShares the AgentRow schema (including last_active_at computation) with\n`GET /api/agents` (list all).\n\n404: agent_id does not exist (AgentNotFound -> handler returns 404 + reason).",
        "operationId": "get_agent_api_agents__agent_id__get",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AgentRow"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/models": {
      "get": {
        "summary": "Get Models",
        "description": "List selectable LLM models (grouped by provider) + the cluster default.\n\nRoster and tuning come from the model registry; current rates come from\nthe versioned pricing catalog. The default mirrors `settings.lm.llm_model`\nso the UI can pre-select it.",
        "operationId": "get_models_api_models_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ModelsResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents": {
      "get": {
        "summary": "Get Agents",
        "description": "List agent snapshots for the requested roster scope.\n\n``all`` is the compatibility default for SDK / ops callers.\nFrontend fleet/sidebar readers request ``live`` so Postgres excludes\nterminated history before evaluating the per-agent snapshot lookups, and\nrequest ``terminated`` alongside it: the sidebar's spawn tree needs the\nterminated rows as lineage joints (an alive child of a terminated parent\nre-parents under the nearest visible ancestor — #312 orphan regression).\nEvery scope returns raw spawner / fork-source truth; the show-terminated\nUI toggle only controls rendering, never the fetch.\n\n``fields=full`` preserves the historical response. ``fields=summary`` is\nthe reduced SQL projection used by roster consumers; ``fields=compact``\nremains as a legacy narrow projection. The CLI renders four fields from the\nsummary projection. Detail and SSE keep the full snapshot.\nAll scopes remain unpaginated for wire compatibility.",
        "operationId": "get_agents_api_agents_get",
        "parameters": [
          {
            "name": "scope",
            "in": "query",
            "required": false,
            "schema": {
              "enum": [
                "all",
                "live",
                "terminated"
              ],
              "type": "string",
              "default": "all",
              "title": "Scope"
            }
          },
          {
            "name": "fields",
            "in": "query",
            "required": false,
            "schema": {
              "enum": [
                "full",
                "summary",
                "compact"
              ],
              "type": "string",
              "default": "full",
              "title": "Fields"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "$ref": "#/components/schemas/AgentRow"
                      },
                      {
                        "$ref": "#/components/schemas/AgentSummary"
                      },
                      {
                        "$ref": "#/components/schemas/AgentCompact"
                      }
                    ]
                  },
                  "title": "Response Get Agents Api Agents Get"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      },
      "post": {
        "summary": "Post Agents",
        "description": "Spawn a new agent — uniform HTTP path for SDK / frontend / scripts.\n\nSpawn is HTTP-uniform: every spawn funnels through\n`create_and_launch_agent`, which creates the agent row in the gateway\nprocess (the main data-plane identity — the target runner's ops server\nruns as the least-privilege `ava_runner` role and cannot INSERT agents)\nand forwards a launch-only op to the target runner's ops server, whether\nthe runner is remote or co-located on this box (localhost) — one code\npath, uniform logs/traces, no in-process shortcut. Auto label generation\nis done asynchronously by the services/labeler daemon — does not block\nthe spawn response.\n\nFork prompt delivery: for a fork, the prompt is inserted pre-launch by\ncreate_agent_row (the forked agent inherits a full history and would\notherwise start a turn on the inherited task before a post-launch prompt\nlanded), with no InboundArrived (the agent's claim emits InboundCommitted\nonce it picks the prompt up — same as resurrect); for a plain spawn, the\nlaunch op delivers the first prompt post-launch and publishes\nInboundArrived so all UIs see the new agent received its first task in\nreal time.\n\nbody.machine = None targets the local machine (which must be a registered\nagent-runner).\n\n400: the target machine is registered but has no agent-runner capability\n(wire `reason='spawn_target_not_agent_runner'`). 404: the target is not in the\nregistry. 409: the fork_from agent has no checkpoint (no LLM/exec step yet).",
        "operationId": "post_agents_api_agents_post",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SpawnAgentRequest"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SpawnedAgent"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/compact": {
      "post": {
        "summary": "Post Compact",
        "description": "Trigger compact — INSERT kind='compact_request' inbound; the claim\nNode takes over, runs the backend Compaction LLM to generate a summary\nthat replaces messages, and publishes a `compact_done` event to notify\nUI.\n\n`mode` query parameter is preserved for backward compat with old\nfrontends but is ignored — the new design uniformly uses backend LLM\nsummary generation (see\ndecisions/2026-05-02-self-cycling-langgraph.md). Agent-initiated compact still goes through\nava.self.compact() -> kind='compact_summary'; this is a separate signal\nfrom UI-triggered compact_request.\n\nA compact targeting a terminated agent auto-resurrects it (shared with the\nchat path): otherwise the compact_request row would sit pending with no live\nprocess to claim it. The co-batched resurrect wins the claim node's recency\nrouting, so the agent wakes and the requested compaction still runs.",
        "operationId": "post_compact_api_agents__agent_id__compact_post",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          },
          {
            "name": "mode",
            "in": "query",
            "required": false,
            "schema": {
              "enum": [
                "framework",
                "agent"
              ],
              "type": "string",
              "default": "framework",
              "title": "Mode"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CompactEnqueued"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/cancel": {
      "post": {
        "summary": "Post Cancel",
        "description": "Pause/stop the agent — INSERT a durable kind='cancel' inbound.\n\nDurable, not fire-and-forget: a running llm/exec node interrupts on the\nrow immediately (it watches the inbound Redis pub/sub path); if the agent\nis between actions when the cancel lands, the row stays pending and the\nnext claim pass halts it to idle. Either way the agent stops and stays\nalive (resumable by the next message). Enqueue-and-return like `/messages`;\nthe kernel emits a `cancelled` SSE event when it actually stops.\n\nNo cross-machine forwarding: the cancel is a durable row in the shared DB\n(plus a Redis wake), delivered regardless of which host runs the agent.",
        "operationId": "post_cancel_api_cancel_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CancelRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CancelRequested"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/terminate": {
      "post": {
        "summary": "Post Agent Terminate",
        "description": "Terminate through the home runner's durable native control path.\n\nClaim returns to END and the host flushes before applying normal termination.\nAn optional message is committed as pending work before the command, retained\nfor later resurrection without causing another model turn. Force returns\nenqueued while the exact original host settles its task and execution\nresources; acceptance and metadata status are not proof of completed exit.\n\nBoth paths forward to the home runner. A missing agent returns 404; an\nalready-terminated identity is a no-op for graceful termination.",
        "operationId": "post_agent_terminate_api_agents__agent_id__terminate_post",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TerminateAgentRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TerminateAgentResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/resurrect": {
      "post": {
        "summary": "Post Agent Resurrect",
        "description": "Resurrect a terminated agent — UPDATE 'terminated' -> 'idling' +\nlaunch a fresh detached process attached to the same agent_id\n(LangGraph state preserved; the agent wakes and continues from its last\nturn).\n\nUsed by the frontend resurrect button. A bare resurrect is a pure\nlifecycle event with no message — the agent just gets the \"you have been\nresurrected\" marker; the \"resurrect with prompt\" path carries a `prompt`\nthat is INSERTed as a chat inbound in the **same transaction** as the\nlifecycle 'resurrect' inbound. The detached session may be created before\ncommit, but its child blocks on the agent row and cannot claim or process\neither inbound early. `resurrected_by` defaults to\n\"user\" and is validated against the envelope source whitelist.\n\nPeer agents have no dedicated resurrect API — they `send_message`, and\nauto-resurrect (`deliver_chat_inbound`) wakes a terminated target. This\nendpoint covers the case auto-resurrect cannot: a resurrect with no\nmessage to deliver.\n\nAlways runs on the agent's home machine via its ops server\n(`_forward_to_home_machine`) — that host starts the new process; launching\nanywhere else would start it on the wrong host.\n\n404: agent_id does not exist (AgentNotFound -> handler returns 404 + reason).\n`already_alive`: agent is still alive\n    (running/idling/restarting); resurrect does not\n    apply — idempotent.",
        "operationId": "post_agent_resurrect_api_agents__agent_id__resurrect_post",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ResurrectAgentRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ResurrectAgentResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/restart": {
      "post": {
        "summary": "Post Agent Restart",
        "description": "Enqueue native restart on the agent's home runner.\n\nThe current turn reaches claim, returns normally and flushes its checkpoint.\nThe host then applies the exact command and releases the incarnation for\nnew admission, retaining agent ID and context. Terminated agents require\nresurrection; restart returns already_terminated for them.",
        "operationId": "post_agent_restart_api_agents__agent_id__restart_post",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RestartAgentRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RestartAgentResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/messages": {
      "post": {
        "summary": "Post Agent Message",
        "description": "Deliver a chat inbound to the specified agent.\n\n`content` is either a plain string or a list of OpenAI-shaped content blocks\n(multimodal: text + `image_url` referencing an upload of this agent). A\nstring is a pure INSERT + return; a block list is capability-gated (422 if\nthe agent's model cannot see images) and stored with the blocks in the\ninbound's JSONB payload for the claim node to inline natively.\n\nAuto-resurrect in `deliver_chat_inbound` ensures the message always reaches\na live agent — the caller does not branch on status. An optional\n`Idempotency-Key` is stored transactionally with the inbound; same-key\nretries return the same `inbound_id` and mismatched key reuse fails 409.\n\n404: agent_id does not exist (AgentNotFound -> handler returns 404 + reason).\n409: the key already identifies a different agent/body/source.\n413: message content exceeds the 1 MiB transport limit.\n422: a block list gated out (non-vision model) or referencing a bad upload.",
        "operationId": "post_agent_message_api_agents__agent_id__messages_post",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 128
                },
                {
                  "type": "null"
                }
              ],
              "title": "Idempotency-Key"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AgentMessageIn"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AgentMessageEnqueued"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      },
      "get": {
        "summary": "Get Agent Messages",
        "description": "Raw state.messages for one agent — the data layer beneath the\nrendered GET .../timeline view. Consumers (ops scripts / other agents /\nevals) get each message as a LangChain BaseMessage `model_dump()`\n(type / content / tool_calls / id / additional_kwargs / ...) rather than\nthe per-block TimelineItem rendering.\n\nSame path as `POST /api/agents/{id}/messages` but different method: the\nPOST enqueues a new chat inbound for delivery; this GET reads back the\nfull conversation history.\n\nWindowing is an absolute-integer-index analog of the timeline's tail-window\nmode (the timeline cursor is an `item_id` string + `has_more`; here the\ncursor is an absolute index into `state.messages`). No `limit` returns the\nnewest 100 messages; `before=<index>` without a limit returns the newest\n100 messages before that exclusive cursor. An explicit `limit` (1..10000)\npreserves the requested page size. `messages[i]` corresponds to\n`state.messages[start_index + i]`; `msg_count` is the total length, and\n`has_more` tells the caller whether `start_index` can be supplied as the\nnext `before` cursor for an older page.\n\n404: agent_id does not exist (same precondition as the timeline GET).\n503: the checkpoint store could not be read — a programmatic data endpoint\n    does not disguise a read failure as an empty history; the caller retries\n    or checks store health.",
        "operationId": "get_agent_messages_api_agents__agent_id__messages_get",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "integer",
                  "maximum": 10000,
                  "minimum": 1
                },
                {
                  "type": "null"
                }
              ],
              "title": "Limit"
            }
          },
          {
            "name": "before",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 0
                },
                {
                  "type": "null"
                }
              ],
              "title": "Before"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AgentMessagesResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/system-note": {
      "post": {
        "summary": "Post Agent System Note",
        "description": "Deliver a framework system note to the specified agent.\n\nThe note is queued as a kind='system_note' inbound; the agent's claim\nrenders it as a system note (system_marker, NoteTag `body.note_tag`) in\nits timeline — no Agent prefix, no peer timestamp — instead of a chat\npeer message. Task notifications (assign / update / reminder) are the\ncurrent family (`note_tag='task'`).\n\n`body.resurrect` mirrors the chat path's auto-resurrect but is a choice,\nnot a given: task assignment (a delegator direction) wakes a terminated\nowner, while plain update / reminder notices must not (user ruling\n2026-08-27 — notification messages never resurrect a terminated owner).\n\n404: agent_id does not exist. 413: content exceeds the 1 MiB transport\nlimit. 422: note_tag is not a NoteTag value, or source is not a legal\nenvelope source.",
        "operationId": "post_agent_system_note_api_agents__agent_id__system_note_post",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SystemNoteIn"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AgentMessageEnqueued"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/messages/reconcile": {
      "post": {
        "summary": "Reconcile Agent Message",
        "description": "Resolve a timed-out POST and heal its still-pending delivery tail.",
        "operationId": "reconcile_agent_message_api_agents__agent_id__messages_reconcile_post",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 128,
              "title": "Idempotency-Key"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AgentMessageIn"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AgentMessageEnqueued"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/traces/{trace_id}/messages": {
      "get": {
        "summary": "Get Trace Checkpoint Messages",
        "description": "Full turn content for one OTel trace, resolved on demand from checkpoints.\n\nTrace v2 content-retrieval path: spans carry metadata only (task #792), so\nclicking an LM span needs this endpoint to bring back the turn's complete\nmessages — system prompt included (the checkpoint's `messages` channel is\nthe whole conversation, not a window).\n\n`pruned` semantics: the checkpoint link is written by the agent after each\nturn (`checkpoints.metadata.trace_id`), and checkpoint_cleanup keeps only\nthe latest K checkpoints per thread, so an old trace resolves to nothing —\nthe response is pruned=true with an empty list and the caller renders the\n\"trimmed\" state. A trace id with no matching checkpoint is the same shape (the\nlink itself may predate the feature or the turn committed no checkpoint).\n\n404: agent_id does not exist. 503: checkpoint store read failed.",
        "operationId": "get_trace_checkpoint_messages_api_agents__agent_id__traces__trace_id__messages_get",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          },
          {
            "name": "trace_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Trace Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TraceCheckpointMessagesResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/last-message": {
      "get": {
        "summary": "Get Last Message",
        "description": "Return the text of the last AI message for an agent.\n\nA non-isolated agent in the same cluster can query any other agent; an\neval-isolated caller is denied before this result can become a replay leak.\n\nReturns ``text=None`` when the agent has no AI message with text\ncontent yet (no checkpoint / no AIMessage / content is not a string).\n\nReads from ``agents_meta.last_message_text`` first — a column that\nsurvives compact (which replaces the entire checkpoint). Falls back\nto scanning the checkpoint when the column is NULL (backward compat\nwith agents that have not yet written to it).",
        "operationId": "get_last_message_api_agents__agent_id__last_message_get",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          },
          {
            "name": "caller",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "description": "Caller identifier, e.g. 'agent:240'",
              "title": "Caller"
            },
            "description": "Caller identifier, e.g. 'agent:240'"
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LastMessageResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/pending": {
      "get": {
        "summary": "Get Pending Messages",
        "description": "Chat inbounds still queued for the agent (status='pending'), oldest\nfirst. These have not been claimed yet, so they are absent from the\ntimeline snapshot; the web UI shows them as a compact strip above the\ncomposer. Once a message is claimed it enters the agent's messages and\nappears in the timeline, dropping out of this list.\n\nReturns an empty list for a nonexistent agent (this is a plain\ninbound-table read with no agent-existence precondition; a missing agent\njust has no pending rows).",
        "operationId": "get_pending_messages_api_agents__agent_id__pending_get",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/PendingInbound"
                  },
                  "title": "Response Get Pending Messages Api Agents  Agent Id  Pending Get"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/activity": {
      "get": {
        "summary": "Get Activity Trail",
        "description": "The agent's activity trail, oldest first (historical rows; the SDK write\nverb `ava.self.log` was removed 2026-08-02, so new rows no longer appear).\nThe collapsed current line is already on the agent snapshot\n(GET /api/agents); this endpoint backs the fleet view's replay of how the\nwork progressed.\n\nReturns an empty list for an agent that has never reported or does not\nexist (a plain activity-table read with no agent-existence precondition).",
        "operationId": "get_activity_trail_api_agents__agent_id__activity_get",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/ActivityEntry"
                  },
                  "title": "Response Get Activity Trail Api Agents  Agent Id  Activity Get"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/token-usage": {
      "get": {
        "summary": "Get Token Usage",
        "description": "The agent's most recent LLM call token usage. `input_tokens` is\ncontext-window occupancy; `max_input_tokens` is the model's context\nwindow ceiling so the frontend can render \"context: 26k/1M tokens\".\nWhen the frontend switches to an existing agent, it uses this endpoint\nto fetch the historical latest value to initialize the token counter\nUI — the SSE `token_usage` event is fire-and-forget without persistence,\nand the previous value is not available without switching to.\n\nReturns (0, 0, 0) on a checkpoint read failure — the intentional\nfail-fast boundary: the read failure is caught here (the SSE\ntoken_usage push refreshes the UI later), but the reverse scan over\nthe loaded messages runs unguarded so a bug surfaces normally rather\nthan being masked as 0/0.",
        "operationId": "get_token_usage_api_agents__agent_id__token_usage_get",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TokenUsageResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/context-breakdown": {
      "get": {
        "summary": "Get Context Breakdown",
        "description": "How the agent's context window is spent, for the composer's breakdown\npanel (lazy-loaded on expand).\n\nBuckets the checkpoint messages by kind + splits the system prompt into its\ntop-level sections, each a chars/4 estimate proportionally normalized to the\nlast LLM call's real `input_tokens` (so the categories sum to the truth). Pure\ngateway-side view logic (`gateway/context_breakdown.py`) — one checkpoint read,\nno kernel/agent involvement. A checkpoint read failure / no checkpoint yields\nan empty breakdown with zeroed totals (same tolerance as token-usage: the\npanel re-opens fine later).",
        "operationId": "get_context_breakdown_api_agents__agent_id__context_breakdown_get",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ContextBreakdownResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/events/stream": {
      "get": {
        "summary": "Get Events Stream",
        "description": "SSE endpoint — subscribe to Redis `ava:events`, filter by `agent_id`,\nforward.\n\nClient `EventSource` receives `data: {json}\\n\\n` frames, one JSON\n`events.Event` per frame. code_delta chunks pass through immediately,\nnative streaming. This is the live tail; `GET /api/agents/{id}/events`\n(no `/stream`) is the historical REST query over the persisted\n`events` table.\n\nDoes **not** check agent_exists as a precondition: subscribing to a\nnon-existent agent is allowed, you just receive no messages. Otherwise\nagents that were just switched / newly created but with slight DB\nvisibility lag would 404.",
        "operationId": "get_events_stream_api_agents__agent_id__events_stream_get",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/events": {
      "get": {
        "summary": "Get Agent Events",
        "description": "Historical slice of this agent's `events` rows — the REST\ncounterpart to the live `…/events/stream` SSE tail (which only carries\nevents from the moment you subscribe). Backs ops / SDK / eval consumers\nthat need to query the past.\n\nFilters compose (AND):\n  - `from=<ISO-8601>` / `to=<ISO-8601>`: inclusive time window\n    (`ts >= from AND ts <= to`); `from` omitted defaults to the last\n    24h (same lower-bound contract as /api/events). Bad timestamps 422\n    (FastAPI datetime parse).\n  - `event=<name>`: exact event type, e.g. `llm_usage` / `turn_end` /\n    `exec` / `exec_failed`.\n  - `level=<INFO|WARNING|ERROR|…>`: exact level (case-insensitive, any\n    case accepted; the stored/returned values are LOWERCASE since the W9\n    events-table switch — `agent_events`' uppercase `INFO`/`WARNING`/\n    `ERROR` no longer appears on the wire). This is an *exact* match,\n    unlike `/api/cluster/admin/events` where `level`\n    is a minimum threshold — a per-agent history query reads more\n    predictably as \"give me exactly this level\".\n\nReturns newest-first (`ts DESC`; `id` is a stable surrogate derived\nfrom the log line, so `limit`/`offset` paging stays deterministic).\n`limit` defaults to 100, capped at 1000 (over-limit 422s); `offset`\npages further back and is capped at 10,000. Loki has no native offset,\nso the cap bounds the in-memory parse of `limit + offset + 1` rows.\n\nNo agent-existence precondition (same as `…/activity` and `…/pending`):\nan unknown agent or an empty window just returns `[]`.",
        "operationId": "get_agent_events_api_agents__agent_id__events_get",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          },
          {
            "name": "from",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string",
                  "format": "date-time"
                },
                {
                  "type": "null"
                }
              ],
              "title": "From"
            }
          },
          {
            "name": "to",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string",
                  "format": "date-time"
                },
                {
                  "type": "null"
                }
              ],
              "title": "To"
            }
          },
          {
            "name": "event",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Event"
            }
          },
          {
            "name": "level",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Level"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 1000,
              "minimum": 1,
              "default": 100,
              "title": "Limit"
            }
          },
          {
            "name": "offset",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 10000,
              "minimum": 0,
              "default": 0,
              "title": "Offset"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/AgentEventRow"
                  },
                  "title": "Response Get Agent Events Api Agents  Agent Id  Events Get"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/computer/traces": {
      "get": {
        "summary": "Get Computer Trace",
        "description": "The desktop-action trail for one task: session envelope + actions.\n\nReturns:\n  {task_id, actions: [{id, ts, agent_id, event, action, app, outcome,\n    coords, path, error}...], start: {...}|null, end: {...}|null}\n— chronological, one entry per event row. `start`/`end` are the session\nenvelope rows (may be null while a session is open); `actions` carries\nthe computer_action rows with the replay-relevant payload keys.",
        "operationId": "get_computer_trace_api_computer_traces_get",
        "parameters": [
          {
            "name": "task_id",
            "in": "query",
            "required": true,
            "schema": {
              "type": "integer",
              "minimum": 1,
              "description": "task id whose desktop trail to read",
              "title": "Task Id"
            },
            "description": "task id whose desktop trail to read"
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "additionalProperties": true,
                  "title": "Response Get Computer Trace Api Computer Traces Get"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/inspect/live": {
      "get": {
        "summary": "Get Agent Inspect Live",
        "description": "Cheap current-state half of the inspector panel.\n\nReads the agent projection and open notice from Postgres, probes shells on\nthe owning runner, and performs only one bounded best-effort Loki lookup for\nthe heartbeat's recent-pause hint. Unknown agents return 404. Shell probe\nfailures set shells_available=False and Loki failures degrade\n`heartbeat.last_pause` to None, keeping this endpoint useful as the panel's\nfast skeleton source. No part of this response is cached.",
        "operationId": "get_agent_inspect_live_api_agents__agent_id__inspect_live_get",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AgentInspectLive"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/inspect": {
      "get": {
        "summary": "Get Agent Inspect",
        "description": "Per-agent inspector panel data in one shot — the agent's live persistent\nshells, its frozen config overlay, its LLM cost, turn/exec stats, idle\nheartbeat state. The\nsingle-agent counterpart to `/api/stats/dashboard` (fleet-wide).\n\n`?hours=` windows `cost` + `stats` to the selected range (0 = last 5m;\n1/6/24/72/168 = hours, anything else 422s), clamped to Loki retention;\nomitted = cumulative since spawn. `applied_window_hours` reports the\nserved horizon while `window_hours` continues to echo the request.\n`?since_compact=true` windows them to events since the agent's latest\ncompact halt instead — it takes precedence, `hours` is ignored and the\nechoed `window_hours` is None.\n`shells` + `config_overlay` + `heartbeat` are always\ncurrent, independent of the window. 404 if the agent is unknown (no\nagents_meta row). `config_overlay` is the spawn-time override map — `{}` when\nthe agent runs on cluster defaults (the column is NULL).\n\nLatency discipline: event-history sections use a shared bounded executor,\nand only their aggregates ride a 75s TTL cache keyed by (agent_id, hours,\nsince_compact). Concurrent misses share one single-flight Future; no more\nthan `_INSPECT_MAX_CONCURRENT_LOADS` distinct leaders run at once, and a\nsaturated request gets the queue-full 503. Each leader's 15-second response\nbudget is also its load deadline, so expired work stops and releases its\nadmission slot rather than continuing under transport timeouts. The\nagents_meta projection (machine, config, heartbeat inputs, liveness and\ntimestamps), `notice`, and `shells` are fetched fresh on every call and\nnever ride the cache. `shells` is probed on the agent's own machine via the\n`shell_probe` cluster op (the gateway never runs sessions itself; every\nmachine — its own included — is dialed at its registered ops URL), so a\nsplit deployment reflects each agent's runner and an unreachable machine\nsets shells_available=False rather than claiming no shells exist.\n`heartbeat` is the agent's idle check-in state: the\nprojected next check-in due time when idle (or the active pause / running\nsuppression) plus its most recent pause from history.\n\nThe retained live lifecycle leg begins at the index-label cutover and never\nscans the legacy slice. It has an 8-second Loki timeout and a per-agent\nthirty-minute single-flight cache, so changing `hours` or `since_compact`\ndoes not repeat that indexed read. Agents whose lifecycle history is wholly\npre-cutover use the `spawned_at` fallback described by `_alive_seconds`.",
        "operationId": "get_agent_inspect_api_agents__agent_id__inspect_get",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          },
          {
            "name": "hours",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "$ref": "#/components/schemas/StatsWindowHours"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Hours"
            }
          },
          {
            "name": "since_compact",
            "in": "query",
            "required": false,
            "schema": {
              "type": "boolean",
              "default": false,
              "title": "Since Compact"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AgentInspect"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/neighbors": {
      "get": {
        "summary": "Get Agent Neighbors",
        "description": "The agents most strongly tied to `agent_id`, ranked by recency-weighted\ninteraction strength (spawn / fork / resurrect / message, all equal weight),\nplus `ancestors` — the immutable birth chain above `agent_id`, nearest ancestor first.\n\n`depth=1` returns direct ties only; a higher `depth` follows ties outward,\ndiscounting each extra hop. `ancestors` ignores `depth`/`limit`: it walks\nthe immutable born_spawner chain to the top (message ties never form\nancestors), each row's `depth` = hops up (1 = the direct birth parent).\nTerminated agents are included (each row carries `status`); `limit` caps\nthe neighbor count, strongest first. The tie graph reads the unified event\nstream (task #180 LGTM cutover): audit edge events stitch the frozen PG\n`events` archive with the Loki live tail and the walks run in Python\n(gateway/neighbors.py) — the retired `agent_neighbors` SQL function died\nwith the frozen table it read.\n\n404: agent_id does not exist (AgentNotFound -> handler returns 404 + reason).",
        "operationId": "get_agent_neighbors_api_agents__agent_id__neighbors_get",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          },
          {
            "name": "depth",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 5,
              "minimum": 1,
              "default": 1,
              "title": "Depth"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 100,
              "minimum": 1,
              "default": 20,
              "title": "Limit"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/NeighborsResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/inspect/metrics": {
      "get": {
        "summary": "Get Agent Plugin Metrics",
        "description": "The agent's plugin metrics for the inspector panel — the W13b inspector\nsurface of the plugin metric system (see `shared/plugin_metrics.py`).\n\nBuilds the metric registry in process (task #180 PR D — shipped\nplugin `metrics.py` modules + core definitions), keeps the metrics whose\n`output` includes \"inspector\", renders each template for this agent\n({{agent_id}} -> ``agent_id = <n>``), re-validates the rendered query,\nsubstitutes the Grafana time macros with a fixed recent window (24h in 1h\nbuckets), and executes each query — LogQL against Loki, SQL read-only\nagainst the cluster's Postgres.\n\nResponse: one `PluginMetricResult` per registered inspector metric, in\nregistration order. `timeseries` / `barchart` metrics carry `series`\n(bucket ts + value, chronological); `stat` metrics carry `value`. A\nmetric whose query fails at runtime carries `error` (the others still\nrender); registry-level problems are HTTP errors instead — a template\nfailing the safety re-validation -> 500 with the reason, {{agent_id}}\ntemplate without an agent id -> 400 (unreachable here — the id is a path\nparam). 404 when the agent does not exist. The frontend panel polls this\nevery 5s like the parent /inspect. Implementation in\n``gateway/routers/_plugin_metrics.py``.",
        "operationId": "get_agent_plugin_metrics_api_agents__agent_id__inspect_metrics_get",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/PluginMetricResult"
                  },
                  "title": "Response Get Agent Plugin Metrics Api Agents  Agent Id  Inspect Metrics Get"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/shell/{session_id}": {
      "get": {
        "summary": "Get Agent Shell",
        "description": "Capture the most recent terminal output of one of an agent's persistent\nshells — the data the shell monitor page fetches on demand.\n\nDispatches the `shell_capture` op to the machine the agent runs on\n(`agents_meta.machine`, resolved through the `machines` table to that\nhost's ops server URL — one uniform path for every machine, the gateway's\nown box included). The runner resolves `session_id` against its live shell\nsessions for the agent, reconstructs the full session name (carrying\nthe optional `-<name>` suffix), and captures the last\n`lines` lines.\n\n404 if the agent is unknown (no agents_meta row), if the agent has no live\nshell with that id on its machine, or if the capture fails (the session\ndied between the probe and the capture). 503 if the agent's machine's ops\nserver is unreachable — the shell may still exist, but this gateway cannot\nreach it right now.",
        "operationId": "get_agent_shell_api_agents__agent_id__shell__session_id__get",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          },
          {
            "name": "session_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Session Id"
            }
          },
          {
            "name": "lines",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 2000,
              "minimum": 50,
              "default": 200,
              "title": "Lines"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ShellCaptureResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/timeline": {
      "get": {
        "summary": "Get Timeline",
        "description": "Timeline = raw view of LangGraph state.messages, one window at a time.\n\nDefault (no `before`) returns the newest `limit` items. Pass\n`before=<oldest item_id you hold>` to fetch the previous window for\nscroll-up history loading. `has_more` reports whether older items exist\nbefore the returned window.\n\nOne checkpoint segment is built at a time; windowing trims the payload +\nthe frontend render. A checkpoint read failure renders an empty view + 200\n(cold-load tolerance, see `shared.checkpoint`).",
        "operationId": "get_timeline_api_agents__agent_id__timeline_get",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 1000,
              "minimum": 1,
              "default": 50,
              "title": "Limit"
            }
          },
          {
            "name": "before",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Before"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TimelineResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/system": {
      "get": {
        "summary": "Get System Broadcast",
        "description": "Global SSE broadcast — does not filter by agent_id, forwards the\ncross-agent low-frequency GLOBAL_ROLES (agent lifecycle + page open/close)\nfor every agent.\n\nThe per-turn streaming roles are NOT here — clients subscribe to\n/api/agents/{id}/system for the agent they are observing.",
        "operationId": "get_system_broadcast_api_system_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/system": {
      "get": {
        "summary": "Get System Events",
        "description": "SSE endpoint — subscribe to `ava:events`, only forward SYSTEM_ROLES events.\n\nSYSTEM_ROLES contains code_delta / exec_output / compact_done /\ncancelled etc. \"system state\" events — clients that need to track the\nkernel runtime state subscribe to this channel.",
        "operationId": "get_system_events_api_agents__agent_id__system_get",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/system/all": {
      "get": {
        "summary": "Get All Events",
        "description": "Throttled SSE stream with optional comma-separated agent filtering.\n\n    With no ``agents`` query, every event for every agent passes. With a query,\n    only the selected agents plus system-level ``agent_id == 0`` events pass.\n    There is no role filtering. Events are batched and flushed at the cadence\n    set by settings.gateway.sse_throttle_rate, each flush carrying a JSON array\n    of raw event payloads.\n\n    Wire format: `data: [event_json, ...]\n\n`\n\n    The frontend subscribes once for the active agent and still filters by\n    agent_id / role defensively.",
        "operationId": "get_all_events_api_system_all_get",
        "parameters": [
          {
            "name": "agents",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Agents"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/pages": {
      "post": {
        "summary": "Post Page Register",
        "description": "ava.ui.show register / update a page — SDK main entry.\n\nEach agent can have at most one open page. Before registering the new\npage, any existing open pages for this agent are auto-closed (one\nagent, one page). The closed pages get individual PageClosed events\nso the frontend removes them from the popover.\n\nReturns 409 when agent is terminated — dead agents do not show UI.",
        "operationId": "post_page_register_api_agents__agent_id__pages_post",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PageRegisterRequest"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PageRow"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      },
      "get": {
        "summary": "Get Pages",
        "description": "UI page-load fetches the agent's currently open page list; SSE PageOpened/Closed handles deltas.",
        "operationId": "get_pages_api_agents__agent_id__pages_get",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/PageRow"
                  },
                  "title": "Response Get Pages Api Agents  Agent Id  Pages Get"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/pages/{name}": {
      "delete": {
        "summary": "Delete Page",
        "description": "ava.ui.close close a page — CAS open->closed.\n\n404: agent does not exist / page name does not exist / already closed\n(single 404 does not distinguish them; caller only cares that \"the\ntarget page is no longer in open state\").",
        "operationId": "delete_page_api_agents__agent_id__pages__name__delete",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Name"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PageRow"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/pages": {
      "get": {
        "summary": "Get All Pages",
        "description": "Fleet view page-load: all agents' currently open pages in one fetch, so\neach fleet row can surface its agent's panels without a per-row request.\nSSE PageOpened/PageClosed handles deltas.",
        "operationId": "get_all_pages_api_pages_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/PageRow"
                  },
                  "type": "array",
                  "title": "Response Get All Pages Api Pages Get"
                }
              }
            }
          }
        }
      }
    },
    "/pages/{page_key}": {
      "get": {
        "summary": "Proxy Page Root New",
        "description": "Trailing-slash canonicalization: `/pages/1-foo` redirects to\n`/pages/1-foo/` so relative asset links resolve.",
        "operationId": "proxy_page_root_new_pages__page_key__get",
        "parameters": [
          {
            "name": "page_key",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Page Key"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/pages/{page_key}/{rest}": {
      "get": {
        "summary": "Proxy Page Get New",
        "description": "Page URL reverse-proxy: `/pages/<agent_id>-<name>/...`.\n\n404 for a malformed key, unknown/closed page, or missing agent.",
        "operationId": "proxy_page_get_new_pages__page_key___rest__get",
        "parameters": [
          {
            "name": "page_key",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Page Key"
            }
          },
          {
            "name": "rest",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Rest"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/notices/open": {
      "get": {
        "summary": "Get Open Notices",
        "description": "The cross-fleet open notices feed, priority-then-newest.\n\nBy default only FYI notices (require_response false) — response-required\nones ride the agent snapshot. include_awaiting=True returns both kinds:\nthe IM bridge's \"/notice list\" queue view (Task #941) lists everything\nstill open and hands each item its own processing buttons. A notice is\nopen when resolved_at IS NULL. `limit` caps a runaway backlog.",
        "operationId": "get_open_notices_api_notices_open_get",
        "parameters": [
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 500,
              "minimum": 1,
              "default": 200,
              "title": "Limit"
            }
          },
          {
            "name": "include_awaiting",
            "in": "query",
            "required": false,
            "schema": {
              "type": "boolean",
              "default": false,
              "title": "Include Awaiting"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/NoticeItem"
                  },
                  "title": "Response Get Open Notices Api Notices Open Get"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/notices/escalations": {
      "get": {
        "summary": "Get Escalation Notices",
        "description": "Open task escalations with their task and current-owner review context.\n\nThis is the operator's read-only queue: only response-required notices that\nname a task are included. Escalations never run the FYI lazy-expiry sweep.",
        "operationId": "get_escalation_notices_api_notices_escalations_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/EscalationNoticeItem"
                  },
                  "type": "array",
                  "title": "Response Get Escalation Notices Api Notices Escalations Get"
                }
              }
            }
          }
        }
      }
    },
    "/api/notices/live": {
      "get": {
        "summary": "Get Notices Live",
        "description": "New open notices across the fleet, both kinds, oldest-first by id.\n\nThe IM bridge polls this with the max id it has seen and pushes new\nnotices to Telegram (Task #884) — one query, no event dependency, and it\ncovers require_response notices (which publish no NoticePosted event) as\nwell as FYIs. Idempotent: rows are only ever returned while open, so a\npoll after a notice was resolved simply stops seeing it.",
        "operationId": "get_notices_live_api_notices_live_get",
        "parameters": [
          {
            "name": "after",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "minimum": 0,
              "default": 0,
              "title": "After"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 500,
              "minimum": 1,
              "default": 200,
              "title": "Limit"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/NoticeItem"
                  },
                  "title": "Response Get Notices Live Api Notices Live Get"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/notices/resolved": {
      "get": {
        "summary": "Get Resolved Notices",
        "description": "One page of resolved notices across the fleet, newest resolution first.\n\nFeeds the greyed history beneath each open queue. `require_response` filters to\none queue's history (the \"needs response\" tab passes true, the FYI tab false);\nomit it for both. Keyset-paginated on (resolved_at, id): pass the last row's\n(before_at, before_id) for the next page strictly older. Supply both or neither.",
        "operationId": "get_resolved_notices_api_notices_resolved_get",
        "parameters": [
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 100,
              "minimum": 1,
              "default": 30,
              "title": "Limit"
            }
          },
          {
            "name": "require_response",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Require Response"
            }
          },
          {
            "name": "before_at",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string",
                  "format": "date-time"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Before At"
            }
          },
          {
            "name": "before_id",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Before Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/NoticeItem"
                  },
                  "title": "Response Get Resolved Notices Api Notices Resolved Get"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/notices": {
      "get": {
        "summary": "Get Notices Feed",
        "description": "The unified inbox feed (Task #1024, R4 layer 2, decision Q1=A): one\nrequest carries the whole Inbox panel.\n\n- `open` — cross-fleet open FYI notices (require_response false),\n  priority then newest. Same content as GET /api/notices/open.\n- `awaiting` — cross-fleet open require_response notices, same sort.\n  Today these ride the agent snapshot (notices_awaiting_response); the\n  merged endpoint makes them a first-class pipe so the frontend's Inbox\n  no longer merges three sources.\n- `resolved_page` + `next_cursor` — one keyset page of the resolved\n  history (both kinds, newest resolution first), same semantics as\n  GET /api/notices/resolved: pass next_cursor back as before_at /\n  before_id for the next strictly-older page; None means the end.\n\nThe standalone endpoints stay for their other consumers (IM bridge,\nCLI). The open sweep (FYI TTL auto-resolve) runs once per call, so the\nopen list, the awaiting list and the history agree within one request.",
        "operationId": "get_notices_feed_api_notices_get",
        "parameters": [
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 500,
              "minimum": 1,
              "default": 200,
              "title": "Limit"
            }
          },
          {
            "name": "resolved_limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 100,
              "minimum": 1,
              "default": 30,
              "title": "Resolved Limit"
            }
          },
          {
            "name": "before_at",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string",
                  "format": "date-time"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Before At"
            }
          },
          {
            "name": "before_id",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Before Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/NoticesFeed"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/notices/{notice_id}/resolve": {
      "post": {
        "summary": "Post Notice Resolve",
        "description": "Resolve one open notice and (when a reply is given) wake the agent with it.\n\n`action` is the explicit close verb: answer applies to both kinds — the\nuser can reply to an FYI too (Task #1061, the reply reaches the notice's\nagent like a require_response answer); dismiss applies to a require_response\nnotice, read to an FYI notice. The endpoint marks the notice resolved\n(resolved_at + resolution + cached reply) iff it is still open, then — when a\nreply is supplied, or a require_response notice is dismissed — INSERTs a\nself-describing chat inbound so the agent knows which notice this answers and\n(for a dismissed blocking notice) stops waiting. The inbound is system-sourced\n(`system:notice-reply` / `system:notice-dismiss`), rendered `[system] ...`\nwith no User envelope: a notice resolution is a notice-system event, not user\nspeech, so no user-role message is consumed and nothing is shaped like a\nreply request.\n\n409 when the notice does not exist, or `action` does not match the notice's\nkind (dismiss on an FYI notice, or read on one needing a response). A bare\n`read` on an already-resolved notice is an idempotent success (user ruling\n2026-08-28: \"Mark read\" means \"I have read it\", so the already-read state\nends silently, like the normal path — the same skip-not-error semantics as\nthe batch endpoint); a note attached to that read still reaches the agent,\nand `answer` / `dismiss` on an already-resolved notice keep the 409 so a\nreply or a dismissal is never silently dropped. 422 when `action` is\n`answer` without a reply.",
        "operationId": "post_notice_resolve_api_agents__agent_id__notices__notice_id__resolve_post",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          },
          {
            "name": "notice_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Notice Id"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ResolveNoticeIn"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AgentMessageEnqueued"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/notices": {
      "post": {
        "summary": "Post Notice Create",
        "description": "Post a notice at one of the three obligation rungs (FYI /\nresponse-required / blocking), atomically superseding the agent's\nprevious open notice.\n\nOne transaction: every open notice of the agent is resolved as\n'superseded' (with a resolve event each), then the new row is inserted.\nMirrors the previous SDK-side logic exactly — same supersede semantics,\nsame local_id allocation, same events — now executed server-side so the\ntwo writes cannot tear.\n\nReturns the SDK Notice shape: the new notice's local id, the pending\ncount + titles (for the SDK's pending list), and the superseded ids.",
        "operationId": "post_notice_create_api_agents__agent_id__notices_post",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/NoticeCreateIn"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "additionalProperties": true,
                  "title": "Response Post Notice Create Api Agents  Agent Id  Notices Post"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/notices/current": {
      "patch": {
        "summary": "Patch Notice Edit",
        "description": "Revise the agent's open notice (the SDK ava.ui.edit_notice contract).\n\nPass only the fields to change; require_response cannot change (turn an\nFYI into a question by dismissing and posting fresh). No open notice ->\nidempotent 204 (the SDK contract: silently return). The posted event is\nre-published so the frontend feed picks up the revision.",
        "operationId": "patch_notice_edit_api_agents__agent_id__notices_current_patch",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/NoticeEditIn"
              }
            }
          }
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/notices/current/dismiss": {
      "post": {
        "summary": "Post Notice Dismiss",
        "description": "Withdraw the agent's open notice (the SDK ava.ui.dismiss_notice\ncontract): resolution 'withdrawn' + resolve event, plus an inspector\nsnapshot refresh when the withdrawn row required a response. No open notice\n-> idempotent 204.",
        "operationId": "post_notice_dismiss_api_agents__agent_id__notices_current_dismiss_post",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/cluster/stop": {
      "post": {
        "summary": "Post Cluster Stop",
        "description": "Phase A handler: drain native agent controls while SDK dependencies\nremain available, then stop local services through cluster_stop.",
        "operationId": "post_cluster_stop_api_cluster_stop_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ClusterTransitionPayload"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": {
                    "type": "boolean"
                  },
                  "type": "object",
                  "title": "Response Post Cluster Stop Api Cluster Stop Post"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/cluster/resume": {
      "post": {
        "summary": "Post Cluster Resume",
        "description": "Compensating unpause: restore posture and release native admission holds,\nexecuted by this host's ops server via a cluster_resume op.\n\nSymmetric inverse of `/api/cluster/stop`. The orchestration's failure path\nfans this out (by dialing each host's ops server) to every host it had paused.\nOperators recover a stranded host through `/api/cluster/recover`; this route\nrequires the opaque exact capability of the deploy that created the pause.",
        "operationId": "post_cluster_resume_api_cluster_resume_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ClusterTransitionPayload"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": {
                    "type": "boolean"
                  },
                  "type": "object",
                  "title": "Response Post Cluster Resume Api Cluster Resume Post"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/cluster/recover": {
      "post": {
        "summary": "Post Cluster Recover",
        "description": "Operator stranded-cluster recovery — force-clear a pause + update lock left\nbehind by a hard-killed rollout, so the UI/SDK unblock and the next rollout can\nrun without waiting out the lock TTL.\n\nBypasses the cluster-paused 503 middleware (`/api/cluster/*`), so it is callable\nexactly when the cluster is wedged paused. Refuses (409) if an orchestration is\nactually in flight on this host — recovery is only for the no-session strand.\n\nReturns {\"unlocked_holder\": <prior lock holder or None>}.",
        "operationId": "post_cluster_recover_api_cluster_recover_post",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "type": "object",
                  "title": "Response Post Cluster Recover Api Cluster Recover Post"
                }
              }
            }
          }
        }
      }
    },
    "/api/cluster/stopping": {
      "post": {
        "summary": "Post Cluster Stopping",
        "description": "Record that the (machine, home) unit is shutting down intentionally.\n\n`ava stop` POSTs this (best-effort) just before local teardown so the\ncluster view distinguishes a deliberate stop from a crash — the live probe\ncannot. Stamps the unit's `stopped_at` and recomputes the composed\n`machines` row; `ava start` clears it. `home` is the stopping unit's\n$AVA_HOME so a co-located peer unit keeps its capability.\n\n`machine`/`home` are caller-asserted, not verified against the auth\nprincipal (same trust model as the other cluster control endpoints).\nLow-stakes: stopped_at is cosmetic and a spuriously-stamped live host still\nprobes online=True.",
        "operationId": "post_cluster_stopping_api_cluster_stopping_post",
        "parameters": [
          {
            "name": "machine",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Machine"
            }
          },
          {
            "name": "home",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Home"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "additionalProperties": {
                    "type": "string"
                  },
                  "title": "Response Post Cluster Stopping Api Cluster Stopping Post"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/cluster/update": {
      "post": {
        "summary": "Post Cluster Update",
        "description": "Trigger an update.\n\n`target` selects which machine to update (omitted means this host). Either\nway the op POSTs to the target's ops server, which calls cluster_update_op\nin-process there — spawning a detached updater and returning quickly. This\nhost is no special case: its own ops server is dialed at its registered\nlocalhost URL.\n\n`target_sha` pins the force-checkout commit (the watchdog off-pin self-heal\npasses the cluster pin so the host converges to exactly it, not the moving\norigin/main tip); threaded into the op payload.\n\nReturns the orchestration session name + tee'd log path.\n\n503 when the target's ops server is unreachable; 502 when the op ran but\nreported failure (e.g. an updater session already in flight there — the\ncaller waits for paused=false then retries).",
        "operationId": "post_cluster_update_api_cluster_update_post",
        "parameters": [
          {
            "name": "target",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Target"
            }
          },
          {
            "name": "target_sha",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Target Sha"
            }
          }
        ],
        "responses": {
          "202": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "additionalProperties": {
                    "type": "string"
                  },
                  "title": "Response Post Cluster Update Api Cluster Update Post"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/cluster/rollout": {
      "post": {
        "summary": "Post Cluster Rollout",
        "description": "Launch the whole-cluster `ava cluster update` rollout, detached.\n\nGateway only. Spawns a detached session running the full\norchestration — Phase A pauses every agent-runner, the gateway stops /\npulls / syncs / migrates, Phase B fans out the agent-runner self-updates,\nthen polls each host back to healthy. Returns 202 immediately; clients\npoll `GET /api/cluster/status` per host to observe progress.\n\nBody is fully optional; `origin` (default \"user\") names the trigger and\nheads the rollout log + the cluster pin's `updated_by`.\n\nReturns the orchestration session name + tee'd log path, plus\n`backend_changed` (whether this rollout restarts agent processes) and\n`needs_replay` (whether the installed commit is ahead of the running\nbookmark). The frontend uses the first to describe agent restarts; the CLI\nuses the second to identify a half-deployed state. The SDK initiator that\nonce waited on the restart signal, `ava.self.update()`, was removed 2026-08.\n\nErrors:\n- 400 if called on an agent-runner (rollout is a gateway operation).\n- 409 if a rollout / update is already in flight.\n- 422 if the cluster is already up to date (behind==0 and no replay is\n  needed) — nothing to roll out, so the fleet is not bounced. Use\n  /api/cluster/restart to bounce on the current code.\n- 503 if the session backend could not start the orchestration session.",
        "operationId": "post_cluster_rollout_api_cluster_rollout_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ClusterOpRequest"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": {
                    "anyOf": [
                      {
                        "type": "string"
                      },
                      {
                        "type": "boolean"
                      }
                    ]
                  },
                  "type": "object",
                  "title": "Response Post Cluster Rollout Api Cluster Rollout Post"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/cluster/restart": {
      "post": {
        "summary": "Post Cluster Restart",
        "description": "Launch a whole-cluster restart (no pull), detached.\n\nGateway only. Same three-phase orchestration as rollout — pause every\nagent-runner, gracefully quiesce agents, bounce this host, fan out the\nagent-runner bounces — but skips git pull / uv sync / migration. Use it to\napply config changes (or unwedge a service) without changing the checked-out\ncode. Returns 202 immediately; clients poll `GET /api/cluster/status`.\n\nBody is fully optional; `origin` (default \"user\") names the trigger and\nheads the restart log.\n\nReturns the orchestration session name + tee'd log path.\n\nErrors:\n- 400 if called on an agent-runner (restart is a gateway operation).\n- 409 if a restart / rollout / update is already in flight.\n- 503 if the session backend could not start the orchestration session.",
        "operationId": "post_cluster_restart_api_cluster_restart_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ClusterOpRequest"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": {
                    "type": "string"
                  },
                  "type": "object",
                  "title": "Response Post Cluster Restart Api Cluster Restart Post"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/cluster/update-check": {
      "get": {
        "summary": "Get Cluster Update Check",
        "description": "Read-only preflight for the Update button — how far behind origin/main and\nwhat a rollout would restart, or whether an interrupted rollout needs replay.\n\nGateway only (it inspects the gateway checkout). Does a\n`git fetch` but never pulls or mutates the tree, so the UI can poll it.\nA clean `behind == 0` → the UI shows \"no updates\" and does not launch a\nrollout. An installed commit ahead of the running bookmark is instead\nreturned as `needs_replay`.",
        "operationId": "get_cluster_update_check_api_cluster_update_check_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UpdateCheck"
                }
              }
            }
          }
        }
      }
    },
    "/api/cluster/status": {
      "get": {
        "summary": "Get Cluster Status",
        "description": "This host's own snapshot (name / role / paused).\n\nOn an agent-runner-capable host the snapshot comes from this host's ops\nserver (a status_probe op dialed at its registered localhost URL) — the\ngateway never probes sessions/pidfiles itself. A pure gateway runs no ops\nserver, so it assembles a lightweight local snapshot (paused flag +\norchestration session + prod-source HEAD; no shell/daemon probes — that is\nagent-runner data a pure gateway does not have).\n\nConsumed by `ava status`'s gateway supplement. For the full multi-machine\nroster use `/api/cluster/roster`. Bypasses 503 mode so status stays visible\nduring pause — observability is always online.",
        "operationId": "get_cluster_status_api_cluster_status_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ClusterStatus"
                }
              }
            }
          }
        }
      }
    },
    "/api/cluster/roster": {
      "get": {
        "summary": "Get Cluster Roster",
        "description": "The full multi-machine roster — every registered machine + live status.\n\nBacks the thin-client `ava cluster status`: the gateway's own row is\nresolved locally; each agent-runner is probed in parallel via the\nstatus_probe op (total wall ≈ the probe timeout regardless\nof N). Same fan-out the `/api/status` cluster panel uses. Bypasses 503 mode\nso the roster stays visible during pause.",
        "operationId": "get_cluster_roster_api_cluster_roster_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/MachineStatus"
                  },
                  "type": "array",
                  "title": "Response Get Cluster Roster Api Cluster Roster Get"
                }
              }
            }
          }
        }
      }
    },
    "/api/cluster/admin/events": {
      "get": {
        "summary": "Get Cluster Admin Events",
        "description": "Slice the unified event stream from Loki (category=telemetry/log) for\nops debugging without SSH — the LGTM replacement for the PG `events` read\n(task #1197).\n\nFilters compose (AND):\n  - `agent_id=N`: only this agent's events (gateway / daemon rows excluded).\n  - `service_only=true`: only events with no agent_id (gateway / daemons).\n  - `level=ERROR`: minimum level (DEBUG/INFO/WARNING/ERROR/CRITICAL).\n  - `since=2h` / `since=2026-05-25T00:00Z`: relative window or absolute\n    timestamp. Relative format `<int><unit>` with unit `s/m/h/d`.\n  - `event=spawn,terminate`: comma-separated event names.\n  - `grep=<substring>`: substring match on the raw log line (the JSON\n    body includes the `msg` payload).\n  - `limit`: max rows to return, capped at 1000. Default 200.\n\nReturns newest-first; the client paginates by passing\n`since=<oldest_ts_seen>` on the next call.",
        "operationId": "get_cluster_admin_events_api_cluster_admin_events_get",
        "parameters": [
          {
            "name": "agent_id",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Agent Id"
            }
          },
          {
            "name": "service_only",
            "in": "query",
            "required": false,
            "schema": {
              "type": "boolean",
              "default": false,
              "title": "Service Only"
            }
          },
          {
            "name": "level",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Level"
            }
          },
          {
            "name": "since",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Since"
            }
          },
          {
            "name": "event",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Event"
            }
          },
          {
            "name": "grep",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Grep"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "default": 200,
              "title": "Limit"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AgentEventsResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/cluster/machines": {
      "get": {
        "summary": "Get Cluster Machines",
        "description": "List every registered machine with its description + live status.\n\nBacks ava.agents.list_machines(). Live status comes from the same\nstatus_probe op round-trip the status panel uses\n(gateway is live from its own perspective); total wall time is\nbounded by the probe timeout regardless of machine count. role /\ngateway_url are intentionally omitted — agents reason over the free-text\ndescription, not ops topology.",
        "operationId": "get_cluster_machines_api_cluster_machines_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/AgentMachineRow"
                  },
                  "type": "array",
                  "title": "Response Get Cluster Machines Api Cluster Machines Get"
                }
              }
            }
          }
        }
      }
    },
    "/api/cluster/machines/{name}/staging": {
      "post": {
        "summary": "Set Machine Staging",
        "description": "Set or clear a machine's operator staging flag (`is_staging`).\n\nThe staging latch is what keeps a registered staging host out of the\nrollout target set — `ava start` on it clears its `stopped_at` like any\nhost, and this flag is the exclusion (`shared.machines.list_agent_runners`\nskips is_staging rows). Backed by `shared.machines.set_staging`; the CLI\nverbs `ava cluster mark-staging` / `unmark-staging` call this endpoint.",
        "operationId": "set_machine_staging_api_cluster_machines__name__staging_post",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Name"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MachineStagingRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MachineDeleteResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/cluster/machines/{name}": {
      "delete": {
        "summary": "Delete Cluster Machine",
        "description": "Remove a row from the `machines` table.\n\nUsed to retire a decommissioned agent-runner or clean up after a rename\n(e.g. `laminar`→`cloud`: the new name's `register_self()` INSERTs the\nnew row at startup, then ops calls this endpoint to drop the now-stale\nold-name row). Idempotent — calling on a missing name returns\n`deleted=false`.\n\nRefuses to delete the row corresponding to this gateway's own\n`machine_name()` — the live process needs its registration to remain\nintact (cluster status probe targets it; `register_self` only writes\non startup, so a runtime DELETE wouldn't be repaired until next\nrestart).",
        "operationId": "delete_cluster_machine_api_cluster_machines__name__delete",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Name"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MachineDeleteResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/cluster/machines/{name}/pause": {
      "post": {
        "summary": "Pause Cluster Machine",
        "description": "Temporarily pull a machine out of the cluster (`ava cluster pause`).\n\nThe operator act starts with an admission fence, then drains and sweeps:\n\n1. **Latch** — set `paused_at`/`pause_reason` first. Every resurrection\n   transaction (explicit, pending-work, controller, and retry) locks and\n   checks this latch before its agent row, so stale resurrection cannot\n   escape the sweep.\n2. **Drain** — every in_progress task owned by a live agent on this\n   machine is reassigned to the drain owner (#405) with a note; the\n   reminder daemon then surfaces it.\n3. **Final sweep** — every agent row on the machine, including an already\n   terminated row, receives a newer force-terminate intent fence. A\n   pre-latch resurrection holding the agent row lock completes first, then\n   this sweep terminates its exact session; older pending work on a dead\n   row cannot reverse the pause either. Other existing-agent re-entry paths\n   (respawn/swap/revive) keep their established admission rules and are\n   caught when they hold or update an existing row before the final sweep.\n\nThe live rows are\n   force-terminated via the machine's own ops server (kill the process +\n   mark terminated in one op, source=\"machine-pause\"): the machine is\n   about to be disconnected, so waiting for graceful turns is not worth\n   the non-determinism of an agent that may never claim. An agent whose\n   ops server is unreachable (the machine is already down) is force-marked\n   terminated in the shared DB — its process is on the departing host\n   anyway.\nFrom the latch onward the machine vanishes from the roster / cluster panel /\nagents' list_machines, `list_agent_runners()` drops it (no probe, no offline\nalert, rollout skips it) and ordinary spawns targeting it are refused (409).\nThe separate transaction race between the pause latch and creation of a\nbrand-new agent row is outside this resurrection boundary.\n\nOpen \"machine offline\" alerts for the machine are resolved as part of the\nlatch step — an expected absence is not an incident. Idempotent: pausing\nan already-paused machine re-runs the (now-empty) drain/terminate and\nreturns the existing latch.\n\nRefuses (400) to pause the gateway's own machine — the gateway host must\nstay a cluster member for the cluster to answer anything.",
        "operationId": "pause_cluster_machine_api_cluster_machines__name__pause_post",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Name"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MachinePauseRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MachinePauseResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/cluster/machines/{name}/resume": {
      "post": {
        "summary": "Resume Cluster Machine",
        "description": "Restore a paused machine as a normal cluster member (`ava cluster resume`).\n\nClears `paused_at`/`pause_reason`; the row's registration info (gateway_url\n/ role / description) was never touched, so probing, the roster, rollout and\nspawn acceptance all resume immediately. The machine's own side needs no\ngateway action: when it is back online it re-runs `ava start`, whose\n`register_self()` refreshes its dial URL (its reachable address may have\nchanged) and clears its `stopped_at` latch. If the machine's reachable\naddress changed, the gateway's pg_hba must cover the new IP — see the ops checklist in the\nCLI output (`AVA_TRUSTED_CIDRS` + `ava cluster update --restart-only`).\n\n404 when the row does not exist; idempotent (resumed=False) when the\nmachine was not paused.",
        "operationId": "resume_cluster_machine_api_cluster_machines__name__resume_post",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Name"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MachineResumeResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/commands": {
      "get": {
        "summary": "Get Commands",
        "description": "Composer commands, globally by default or as one agent sees them.\n\nAn agent view always dials the machine recorded on ``agents_meta`` (including\nthe gateway's own machine).  A missing/offline/version-skewed runner falls\nback to the historical gateway-local catalog so autocomplete remains usable.",
        "operationId": "get_commands_api_commands_get",
        "parameters": [
          {
            "name": "agent_id",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Agent Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/CommandItem"
                  },
                  "title": "Response Get Commands Api Commands Get"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/config": {
      "get": {
        "summary": "Get Config",
        "description": "Return metadata + current value + the override set for all config items.\n\nHost-scope fields come from `machine` (default = this gateway): their\neffective value + read-time capability hint are produced by a config_read on\nthat host. Cluster + agent fields are always the gateway's own\n(machine-independent), masked here. `raw_overrides` is the editable set the\npanel deltas against, and it is keyed on whether `machine` was GIVEN, not on\nwhether the target is this gateway: the Cluster view (machine omitted) deltas\nagainst this gateway's manageable cluster + writable-host .env set; a\nmachine-addressed view (`?machine=<name>`, even == this gateway on a dual-role\nbox) deltas against that host's remotely-writable host set.",
        "operationId": "get_config_api_config_get",
        "parameters": [
          {
            "name": "machine",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Machine"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ConfigView"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      },
      "put": {
        "summary": "Put Config",
        "description": "Merge a config patch for `machine` (default = this gateway) into `.env`,\nscope-routed. Persist only — no restart (restart_required says which process\nto restart).\n\nThe body is parsed by `ConfigPatchPlan.parse` (shared with the host-side\nconfig_write_op): the editability gate, scope routing, the merge-patch\nreducer and cluster-scalar coercion all live there as one pure function.\nReducer / JSON-merge-patch semantics: a key with a value is set/replaced, a\nkey mapped to `null` is unset (reverted to its default), and an ABSENT key is\nleft untouched. Deletion is always the explicit null — never inferred from a\nkey's absence — so a partial PUT can never drop a field it did not name (a\nfull-replace once wiped a cluster's secrets that way). Only editable fields\nare accepted; the body is split by scope:\n- cluster-scope keys merge into the gateway's `.env` (machine-independent).\n- host-scope keys merge into the target machine's own `.env`, gated host-side\n  by remote_writable + the capability validator.\nAgent-scope fields have writable=False and are rejected by the writable gate.\n\nA machine-addressed PUT (`?machine=<name>`, including this gateway's own name on\na dual-role box) may carry host-scope fields only — a cluster key in the body\n400s (\"edit it on the Cluster view\"). A machine-addressed PUT also edits that\nhost's fields via the remote_writable gate, EVEN when the name is this gateway\nitself on a dual-role box — keyed on whether `machine` was given, not on\ntarget == this gateway (otherwise an explicit self selection would collapse\ninto the Cluster view and its host-only, remote_writable-gated toggles\n(heartbeat_enabled / task_maintenance_enabled — writable=False) would be\nrejected as read-only). The host-side op is authoritative for per-field host\nverdicts; the returned ConfigWriteResult surfaces them so the frontend can\nshow inline failures + the per-machine restart banner.",
        "operationId": "put_config_api_config_put",
        "parameters": [
          {
            "name": "machine",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Machine"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "additionalProperties": true,
                "title": "Body"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ConfigWriteResult"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/config/resolved": {
      "get": {
        "summary": "Get Resolved Config",
        "description": "Resolve every per-model-defaultable setting for one model — read-only.\n\nAnswers \"what will an agent on this model actually run with, and which layer\ndecided that\": shared default < per-model default (both code, in\n`shared/lm/registry.py`) < explicit `.env` value. `explain_setting` does the\nlayering — the same function the runtime resolves through, so this view\ncannot drift from the value an agent gets.\n\nThe explicit layer is each field's `current_value` from the config metadata,\ni.e. read fresh from `.env` rather than the gateway's boot-time settings —\nmatching what the config panel shows and what the next agent process boots\nwith (every tunable here is `restart_required=\"agent\"`).\n\nMachine-independent by construction: every tunable is cluster-scope, so\nthere is no `?machine=` here. The per-agent overlay — the one layer above\nexplicit — is per process and invisible from the cluster; `per_agent` marks\nwhich fields a spawn/restart overlay may still override.",
        "operationId": "get_resolved_config_api_config_resolved_get",
        "parameters": [
          {
            "name": "model",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Model"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ResolvedConfigView"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/config/default-model": {
      "get": {
        "summary": "Get Default Model",
        "description": "The model a new agent is born on, and where that value came from.",
        "operationId": "get_default_model_api_config_default_model_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DefaultModelView"
                }
              }
            }
          }
        }
      },
      "put": {
        "summary": "Put Default Model",
        "description": "Set the cluster's default model.\n\n400 when the id is not a spawnable model in `shared/lm/registry.py:MODELS`.\nTakes effect for agents born after the write; every existing agent keeps the\nmodel stamped on its own row.",
        "operationId": "put_default_model_api_config_default_model_put",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DefaultModelWrite"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DefaultModelView"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/settings": {
      "get": {
        "summary": "Get Settings",
        "description": "Return every row in the user_settings table.",
        "operationId": "get_settings_api_settings_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UserSettingListResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/settings/{key}": {
      "put": {
        "summary": "Put Setting",
        "description": "Upsert a single setting by key.",
        "operationId": "put_setting_api_settings__key__put",
        "parameters": [
          {
            "name": "key",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Key"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UserSettingUpdateRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UserSettingRow"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/schedules": {
      "get": {
        "summary": "List Schedules",
        "description": "List all schedules (enabled and disabled), newest first. Omits `script`.",
        "operationId": "list_schedules_api_schedules_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/ScheduleSummary"
                  },
                  "type": "array",
                  "title": "Response List Schedules Api Schedules Get"
                }
              }
            }
          }
        }
      },
      "post": {
        "summary": "Create Schedule",
        "description": "Create a schedule. 400 on a script syntax error, 409 on a name clash.",
        "operationId": "create_schedule_api_schedules_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ScheduleCreate"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ScheduleView"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/schedules/{schedule_id}": {
      "get": {
        "summary": "Get Schedule",
        "description": "Get one schedule (with its script). 404 if it does not exist.",
        "operationId": "get_schedule_api_schedules__schedule_id__get",
        "parameters": [
          {
            "name": "schedule_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Schedule Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ScheduleView"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      },
      "put": {
        "summary": "Update Schedule",
        "description": "Partial-update a schedule — only the fields present in the body change. A\nscript/command change is snapshotted into schedule_versions and, if the\nschedule is enabled, applied immediately (the session is relaunched on the new\nscript). An enabled-flag value change also converges immediately, so disable\nreaps the session and enable creates it; a same-value update leaves the\nsession intact. 400 on a script syntax error, 404 if missing, 409 on a name\nclash.",
        "operationId": "update_schedule_api_schedules__schedule_id__put",
        "parameters": [
          {
            "name": "schedule_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Schedule Id"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ScheduleUpdate"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ScheduleView"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      },
      "delete": {
        "summary": "Delete Schedule",
        "description": "Delete a schedule. 404 if missing. The reconcile loop kills its (now\norphaned) session; its work dir is removed.",
        "operationId": "delete_schedule_api_schedules__schedule_id__delete",
        "parameters": [
          {
            "name": "schedule_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Schedule Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "additionalProperties": {
                    "type": "string"
                  },
                  "title": "Response Delete Schedule Api Schedules  Schedule Id  Delete"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/schedules/{schedule_id}/start": {
      "post": {
        "summary": "Start Schedule",
        "description": "Enable + launch the schedule now. 404 if missing.",
        "operationId": "start_schedule_api_schedules__schedule_id__start_post",
        "parameters": [
          {
            "name": "schedule_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Schedule Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ScheduleView"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/schedules/{schedule_id}/stop": {
      "post": {
        "summary": "Stop Schedule",
        "description": "Disable + kill the schedule's session now. 404 if missing.",
        "operationId": "stop_schedule_api_schedules__schedule_id__stop_post",
        "parameters": [
          {
            "name": "schedule_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Schedule Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ScheduleView"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/schedules/{schedule_id}/restart": {
      "post": {
        "summary": "Restart Schedule",
        "description": "Relaunch the schedule's session now (reloading its script), clearing crash\nbackoff. 404 if missing, 409 if the schedule is disabled.",
        "operationId": "restart_schedule_api_schedules__schedule_id__restart_post",
        "parameters": [
          {
            "name": "schedule_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Schedule Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ScheduleView"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/schedules/{schedule_id}/logs": {
      "get": {
        "summary": "Get Schedule Logs",
        "description": "Recent output of the schedule. Live session -> its capture; a dead\nsession -> its PTY transcript file; otherwise the last crash traceback\n(`last_error`). 404 if the schedule is missing.",
        "operationId": "get_schedule_logs_api_schedules__schedule_id__logs_get",
        "parameters": [
          {
            "name": "schedule_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Schedule Id"
            }
          },
          {
            "name": "lines",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "default": 200,
              "title": "Lines"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ScheduleLogsView"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/schedules/{schedule_id}/runs": {
      "get": {
        "summary": "Get Schedule Runs",
        "description": "Recent run-history rows (newest first). 404 if the schedule is missing.",
        "operationId": "get_schedule_runs_api_schedules__schedule_id__runs_get",
        "parameters": [
          {
            "name": "schedule_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Schedule Id"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "default": 50,
              "title": "Limit"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/ScheduleRunView"
                  },
                  "title": "Response Get Schedule Runs Api Schedules  Schedule Id  Runs Get"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/schedules/draft": {
      "post": {
        "summary": "Draft Schedule",
        "description": "Hand a natural-language request to an ava-schedule-writer agent. Spawns the\nagent (which loads the ava-schedule-writer skill, clarifies, writes the script,\nand POSTs it back) and returns its id so the UI can open the conversation.",
        "operationId": "draft_schedule_api_schedules_draft_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ScheduleDraftRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ScheduleDraftResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/presets": {
      "get": {
        "summary": "List Presets",
        "description": "List all presets, ordered by name.",
        "operationId": "list_presets_api_presets_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/PresetView"
                  },
                  "type": "array",
                  "title": "Response List Presets Api Presets Get"
                }
              }
            }
          }
        }
      },
      "post": {
        "summary": "Create Preset",
        "description": "Create a preset. 409 on a name clash.",
        "operationId": "create_preset_api_presets_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PresetCreate"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PresetView"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/presets/{preset_id}": {
      "get": {
        "summary": "Get Preset",
        "description": "Get one preset. 404 if it does not exist.",
        "operationId": "get_preset_api_presets__preset_id__get",
        "parameters": [
          {
            "name": "preset_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Preset Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PresetView"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      },
      "patch": {
        "summary": "Update Preset",
        "description": "Partial-update a preset — only the fields present in the body change. 404\nif missing, 409 on a name clash.",
        "operationId": "update_preset_api_presets__preset_id__patch",
        "parameters": [
          {
            "name": "preset_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Preset Id"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PresetUpdate"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PresetView"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      },
      "delete": {
        "summary": "Delete Preset",
        "description": "Delete a preset. 404 if missing.",
        "operationId": "delete_preset_api_presets__preset_id__delete",
        "parameters": [
          {
            "name": "preset_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Preset Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "additionalProperties": {
                    "type": "string"
                  },
                  "title": "Response Delete Preset Api Presets  Preset Id  Delete"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/guide/draft": {
      "post": {
        "summary": "Draft Guide",
        "description": "Spawn an ava-guide agent for a natural-language ops request and return its\nid so the UI can open the conversation.",
        "operationId": "draft_guide_api_guide_draft_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/GuideDraftRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GuideDraftResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/inventory": {
      "get": {
        "summary": "Get Inventory",
        "description": "Return the plugin + MCP enable inventory.\n\nWithout `machine`: the cross-machine AGGREGATE matrix (one row per plugin /\nMCP server, one cell per reachable agent-runner; failed hosts in `unreachable`).\nWith `machine`: that one agent-runner's flat plugin + MCP lists (404 unknown /\nnon-agent-runner machine, 503 offline / timed out).",
        "operationId": "get_inventory_api_inventory_get",
        "parameters": [
          {
            "name": "machine",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Machine"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/InventoryAggregate"
                    },
                    {
                      "$ref": "#/components/schemas/InventoryMachineView"
                    }
                  ],
                  "title": "Response Get Inventory Api Inventory Get"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      },
      "put": {
        "summary": "Put Inventory",
        "description": "Apply plugin + MCP enable toggles to agent-runner `machine` (required).\n\n`machine` is required: inventory lives only on agent-runners, so there is no\ngateway default to write to (400 if absent, 404 if not an agent-runner).\nBody shape `{\"plugins\": {name: bool}, \"mcp_servers\": {name: bool}}` — the\nrequest model validates each half's shape (422 on a non-object half). The\nhost-side op is the authoritative per-item gate; the result surfaces its\nverdicts + atomic `applied` flag.",
        "operationId": "put_inventory_api_inventory_put",
        "parameters": [
          {
            "name": "machine",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Machine"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/InventoryWriteRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InventoryWriteResult"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/skills": {
      "get": {
        "summary": "Get Skills",
        "description": "List the skills in this host's load dir, correlated with the registry.\n\nEach top-level directory under `$AVA_HOME/skills/` becomes a row: its source\nlayer (core=repo / plugin / machine=user), its `enabled` toggle, and whether\nthe on-disk copy drifted from what converge last wrote (`modified_locally`).\nA directory with no registry entry is reported as `untracked` (present on\ndisk but the scanner won't load it).",
        "operationId": "get_skills_api_skills_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SkillsView"
                }
              }
            }
          }
        }
      },
      "put": {
        "summary": "Update Skill Enabled",
        "description": "Toggle one skill's enabled flag in the install registry.\n\nThe change is persisted to `installed.json` immediately; the skill scanner\npicks it up on the next agent spawn. Untracked skills (no registry entry)\ncannot be toggled — register them first with `ava skill register`.\n\n`body.name` is an inbound name: the lookup folds dash and underscore\ntogether (`match_key`), and the row's own spelling — not the request's — is\nwhat addresses the directory and comes back in the view.\n\nRead and write are ONE `mutate` cycle. Looking the row up through a separate\nread and then re-saving it would let a concurrent `ava skill install` or\n`ava converge` land its rows in between, and this handler's save — built on\na registry read before those rows existed — would drop them.",
        "operationId": "update_skill_enabled_api_skills_put",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SkillEnableUpdate"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SkillView"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/packages/draft": {
      "post": {
        "summary": "Draft Package",
        "description": "Spawn an ava-package-installer agent for a natural-language install request\nand return its id so the UI can open the conversation. 422 on an unknown kind.",
        "operationId": "draft_package_api_packages_draft_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PackageDraftRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PackageDraftResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/metrics": {
      "get": {
        "summary": "Get Metrics",
        "description": "Aggregate report over the last `days` of events (all agents, or a\nsingle one via `agent`). `days` is capped at 30 to bound the scan.\n`since_compact=true` additionally narrows each agent's events to those at\nor after its latest compact halt (echoed in `meta.since_compact`).\n`meta.total_events` counts every telemetry/log event in the window —\nincluding service-level rows (agent_id NULL) from every process, a scope\nwidened by the W9 events-table switch (it was agent-kernel lines only\nbefore); audit events are excluded.",
        "operationId": "get_metrics_api_metrics_get",
        "parameters": [
          {
            "name": "days",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 30,
              "minimum": 1,
              "default": 1,
              "title": "Days"
            }
          },
          {
            "name": "agent",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Agent"
            }
          },
          {
            "name": "since_compact",
            "in": "query",
            "required": false,
            "schema": {
              "type": "boolean",
              "default": false,
              "title": "Since Compact"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MetricsReport"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/metrics/agents": {
      "get": {
        "summary": "Get Metrics Agents",
        "description": "Per-agent breakdown of the last `days` of events — one\nheadline-counter row per agent (cost / tokens / cache hit / turn + exec\noutcomes), sorted by cost descending. `since_compact=true` narrows each\nagent's events to those at or after its latest compact halt. Service-level\nevents (no agent_id) count toward `meta.total_events` but produce no row —\nand the count covers every telemetry/log event in the window (all\nprocesses), the W9-widened scope documented on `get_metrics`.",
        "operationId": "get_metrics_agents_api_metrics_agents_get",
        "parameters": [
          {
            "name": "days",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 30,
              "minimum": 1,
              "default": 1,
              "title": "Days"
            }
          },
          {
            "name": "since_compact",
            "in": "query",
            "required": false,
            "schema": {
              "type": "boolean",
              "default": false,
              "title": "Since Compact"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AgentMetricsReport"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/events": {
      "get": {
        "summary": "Get Events",
        "description": "Slice of the unified event stream — every event (audit / telemetry /\nlog) through one query surface, the programmatic read side of the\nunified emitter. Newest-first (`ts DESC`, `id DESC` tiebreak).\n\nFilters compose (AND):\n  - `category=<audit|telemetry|log>`: retention/alerting class (unknown\n    value 422s).\n  - `event_name=<name>`: exact event name, e.g. `llm_usage` /\n    `turn_end` / `spawn` / `send_message` (the legacy `kind=` spelling\n    is accepted as an alias; passing both with different values 422s).\n    The `kind=` alias is retired on 2026-09-30 — pre-alignment clients\n    must move to `event_name` before then, after which the alias and its\n    conflict check are removed.\n  - `agent_id=<n>`: events belonging to that agent; service-level events\n    (NULL) are excluded when set.\n  - `trace_id=<hex>`: one turn's whole call chain — every event that\n    shares the id.\n  - `machine=<name>`: the host dimension.\n  - `level=<debug|info|warning|error|critical>`: exact match,\n    case-insensitive (unknown value 422s).\n  - `tier=<business|anomaly|observation|noise>[,...]`: comma-separated\n    display tiers, ORed within the list and ANDed with every other filter.\n    The Loki predicate is derived before pagination so `meta.total` and\n    page boundaries stay exact.\n  - `from=<ISO-8601>` / `to=<ISO-8601>`: inclusive time window\n    (`ts >= from AND ts <= to`); either side may be omitted. Values\n    MUST carry a timezone offset (`Z` or `+hh:mm`) — a naive timestamp\n    is interpreted in the server's local timezone, so it 422s instead\n    of silently shifting the window.\n  - `hours=<n>`: alternative window — the last N hours\n    (`from = now - hours`). Mutually exclusive with `from`.\n  - Default window: when neither `from` nor `hours` is given, the last\n    24 hours are assumed (`from = now - 24h`) — an unbounded query\n    would scan the whole retention history (6M+ rows across every\n    month partition), so the API never runs one. `meta.window_from`\n    always echoes the effective lower bound.\n  - `limit` (default 100, cap 1000) / `offset` (cap 10,000): offset\n    paging with stable ordering across same-`ts` rows. The cap bounds the\n    in-memory Loki JSON parse (`limit + offset + 1` rows).\n  - `with_total=1`: also compute the exact filtered row count\n    (`meta.total`) via the Loki count path — one extra full-window\n    aggregation, so it is opt-in; without it `meta.total` is null.\n\nResponse: `meta` (opt-in exact filtered `total`, effective\n`window_from`/`window_to`, `limit`/`offset`, `has_more` from the list\nfetch's +1 lookahead) + `items` (the unified `EventRow` shape).\n`window_from` is always set — the default 24h lower bound when the\nrequest named none. An empty window returns `items: []`.",
        "operationId": "get_events_api_events_get",
        "parameters": [
          {
            "name": "category",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Category"
            }
          },
          {
            "name": "event_name",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Event Name"
            }
          },
          {
            "name": "kind",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Kind"
            }
          },
          {
            "name": "agent_id",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Agent Id"
            }
          },
          {
            "name": "trace_id",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Trace Id"
            }
          },
          {
            "name": "machine",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Machine"
            }
          },
          {
            "name": "level",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Level"
            }
          },
          {
            "name": "tier",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Tier"
            }
          },
          {
            "name": "from",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string",
                  "format": "date-time"
                },
                {
                  "type": "null"
                }
              ],
              "title": "From"
            }
          },
          {
            "name": "to",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string",
                  "format": "date-time"
                },
                {
                  "type": "null"
                }
              ],
              "title": "To"
            }
          },
          {
            "name": "hours",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "number",
                  "maximum": 8760,
                  "exclusiveMinimum": 0
                },
                {
                  "type": "null"
                }
              ],
              "title": "Hours"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 1000,
              "minimum": 1,
              "default": 100,
              "title": "Limit"
            }
          },
          {
            "name": "offset",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 10000,
              "minimum": 0,
              "default": 0,
              "title": "Offset"
            }
          },
          {
            "name": "with_total",
            "in": "query",
            "required": false,
            "schema": {
              "type": "boolean",
              "default": false,
              "title": "With Total"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EventsResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/run-timeline": {
      "get": {
        "summary": "Get Run Timeline",
        "description": "Return an event-driven session waterfall with turn or bucket rows.",
        "operationId": "get_run_timeline_api_agents__agent_id__run_timeline_get",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          },
          {
            "name": "from",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string",
                  "format": "date-time"
                },
                {
                  "type": "null"
                }
              ],
              "title": "From"
            }
          },
          {
            "name": "to",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string",
                  "format": "date-time"
                },
                {
                  "type": "null"
                }
              ],
              "title": "To"
            }
          },
          {
            "name": "level",
            "in": "query",
            "required": false,
            "schema": {
              "enum": [
                "turn",
                "bucket"
              ],
              "type": "string",
              "default": "turn",
              "title": "Level"
            }
          },
          {
            "name": "bucket",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Bucket"
            }
          },
          {
            "name": "session",
            "in": "query",
            "required": false,
            "schema": {
              "enum": [
                "compact",
                "current"
              ],
              "type": "string",
              "default": "compact",
              "title": "Session"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunTimelineResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/event-resolutions": {
      "post": {
        "summary": "Create Event Resolution",
        "description": "Dismiss one event class and emit its immutable transition marker.\n\nGateway auth establishes that this is an operator action but does not carry\nan agent identity, so `dismissed_by=0` is the documented user/operator\nsentinel. The ops agent uses the same authenticated API surface.",
        "operationId": "create_event_resolution_api_event_resolutions_post",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/EventResolutionCreate"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EventResolutionRow"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      },
      "get": {
        "summary": "List Event Resolutions",
        "description": "List active dismissals or reopened history for the ops review cycle.",
        "operationId": "list_event_resolutions_api_event_resolutions_get",
        "parameters": [
          {
            "name": "status",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "dismissed",
                    "reopened"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Status"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EventResolutionListResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/event-resolutions/{dismissal_id}/reopen": {
      "post": {
        "summary": "Reopen Event Resolution",
        "description": "Manually reopen one active dismissal and emit its transition marker.",
        "operationId": "reopen_event_resolution_api_event_resolutions__dismissal_id__reopen_post",
        "parameters": [
          {
            "name": "dismissal_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Dismissal Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EventResolutionRow"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/ops/monitor": {
      "get": {
        "summary": "Get Ops Monitor",
        "description": "Time-bucketed ops series over `window` (default 24h). Bucket width is\nderived from the window (1h→60s, 6h→300s, 24h→1800s, 7d→3600s); every\nbucket in the window is present (zero-filled when empty), aligned to\n`meta.bucket_starts`.",
        "operationId": "get_ops_monitor_api_ops_monitor_get",
        "parameters": [
          {
            "name": "window",
            "in": "query",
            "required": false,
            "schema": {
              "enum": [
                "1h",
                "6h",
                "24h",
                "7d"
              ],
              "type": "string",
              "default": "24h",
              "title": "Window"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OpsMonitorReport"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/alerts": {
      "post": {
        "summary": "Ingest Alerts",
        "description": "Alertmanager webhook endpoint — store + SSE publish + IM notify.\n\nBody is the standard Alertmanager webhook payload (``status`` +\n``alerts[]``). One row per (fingerprint, starts_at); re-sends while\nfiring update the row, resolution flips status + sets ends_at. Every\nfiring transition notifies IM (all severities) and every ingested row\nis published to the SSE stream.",
        "operationId": "ingest_alerts_api_alerts_post",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AlertWebhookPayload"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AlertIngestResult"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      },
      "get": {
        "summary": "List Alerts",
        "description": "Unresolved-first alert history for the alert section.\n\nUnresolved instances float above resolved ones (2026-08-05 user ruling);\nwithin a status class, newest starts come first.\n``meta.unresolved_count`` backs the top-bar badge and ``meta.total`` is\nthe full match count.",
        "operationId": "list_alerts_api_alerts_get",
        "parameters": [
          {
            "name": "window",
            "in": "query",
            "required": false,
            "schema": {
              "type": "string",
              "pattern": "^(1h|6h|24h|7d)$",
              "default": "24h",
              "title": "Window"
            }
          },
          {
            "name": "status",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "unresolved",
                    "resolved"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Status"
            }
          },
          {
            "name": "severity",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "critical",
                    "warning",
                    "error"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Severity"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 500,
              "minimum": 1,
              "default": 100,
              "title": "Limit"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AlertsListResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/alerts/stream": {
      "get": {
        "summary": "Get Alerts Stream",
        "description": "SSE endpoint — subscribe to the ``ava:alerts`` Redis channel, forward.\n\n    Client ``EventSource`` receives ``data: {json}\n\n`` frames, one\n    ``AlertRow`` JSON per frame, on every ingest. Same machinery as the\n    agent-events stream (heartbeat frames, error frames, reconnection-safe);\n    this is the live tail — the initial fetch (GET /api/alerts) covers rows\n    ingested before the subscription opened.",
        "operationId": "get_alerts_stream_api_alerts_stream_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          }
        }
      }
    },
    "/api/health": {
      "get": {
        "summary": "Get Health",
        "description": "Liveness probe over the private network (the gateway is unauthenticated). DB ping\nincluded — a gateway that can serve HTTP but cannot reach Postgres is not\n\"healthy\" enough for the watchdog to leave alone.\n\nThe identity fields answer the questions a 200 alone cannot: `name` is\nwhich *service* answered, `home` this gateway's `$AVA_HOME` (its cluster\nidentity), `machine` its host, `started_at` this process's birth, and `sha`\nthe code frozen at boot. A 200 on this port only proves *something* listens\nthere, so the watchdog verifies the stable identity too rather than the status\ncode alone — see `services/healthchecks/gateway.py`. The start and code facts\nalso let read-only post-deploy checks detect a new serving generation without\nconsulting disk state. Each field was additive to the `{\"status\": \"ok\"}` contract.\n\nLiveness must fail faster than the resource it protects: this uses the\ncontrol-plane pool and a 1-2 second database budget rather than waiting up\nto 30 seconds for the data-plane pool. A 503 therefore means the process is\nalive but Postgres is degraded; the watchdog's consecutive-failure policy\ndecides whether that merits a respawn.\n\n`name` is a constant naming the service this route belongs to — the point being\nthat an impostor answering here reports its own name, or none. No probe reads it\nyet; `shared.daemon_health._probe_home` gains the `name` arm only once every\ndeployed gateway emits the field, and that ordering is load-bearing (#1038).",
        "operationId": "get_health_api_health_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          }
        }
      }
    },
    "/api/stats/dashboard": {
      "get": {
        "summary": "Get Stats Dashboard",
        "description": "Pull all data for the sidebar-top stats card in one shot.\n\nData sources:\n- `live_count`: agents_meta table — all non-terminated agents (running/idling/restarting)\n- `tokens` / `cost_usd`: full UTC days from the fleet ledger plus a Loki tail\n- average turn duration: Loki's unified event stream in 12-hour shards\n- warning/error counts: per-class counts via the resolution daemon's\n  grouped query (12h shards), split into total / dismissed / net with\n  the daemon's class arithmetic over the SELECTED window (task #1935)\n- `total_events`: archived event row count — frozen historical constant\n  (task #1281 parity run; PG events dropped; not a live gauge)\n\n`?hours=` selects the aggregation window (0 = last 5m; 1/6/24/72/168 =\nhours), whitelisted by `StatsWindowHours` (anything else 422s); the served horizon is\n`applied_window_hours`. Zero-data scenario: tokens all 0, cost_usd 0.0, avg_turn_seconds\nNone (frontend shows \"—\"). Until the unlabeled legacy slice expires on 2026-08-30,\nthe ledger removes the fixed-cost full-window token scans; afterward the\nindexed Loki tail keeps the same self-healing late-write behavior.",
        "operationId": "get_stats_dashboard_api_stats_dashboard_get",
        "parameters": [
          {
            "name": "hours",
            "in": "query",
            "required": false,
            "schema": {
              "$ref": "#/components/schemas/StatsWindowHours",
              "default": 24
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StatsDashboard"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/status": {
      "get": {
        "summary": "Get System Status",
        "description": "System status panel — pull services / shells / cluster in one shot.\n\nProbe wall time follows the slowest machine, while multiple frontend pollers\nrequest this roster. Fifteen-second staleness is acceptable for diagnostics;\nsingle-flight prevents expiry stampedes.\nEach block queries independently — a single failure does not affect\nthe others (each has its own try/except that falls back to a\ndegraded value).",
        "operationId": "get_system_status_api_status_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SystemStatus"
                }
              }
            }
          }
        }
      }
    },
    "/api/memory/search": {
      "post": {
        "summary": "Post Memory Search",
        "description": "Semantic search the memory pool; return **relative** paths of the\ntop-k most-relevant markdown files.\n\nPaths are relative to memory pool root (settings.services.memory_root, e.g.\n`~/.ava/memory/`); the SDK caller prepends `ava.memory.PATH` to\nreconstruct the absolute path. fs-neutral so gateway fs (e.g.\n/Users/x) and agent-runner fs (/home/y) being different still works.\n\nOnly the gateway hosts the search backend (numpy by default), so\nthis endpoint always runs on the gateway; agent-runner SDK calls reach this\nhandler via the gateway URL they were configured with (CF Tunnel /\nprivate network) and never enter via a local agent-runner gateway\n(agent-runners run no gateway process).\n\nRaises:\n    IndexerUnavailable: embedder API / search backend unreachable, or the\n        search exceeded its deadline (wire 503)",
        "operationId": "post_memory_search_api_memory_search_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MemorySearchRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MemorySearchResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/memory/refresh": {
      "post": {
        "summary": "Post Memory Refresh",
        "description": "Fast-forward the gateway memory checkout to the consolidated pool.\n\nThe gateway indexes `main`; once the pool's per-machine branches are\nmerged into `main`, calling this pulls those changes into the local checkout\n(fast-forward only). The memory indexer then re-embeds the changed files on\nits own. Returns the HEAD sha after the pull.\n\nThis runs only on the gateway (the gateway + memory checkout live\nthere); a runner reaches it via its configured gateway URL.",
        "operationId": "post_memory_refresh_api_memory_refresh_post",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MemoryRefreshResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/memory/graph": {
      "get": {
        "summary": "Get Memory Graph",
        "description": "Return concept notes and cross-links from the gateway memory bundle.",
        "operationId": "get_memory_graph_api_memory_graph_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MemoryGraphResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/memory/note": {
      "get": {
        "summary": "Get Memory Note",
        "description": "Return one parsed memory note by its **relative** path (sans frontmatter).\n\n`path` is a memory-pool-relative markdown path as the graph carries it\n(e.g. `health/user-health-overview.md`). Resolved inside the memory root\nonly — traversal (`..`, absolute paths) is rejected with 404 rather than\nleaking filesystem structure. A file that is not a note (no/invalid\nfrontmatter) is not a note either: 404. The response carries the parsed\nfields plus the markdown body with the YAML frontmatter already stripped.",
        "operationId": "get_memory_note_api_memory_note_get",
        "parameters": [
          {
            "name": "path",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Path"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MemoryNoteResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/memory/pool": {
      "get": {
        "summary": "Get Memory Pool",
        "description": "Download the consolidated memory pool as a git bundle.\n\nServes `gateway_memory_dir()` — the consolidated checkout on `main` — as a\n`git bundle` of HEAD (full real ancestry, so a bootstrapped machine branch\nis a true descendant of `main` and converges cleanly when a memory remote\nis configured later). A fresh agent-runner whose memory remote is not\nconfigured (headless enroll, no GitHub credentials) fetches this over its\ngateway URL and clones it as its initial pool, so the shared index and\nnotes reach its agents without GitHub. Untracked machine-local paths\n(`.cache`, `.githooks`, …) never ride a bundle — git only carries the\ntracked tree. Sits behind the normal Bearer/session middleware like every\n/api route. `X-Pool-Head` carries the checkout's HEAD sha so the receiver\ncan verify what it cloned is the advertised snapshot.",
        "operationId": "get_memory_pool_api_memory_pool_get",
        "responses": {
          "200": {
            "description": "Successful Response"
          }
        }
      }
    },
    "/api/mcp/clients": {
      "get": {
        "summary": "Get Mcp Clients",
        "description": "List MCP clients without their credential hashes.",
        "operationId": "get_mcp_clients_api_mcp_clients_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/McpClientView"
                  },
                  "type": "array",
                  "title": "Response Get Mcp Clients Api Mcp Clients Get"
                }
              }
            }
          }
        }
      },
      "post": {
        "summary": "Post Mcp Client",
        "description": "Create an MCP client and reveal its plaintext token once.",
        "operationId": "post_mcp_client_api_mcp_clients_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/McpClientCreate"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/McpClientCreated"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/mcp/clients/{client_id}/revoke": {
      "post": {
        "summary": "Post Mcp Client Revoke",
        "description": "Revoke one active MCP client.",
        "operationId": "post_mcp_client_revoke_api_mcp_clients__client_id__revoke_post",
        "parameters": [
          {
            "name": "client_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Client Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/McpClientRevoked"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/fleet/graph": {
      "get": {
        "summary": "Get Fleet Graph",
        "description": "Fleet-wide weighted agent graph — nodes (agents) + edges (lineage + messages).\n\nNodes carry status, label, a windowed recent-work `node_score`, and\nrestart-proof `total_tokens` consumed in the retained window (7d). Edges\nsplit into two families: lineage\n(spawn/fork/resurrect) is structural and permanent; messages (send_message)\ndecay with recency. Terminated agents — and edges touching a terminated\nagent — are excluded by default (user ruling 2026-08-09 #1104: terminated\nagents never appear in the graph, mirroring the sidebar's agent tree). The\nfilter ORDER is liveness first: the node set is live-only (`status !=\n'terminated'`, so restarting etc. stay), and edges only ever\nconnect two live endpoints — a live node whose lineage partner has since\nterminated simply renders without that edge. Raw source rows are filtered\nduring the merge; pass `?include_terminated=true` for the full graph.\n\n`?hours=` (0 = last 5m; 1/6/24/72/168 = hours; omitted = all-time) windows\nboth the node score and the edge events. `?decay_lambda=` (range [0, 10],\ndefault 0.5) is the per-day decay constant for the message edge weight,\nquantized to 2dp before both computation and cache-key construction. Its\n1001 values, two terminated states, and the bounded hour-window choices\ncap the cache-key space at approximately 16k entries. Per-caller rate\nlimiting was considered and deferred: this endpoint is auth-gated, and the\nbounded key space leaves no present threat that warrants that infrastructure.\n\nNode score (windowed, drives node size):\n    node_score = SUM(in_total) * 0.1 + SUM(out_total) * 1.0\nover the agent's `llm_usage` counters in the window — read from\nPrometheus (`ava_llm_usage_in_total` / `ava_llm_usage_out_total`,\nwindowed via `increase(...)`). `total_tokens` is the sum of the same two\ncounters over the retained 7d window, also using `increase(...)` so\nexporter process restarts do not reset it.\n\nEdge weight:\n    lineage (spawn/fork/resurrect): weight = event_count * 2.0 (no time decay,\n        always shown — the structural skeleton never fades)\n    message (send_message): weight = SUM(EXP(-decay_lambda * days_ago)) * 1.0\n        (recency-decayed; dropped below 0.01)",
        "operationId": "get_fleet_graph_api_fleet_graph_get",
        "parameters": [
          {
            "name": "include_terminated",
            "in": "query",
            "required": false,
            "schema": {
              "type": "boolean",
              "description": "Include terminated agents",
              "default": false,
              "title": "Include Terminated"
            },
            "description": "Include terminated agents"
          },
          {
            "name": "hours",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "$ref": "#/components/schemas/StatsWindowHours"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Hours"
            }
          },
          {
            "name": "decay_lambda",
            "in": "query",
            "required": false,
            "schema": {
              "type": "number",
              "maximum": 10,
              "minimum": 0,
              "default": 0.5,
              "title": "Decay Lambda"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FleetGraphResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/frontend-telemetry": {
      "post": {
        "summary": "Ingest",
        "description": "Validate one telemetry batch and emit its accepted events (204).\n\nBest-effort by contract: the client never depends on this call. 422 on a\nmalformed batch (a bug in our own client — fail fast, no silent shape\ndrift), 413 past the byte cap, 204 with a warning log when the session\nrate limit drops part of the batch.",
        "operationId": "ingest_api_frontend_telemetry_post",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          }
        }
      }
    },
    "/grafana": {
      "get": {
        "summary": "Grafana Root",
        "description": "Trailing-slash canonicalization: `/grafana` redirects to `/grafana/`\nso the app's relative asset links resolve against the proxy root.",
        "operationId": "grafana_root_grafana_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          }
        }
      }
    },
    "/grafana/{rest}": {
      "get": {
        "summary": "Grafana Proxy Get",
        "description": "GET /grafana/{rest} — forward to Grafana (dashboard pages, static assets).",
        "operationId": "grafana_proxy_get_grafana__rest__get",
        "parameters": [
          {
            "name": "rest",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Rest"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      },
      "post": {
        "summary": "Grafana Proxy Post",
        "description": "POST /grafana/{rest} — forward (Grafana's datasource-query API).",
        "operationId": "grafana_proxy_post_grafana__rest__post",
        "parameters": [
          {
            "name": "rest",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Rest"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      },
      "put": {
        "summary": "Grafana Proxy Put",
        "description": "PUT /grafana/{rest} — forward.",
        "operationId": "grafana_proxy_put_grafana__rest__put",
        "parameters": [
          {
            "name": "rest",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Rest"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      },
      "patch": {
        "summary": "Grafana Proxy Patch",
        "description": "PATCH /grafana/{rest} — forward.",
        "operationId": "grafana_proxy_patch_grafana__rest__patch",
        "parameters": [
          {
            "name": "rest",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Rest"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      },
      "delete": {
        "summary": "Grafana Proxy Delete",
        "description": "DELETE /grafana/{rest} — forward.",
        "operationId": "grafana_proxy_delete_grafana__rest__delete",
        "parameters": [
          {
            "name": "rest",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Rest"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      },
      "head": {
        "summary": "Grafana Proxy Head",
        "description": "HEAD /grafana/{rest} — forward (asset probes).",
        "operationId": "grafana_proxy_head_grafana__rest__head",
        "parameters": [
          {
            "name": "rest",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Rest"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/okf/graph": {
      "get": {
        "summary": "Get Okf Graph",
        "description": "Build the OKF graph from the live `.ava.okf.md` tree and render it\ninto the self-contained D3 template — open directly in a browser tab.",
        "operationId": "get_okf_graph_api_okf_graph_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          }
        }
      }
    },
    "/api/tasks": {
      "get": {
        "summary": "Get Tasks",
        "description": "Return the task registry, newest first.\n\n`fields=full` (the default) returns the compatibility projection. `fields=summary`\nreturns metadata only, selecting neither task text column. `window` (24h / 7d / 30d / all, default all) narrows the list by last activity\n(updated_at) on the backend, so the task graph's default 24-hour view never\npulls the full registry. A windowed list still carries every kept task's\nout-of-window ancestors flagged ghost=True (see _windowed_tasks); without\na window the full table is returned unchanged.\n\nNo pagination — the task registry is bounded (tasks are created by agents;\nclosed tasks are kept). Order by created_at DESC so the newest tasks\nsurface first in the Task Graph view.",
        "operationId": "get_tasks_api_tasks_get",
        "parameters": [
          {
            "name": "window",
            "in": "query",
            "required": false,
            "schema": {
              "type": "string",
              "pattern": "^(24h|7d|30d|all)$",
              "default": "all",
              "title": "Window"
            }
          },
          {
            "name": "fields",
            "in": "query",
            "required": false,
            "schema": {
              "enum": [
                "full",
                "summary"
              ],
              "type": "string",
              "default": "full",
              "title": "Fields"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TaskListResponse"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/tasks/{task_id}": {
      "patch": {
        "summary": "Patch Task",
        "description": "Partially update a task; omitted fields stay unchanged.\n\nstatus, priority, title, description, and results are taken when non-null\n(priority must be one of P0..P3; ongoing marks long-running active work; a\ntitle colliding with another in_progress task's is rejected). owner reassigns to another\nagent (an explicit null is rejected — a task cannot be released).\nremind_interval_seconds must be a positive number of seconds <= 24h (an explicit\nnull is rejected — reminders cannot be disabled). Any write resets the\nreminder counters, same as the SDK update path. An owner reassignment sends\nthe SDK-equivalent task system notes after the database write commits.\n\nThe system root task is immutable: any PATCH targeting it is rejected with\n422 (mirrors the SDK update() guard), so the task-tree anchor can never be\nreassigned, completed, cancelled, or otherwise edited.\n\nA status change to done or cancelled is rejected with 422 while any direct\nchild remains active (in progress or ongoing). Close or cancel those children first.",
        "operationId": "patch_task_api_tasks__task_id__patch",
        "parameters": [
          {
            "name": "task_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Task Id"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TaskUpdateRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TaskRow"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/plugin-ui/{plugin}": {
      "get": {
        "summary": "Plugin Ui Root",
        "description": "Trailing-slash canonicalization, so a page's relative asset links\nresolve against the mount rather than one level above it.",
        "operationId": "plugin_ui_root_api_plugin_ui__plugin__get",
        "parameters": [
          {
            "name": "plugin",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Plugin"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/plugin-ui/{plugin}/{rest}": {
      "get": {
        "summary": "Plugin Ui Get",
        "description": "Serve one file from an enabled plugin's `ui/` directory.\n\nA directory reached without a trailing slash redirects to the slash form\nbefore its index is served: the browser resolves a page's relative asset\nlinks against the URL it was loaded from, so `…/board` would send\n`app.js` to `…/app.js` — one level above where the plugin put it.",
        "operationId": "plugin_ui_get_api_plugin_ui__plugin___rest__get",
        "parameters": [
          {
            "name": "plugin",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Plugin"
            }
          },
          {
            "name": "rest",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Rest"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/ui/contributions": {
      "get": {
        "summary": "Get Ui Contributions",
        "description": "Every console contribution the cluster's enabled plugins declare.",
        "operationId": "get_ui_contributions_api_ui_contributions_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UiContributionsResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/uploads": {
      "post": {
        "summary": "Upload Files",
        "description": "Upload one or more files in a single batch. Each file is saved to\n~/Downloads/AvaAgent-{agent_id}/ and returned with its reference url.\n\n`deliver=true` (default) inserts a single inbound listing every saved path,\nso the agent learns about the whole batch at once. `deliver=false` saves\nsilently (no inbound) — the caller carries the returned urls into a later\nmultimodal message.\n\nresponse_model makes FastAPI validate the return + emit the schema into\nOpenAPI components; the frontend codegen auto-syncs to types-generated.ts.",
        "operationId": "upload_files_api_agents__agent_id__uploads_post",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          },
          {
            "name": "deliver",
            "in": "query",
            "required": false,
            "schema": {
              "type": "boolean",
              "default": true,
              "title": "Deliver"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "multipart/form-data": {
              "schema": {
                "$ref": "#/components/schemas/Body_upload_files_api_agents__agent_id__uploads_post"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UploadedBatch"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/agents/{agent_id}/uploads/{filename}": {
      "get": {
        "summary": "Serve Upload",
        "description": "Serve one saved upload back (image thumbnails in the timeline).\n\nTraversal-safe: `resolve_upload_path` refuses any name escaping the agent's\nupload dir. 404 when the agent or the file does not exist.",
        "operationId": "serve_upload_api_agents__agent_id__uploads__filename__get",
        "parameters": [
          {
            "name": "agent_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "title": "Agent Id"
            }
          },
          {
            "name": "filename",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Filename"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/work-failed": {
      "post": {
        "summary": "Post Work Failed",
        "description": "Record one failure and route it once without enforcing source assertions.",
        "operationId": "post_work_failed_api_work_failed_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/WorkFailedIn"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WorkFailedResult"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "ActivityEntry": {
        "properties": {
          "text": {
            "type": "string",
            "title": "Text"
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          }
        },
        "type": "object",
        "required": [
          "text",
          "created_at"
        ],
        "title": "ActivityEntry",
        "description": "One entry of an agent's self-reported activity trail (migration 0042).\n\nHistorical only: the SDK write verb (`ava.self.log`) was removed\n2026-08-02, so no new rows appear; this backs the frozen\nGET /api/agents/{id}/activity endpoint."
      },
      "AgentActivity": {
        "properties": {
          "active_seconds": {
            "type": "number",
            "minimum": 0.0,
            "title": "Active Seconds"
          },
          "alive_seconds": {
            "type": "number",
            "minimum": 0.0,
            "title": "Alive Seconds"
          },
          "active_rate": {
            "type": "number",
            "maximum": 1.0,
            "minimum": 0.0,
            "title": "Active Rate"
          },
          "llm_seconds": {
            "type": "number",
            "minimum": 0.0,
            "title": "Llm Seconds"
          },
          "exec_seconds": {
            "type": "number",
            "minimum": 0.0,
            "title": "Exec Seconds"
          }
        },
        "type": "object",
        "required": [
          "active_seconds",
          "alive_seconds",
          "active_rate",
          "llm_seconds",
          "exec_seconds"
        ],
        "title": "AgentActivity",
        "description": "Active-rate: the share of an agent's alive wall-clock it spent actively\nworking versus idle-waiting for its next input. The complement\n(`1 - active_rate`) is how much of its life the agent sat blocked on a\nhuman/peer message — the metric's reason for being: surface how much an\nagent is gated on people. A project-lead agent that decides/approves every\nminute reads near 100%; one woken once a week reads near 0%; a worker that\nruns flat-out then terminates reads ~100%.\n\n`active_seconds` = Σ `duration_seconds` of every non-`claim` graph\n`node_exit` in the window — llm generation + execute_code + before/after\nhooks, i.e. all real processing. `llm_seconds` = Σ `duration_seconds` of\nevery `turn_end` event within the window — the model's generation wall-clock\n(reasoning + output). `exec_seconds` = Σ `duration_seconds` of every\n`node_exit` for the `exec` node — the code execution wall-clock. The `claim`\nnode is excluded because its wall-clock IS the idle-wait (it parks the agent\nin the inbound wait between turns), so it falls into the blocked remainder.\n`alive_seconds` = the agent's alive wall-clock (spawn/resurrect→terminate\nintervals, open tail to now), clipped to the same window — the same lifecycle\nbasis as `AgentTps.agent_lifecycle_tps`. `active_rate` = active/alive, capped\nat 1.0 (a node that began before the window's leading edge counts in full, so\nraw active can momentarily exceed windowed alive); 0.0 when alive is 0. All\nfields follow the request's `?hours=` / `?since_compact=` window."
      },
      "AgentCompact": {
        "properties": {
          "agent_id": {
            "type": "integer",
            "title": "Agent Id"
          },
          "status": {
            "$ref": "#/components/schemas/AgentStatus"
          },
          "label": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Label"
          }
        },
        "type": "object",
        "required": [
          "agent_id",
          "status",
          "label"
        ],
        "title": "AgentCompact",
        "description": "One row of the legacy narrow ``GET /api/agents?fields=compact`` projection."
      },
      "AgentCost": {
        "properties": {
          "cost_usd": {
            "type": "number",
            "minimum": 0.0,
            "title": "Cost Usd"
          },
          "unpriced_calls": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Unpriced Calls"
          },
          "llm_calls": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Llm Calls"
          },
          "tokens_in": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Tokens In"
          },
          "tokens_out": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Tokens Out"
          },
          "tokens_cached": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Tokens Cached"
          },
          "tokens_reasoning": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Tokens Reasoning"
          },
          "cache_hit_pct": {
            "type": "number",
            "maximum": 100.0,
            "minimum": 0.0,
            "title": "Cache Hit Pct"
          }
        },
        "type": "object",
        "required": [
          "cost_usd",
          "unpriced_calls",
          "llm_calls",
          "tokens_in",
          "tokens_out",
          "tokens_cached",
          "tokens_reasoning",
          "cache_hit_pct"
        ],
        "title": "AgentCost",
        "description": "LLM spend + token usage for one agent over the requested window (whole\nlife = ledger days + today's live tail; every `llm_usage` event under its\nagent_id — the agent's \"session\", which spans restarts/resurrects since\nagent_id is stable). `cost_usd` sums the rows' usage-time price snapshots\nonly — never re-priced against the current registry. Calls without a\nsnapshot (unpriced model) contribute 0 to cost and are counted in\n`unpriced_calls`. `cache_hit_pct` = cache_read / input * 100; input=0\ndegrades to 0."
      },
      "AgentEventRow": {
        "properties": {
          "id": {
            "type": "integer",
            "title": "Id"
          },
          "ts": {
            "type": "string",
            "format": "date-time",
            "title": "Ts"
          },
          "agent_id": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Agent Id"
          },
          "level": {
            "type": "string",
            "title": "Level"
          },
          "event": {
            "type": "string",
            "title": "Event"
          },
          "payload": {
            "additionalProperties": true,
            "type": "object",
            "title": "Payload"
          }
        },
        "type": "object",
        "required": [
          "id",
          "ts",
          "agent_id",
          "level",
          "event",
          "payload"
        ],
        "title": "AgentEventRow",
        "description": "One row from the `events` PG table — admin event log entry (category=telemetry/log).\n\n`agent_id` is None for service-level lines (gateway / scheduler / labeler\n/ runner / etc.) and an int for agent process lines. `payload` keeps the\nfull structured record (msg, kwargs from logger.bind / kwargs from the\ncall, plus traceback fields when an exception was attached).\n`level` is LOWERCASE (`debug`/`info`/`warning`/`error`/`critical`): the\nW9 switch from the legacy `agent_events` table (loguru's uppercase `INFO`/`WARNING`/\n`ERROR`) flipped the wire values — consumers matching uppercase must\nadapt."
      },
      "AgentEventsResponse": {
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/AgentEventRow"
            },
            "type": "array",
            "title": "Items"
          }
        },
        "type": "object",
        "required": [
          "items"
        ],
        "title": "AgentEventsResponse",
        "description": "GET /api/cluster/admin/events response — newest-first slice."
      },
      "AgentInspect": {
        "properties": {
          "agent_id": {
            "type": "integer",
            "title": "Agent Id"
          },
          "machine": {
            "type": "string",
            "title": "Machine"
          },
          "liveness_state": {
            "type": "string",
            "enum": [
              "online",
              "offline",
              "unknown"
            ],
            "title": "Liveness State"
          },
          "last_probe_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Probe At"
          },
          "observation": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/AgentObservation"
              },
              {
                "type": "null"
              }
            ]
          },
          "shells_available": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Shells Available"
          },
          "spawned_at": {
            "type": "string",
            "format": "date-time",
            "title": "Spawned At"
          },
          "started_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Started At"
          },
          "window_hours": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/StatsWindowHours"
              },
              {
                "type": "null"
              }
            ]
          },
          "applied_window_hours": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Applied Window Hours"
          },
          "since_compact": {
            "type": "boolean",
            "title": "Since Compact",
            "default": false
          },
          "shells": {
            "items": {
              "$ref": "#/components/schemas/ShellInfo"
            },
            "type": "array",
            "title": "Shells"
          },
          "config_overlay": {
            "additionalProperties": true,
            "type": "object",
            "title": "Config Overlay"
          },
          "notice": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/OpenNotice"
              },
              {
                "type": "null"
              }
            ]
          },
          "cost": {
            "$ref": "#/components/schemas/AgentCost"
          },
          "stats": {
            "$ref": "#/components/schemas/AgentStats"
          },
          "tps": {
            "$ref": "#/components/schemas/AgentTps"
          },
          "activity": {
            "$ref": "#/components/schemas/AgentActivity"
          },
          "heartbeat": {
            "$ref": "#/components/schemas/HeartbeatInfo"
          }
        },
        "type": "object",
        "required": [
          "agent_id",
          "machine",
          "liveness_state",
          "spawned_at",
          "shells",
          "config_overlay",
          "cost",
          "stats",
          "tps",
          "activity",
          "heartbeat"
        ],
        "title": "AgentInspect",
        "description": "GET /api/agents/{id}/inspect response — the per-agent inspector panel.\n\nA single-agent view, in contrast to `/api/stats/dashboard` (the whole fleet\naggregated): the agent's live persistent shells, its frozen per-agent config\noverlay, its current open notice (since #152 at most one), its cumulative\nLLM cost, turn/exec stats, active-rate (working vs blocked-on-a-human),\nidle-heartbeat state, and delivery-obligation state.\n`config_overlay` is the spawn-time field-override map (empty when the agent\nruns on cluster defaults). `shells` is probed on the agent's own machine via\nthe `shell_probe` cluster op — the gateway never runs sessions itself, so a\nsplit deployment sees each agent's shells wherever that agent runs.\n\n`window_hours` echoes the `?hours=` request parameter: None = cumulative\nsince spawn (the default), else `cost` + `stats` + `tps` + `activity`\naggregate only over the past N hours. `applied_window_hours` is the\nactually served window in hours, no greater than `window_hours` and\nclamped to the Loki retention horizon; it is None for whole-life and\nsince-compact reads. `since_compact` echoes\n`?since_compact=`: True = those cover only events since the agent's latest\ncompact halt (`hours` is ignored and `window_hours` is None). `shells`,\n`config_overlay`, `notice`, and `heartbeat` are always current, independent\nof the window."
      },
      "AgentInspectLive": {
        "properties": {
          "agent_id": {
            "type": "integer",
            "title": "Agent Id"
          },
          "machine": {
            "type": "string",
            "title": "Machine"
          },
          "liveness_state": {
            "type": "string",
            "enum": [
              "online",
              "offline",
              "unknown"
            ],
            "title": "Liveness State"
          },
          "last_probe_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Probe At"
          },
          "observation": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/AgentObservation"
              },
              {
                "type": "null"
              }
            ]
          },
          "shells_available": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Shells Available"
          },
          "spawned_at": {
            "type": "string",
            "format": "date-time",
            "title": "Spawned At"
          },
          "started_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Started At"
          },
          "shells": {
            "items": {
              "$ref": "#/components/schemas/ShellInfo"
            },
            "type": "array",
            "title": "Shells"
          },
          "config_overlay": {
            "additionalProperties": true,
            "type": "object",
            "title": "Config Overlay"
          },
          "notice": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/OpenNotice"
              },
              {
                "type": "null"
              }
            ]
          },
          "heartbeat": {
            "$ref": "#/components/schemas/HeartbeatInfo"
          }
        },
        "type": "object",
        "required": [
          "agent_id",
          "machine",
          "liveness_state",
          "spawned_at",
          "shells",
          "config_overlay",
          "heartbeat"
        ],
        "title": "AgentInspectLive",
        "description": "GET /api/agents/{id}/inspect/live response — the inspector's cheap,\nwindow-independent skeleton.\n\nEvery field reflects current database or runner state except\n`heartbeat.last_pause`, which is a single bounded recent-history lookup and\ndegrades to None when Loki is unavailable. The response intentionally omits\ncost, stats, TPS, and activity so switching agents does not wait for the\nexpensive event-history aggregate fan-out."
      },
      "AgentMachineRow": {
        "properties": {
          "name": {
            "type": "string",
            "title": "Name"
          },
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Description"
          },
          "live": {
            "type": "boolean",
            "title": "Live"
          },
          "is_staging": {
            "type": "boolean",
            "title": "Is Staging",
            "default": false
          }
        },
        "type": "object",
        "required": [
          "name",
          "live"
        ],
        "title": "AgentMachineRow",
        "description": "One machine as exposed to agents via ava.agents.list_machines().\n\nIntentionally minimal: name + free-text description + determinate liveness.\nA paused host is still live, but a reached host whose status operation\nfailed has no liveness verdict, so `live=False` until a probe returns a\nconcrete paused value. role / gateway_url stay internal to ops and are not\nsurfaced to agents."
      },
      "AgentMessageEnqueued": {
        "properties": {
          "status": {
            "$ref": "#/components/schemas/AgentStatus"
          },
          "inbound_id": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Inbound Id"
          }
        },
        "type": "object",
        "required": [
          "status"
        ],
        "title": "AgentMessageEnqueued",
        "description": "POST /messages receipt — durable inbound id plus current agent status.\n\nSame-key retries return the same `inbound_id`; `status` may be recomputed\nbecause auto-resurrection can advance while the client reconciles."
      },
      "AgentMessageIn": {
        "properties": {
          "content": {
            "anyOf": [
              {
                "type": "string",
                "minLength": 1
              },
              {
                "items": {
                  "oneOf": [
                    {
                      "$ref": "#/components/schemas/TextContentBlock"
                    },
                    {
                      "$ref": "#/components/schemas/ImageUrlContentBlock"
                    }
                  ],
                  "discriminator": {
                    "propertyName": "type",
                    "mapping": {
                      "image_url": "#/components/schemas/ImageUrlContentBlock",
                      "text": "#/components/schemas/TextContentBlock"
                    }
                  }
                },
                "type": "array",
                "minItems": 1
              }
            ],
            "title": "Content"
          },
          "source": {
            "type": "string",
            "maxLength": 64,
            "minLength": 1,
            "title": "Source"
          }
        },
        "type": "object",
        "required": [
          "content",
          "source"
        ],
        "title": "AgentMessageIn",
        "description": "POST /api/agents/{id}/messages request body — for SDK send_message and\nthe `ava agents send` CLI (which the generated background-run / watcher\ncompletion notices invoke).\n\nPure INSERT + return — the caller does not inspect status.\nAuto-resurrect on the gateway side ensures delivery to any agent,\nterminated or not.\n\n`source` is required — the SDK passes f\"agent:{my_id}\", the generated\nnotices pass shell:N / watcher:N; there is no default to prevent callers\nfrom forgetting and having inbounds silently tagged as \"user\", muddying\nenvelope labels. The valid set is in `shared/envelope.py:validate_source`\n(system / agent:N / user / ui:page:<name> / watcher:N / shell:N /\nschedule:N); an illegal source is intercepted by 422 at the HTTP layer —\notherwise it would land in inbound_messages and the agent claim node\nwould hit ValueError and kill the process."
      },
      "AgentMessagesResponse": {
        "properties": {
          "messages": {
            "items": {
              "additionalProperties": true,
              "type": "object"
            },
            "type": "array",
            "title": "Messages"
          },
          "msg_count": {
            "type": "integer",
            "title": "Msg Count"
          },
          "start_index": {
            "type": "integer",
            "title": "Start Index"
          },
          "has_more": {
            "type": "boolean",
            "title": "Has More"
          }
        },
        "type": "object",
        "required": [
          "messages",
          "msg_count",
          "start_index",
          "has_more"
        ],
        "title": "AgentMessagesResponse",
        "description": "GET /api/agents/{id}/messages response — the raw, unrendered\nstate.messages for programmatic consumers (ops scripts / other agents /\nevals). The rendered, frontend-facing view is GET .../timeline; this is\nthe data layer beneath it.\n\n`messages[i]` is one LangChain BaseMessage `model_dump()` (raw fields:\ntype / content / tool_calls / id / additional_kwargs / ...) and\ncorresponds to `state.messages[start_index + i]`. `msg_count` is the\ntotal length of state.messages; `start_index` is the absolute index of\nthe first returned message. `has_more` reports whether older messages\nexist; when true, `start_index` is the exclusive `before` cursor for the\nnext older page."
      },
      "AgentMetricsItem": {
        "properties": {
          "agent_id": {
            "type": "integer",
            "title": "Agent Id"
          },
          "label": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Label"
          },
          "events": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Events"
          },
          "cost_usd": {
            "type": "number",
            "minimum": 0.0,
            "title": "Cost Usd"
          },
          "llm_calls": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Llm Calls"
          },
          "tokens_in": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Tokens In"
          },
          "tokens_out": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Tokens Out"
          },
          "tokens_cached": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Tokens Cached"
          },
          "cache_hit_pct": {
            "type": "number",
            "maximum": 100.0,
            "minimum": 0.0,
            "title": "Cache Hit Pct"
          },
          "turn_ok": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Turn Ok"
          },
          "turn_total": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Turn Total"
          },
          "exec_ok": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Exec Ok"
          },
          "exec_failed": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Exec Failed"
          }
        },
        "type": "object",
        "required": [
          "agent_id",
          "label",
          "events",
          "cost_usd",
          "llm_calls",
          "tokens_in",
          "tokens_out",
          "tokens_cached",
          "cache_hit_pct",
          "turn_ok",
          "turn_total",
          "exec_ok",
          "exec_failed"
        ],
        "title": "AgentMetricsItem",
        "description": "One agent's row in the fleet metrics breakdown — headline counters\naggregated over its events within the report window. `label` is the\nagent's display name (None = unset; frontend falls back to \"#id\").\n`cost_usd` prices each call via `shared.lm.pricing.cost_usd`; calls on an\nunpriced model contribute 0. `cache_hit_pct` = cached / in * 100 (in=0\ndegrades to 0). `exec_failed` is every exec outcome other than plain\n`exec` — same exec-ok/exec-failed split as the metrics report."
      },
      "AgentMetricsReport": {
        "properties": {
          "meta": {
            "$ref": "#/components/schemas/MetricsMeta"
          },
          "agents": {
            "items": {
              "$ref": "#/components/schemas/AgentMetricsItem"
            },
            "type": "array",
            "title": "Agents"
          }
        },
        "type": "object",
        "required": [
          "meta",
          "agents"
        ],
        "title": "AgentMetricsReport",
        "description": "GET /api/metrics/agents response — the per-agent breakdown behind the\nmetrics page's Agents panel, sorted by cost descending (ties by agent_id).\nSame window semantics as `/api/metrics`; `meta.since_compact` echoes the\noptional since-last-compact filter."
      },
      "AgentObservation": {
        "properties": {
          "machine_probe_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Machine Probe At"
          },
          "machine_probe_valid_until": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Machine Probe Valid Until"
          },
          "runtime_lease_expires_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Runtime Lease Expires At"
          },
          "runtime_owner": {
            "type": "string",
            "const": "unknown",
            "title": "Runtime Owner",
            "default": "unknown"
          }
        },
        "type": "object",
        "title": "AgentObservation",
        "description": "Machine reachability is not proof of runtime ownership.\n\nThis compatibility slice has no generation/owner binding. A fresh lease\nalone, or a PID, must not invent one. UI uses absolute deadlines."
      },
      "AgentRestartRow": {
        "properties": {
          "agent_id": {
            "type": "integer",
            "title": "Agent Id"
          },
          "label": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Label"
          },
          "restarts": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Restarts"
          }
        },
        "type": "object",
        "required": [
          "agent_id",
          "label",
          "restarts"
        ],
        "title": "AgentRestartRow",
        "description": "One agent's restart count within the window."
      },
      "AgentRow": {
        "properties": {
          "agent_id": {
            "type": "integer",
            "title": "Agent Id"
          },
          "spawner": {
            "type": "string",
            "title": "Spawner"
          },
          "fork_source_agent_id": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Fork Source Agent Id"
          },
          "fork_source_checkpoint_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Fork Source Checkpoint Id"
          },
          "status": {
            "$ref": "#/components/schemas/AgentStatus"
          },
          "pid": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Pid"
          },
          "spawned_at": {
            "type": "string",
            "format": "date-time",
            "title": "Spawned At"
          },
          "started_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Started At"
          },
          "last_active_at": {
            "type": "string",
            "format": "date-time",
            "title": "Last Active At"
          },
          "last_inbound_at": {
            "type": "string",
            "format": "date-time",
            "title": "Last Inbound At"
          },
          "label": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Label"
          },
          "machine": {
            "type": "string",
            "title": "Machine"
          },
          "supports_vision": {
            "type": "boolean",
            "title": "Supports Vision"
          },
          "liveness_state": {
            "type": "string",
            "enum": [
              "online",
              "offline",
              "unknown"
            ],
            "title": "Liveness State"
          },
          "last_probe_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Probe At"
          },
          "observation": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/AgentObservation"
              },
              {
                "type": "null"
              }
            ]
          },
          "notices_awaiting_response": {
            "items": {
              "$ref": "#/components/schemas/OpenNotice"
            },
            "type": "array",
            "title": "Notices Awaiting Response"
          },
          "unread_notice_count": {
            "type": "integer",
            "title": "Unread Notice Count"
          },
          "heartbeat_paused_until": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Heartbeat Paused Until"
          }
        },
        "type": "object",
        "required": [
          "agent_id",
          "spawner",
          "fork_source_agent_id",
          "fork_source_checkpoint_id",
          "status",
          "pid",
          "spawned_at",
          "started_at",
          "last_active_at",
          "last_inbound_at",
          "label",
          "machine",
          "supports_vision",
          "liveness_state",
          "last_probe_at",
          "notices_awaiting_response",
          "unread_notice_count",
          "heartbeat_paused_until"
        ],
        "title": "AgentRow",
        "description": "One row of GET /api/agents — full state snapshot of the `agents` table.\n\nIdentical schema to `AgentSnapshot` (the SSE-side type); subclassed\nrather than aliased so OpenAPI keeps the historical name `AgentRow` for\nthe generated frontend types. `last_active_at` is the real-activity clock\n(agents_meta), `last_inbound_at` the latest inbound (issue #183)."
      },
      "AgentStats": {
        "properties": {
          "turn_total": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Turn Total"
          },
          "turn_ok": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Turn Ok"
          },
          "turn_p50_seconds": {
            "type": "number",
            "minimum": 0.0,
            "title": "Turn P50 Seconds"
          },
          "turn_p90_seconds": {
            "type": "number",
            "minimum": 0.0,
            "title": "Turn P90 Seconds"
          },
          "turn_min_seconds": {
            "type": "number",
            "minimum": 0.0,
            "title": "Turn Min Seconds"
          },
          "turn_max_seconds": {
            "type": "number",
            "minimum": 0.0,
            "title": "Turn Max Seconds"
          },
          "exec_ok": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Exec Ok"
          },
          "exec_failed": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Exec Failed"
          }
        },
        "type": "object",
        "required": [
          "turn_total",
          "turn_ok",
          "turn_p50_seconds",
          "turn_p90_seconds",
          "turn_min_seconds",
          "turn_max_seconds",
          "exec_ok",
          "exec_failed"
        ],
        "title": "AgentStats",
        "description": "Cumulative turn + exec counters for one agent (all-time, no window).\n\n`turn_ok` counts `turn_end` events with ok=true (abnormal/cancelled turns\nexcluded); the p50/p90/max are over every turn's `duration_seconds`.\n`exec_ok` is plain `exec` events; `exec_failed` is every other exec outcome\n(exec_failed / exec(timeout) / exec_cancelled; the prefix regex also\ncounts legacy exec_thread_stuck rows for historical continuity)."
      },
      "AgentStatus": {
        "type": "string",
        "enum": [
          "running",
          "idling",
          "restarting",
          "terminated"
        ],
        "title": "AgentStatus"
      },
      "AgentSummary": {
        "properties": {
          "agent_id": {
            "type": "integer",
            "title": "Agent Id"
          },
          "spawner": {
            "type": "string",
            "title": "Spawner"
          },
          "fork_source_agent_id": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Fork Source Agent Id"
          },
          "status": {
            "$ref": "#/components/schemas/AgentStatus"
          },
          "pid": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Pid"
          },
          "spawned_at": {
            "type": "string",
            "format": "date-time",
            "title": "Spawned At"
          },
          "started_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Started At"
          },
          "last_active_at": {
            "type": "string",
            "format": "date-time",
            "title": "Last Active At"
          },
          "last_inbound_at": {
            "type": "string",
            "format": "date-time",
            "title": "Last Inbound At"
          },
          "label": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Label"
          },
          "machine": {
            "type": "string",
            "title": "Machine"
          },
          "supports_vision": {
            "type": "boolean",
            "title": "Supports Vision"
          },
          "liveness_state": {
            "type": "string",
            "enum": [
              "online",
              "offline",
              "unknown"
            ],
            "title": "Liveness State"
          },
          "observation": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/AgentObservation"
              },
              {
                "type": "null"
              }
            ]
          },
          "notices_awaiting_response": {
            "items": {
              "$ref": "#/components/schemas/OpenNotice"
            },
            "type": "array",
            "title": "Notices Awaiting Response"
          },
          "unread_notice_count": {
            "type": "integer",
            "title": "Unread Notice Count"
          },
          "heartbeat_paused_until": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Heartbeat Paused Until"
          }
        },
        "type": "object",
        "required": [
          "agent_id",
          "spawner",
          "fork_source_agent_id",
          "status",
          "pid",
          "spawned_at",
          "started_at",
          "last_active_at",
          "last_inbound_at",
          "label",
          "machine",
          "supports_vision",
          "liveness_state",
          "notices_awaiting_response",
          "unread_notice_count",
          "heartbeat_paused_until"
        ],
        "title": "AgentSummary",
        "description": "One row of ``GET /api/agents?fields=summary`` for roster consumers."
      },
      "AgentTps": {
        "properties": {
          "lm_stage_tps": {
            "type": "number",
            "minimum": 0.0,
            "title": "Lm Stage Tps"
          },
          "agent_lifecycle_tps": {
            "type": "number",
            "minimum": 0.0,
            "title": "Agent Lifecycle Tps"
          }
        },
        "type": "object",
        "required": [
          "lm_stage_tps",
          "agent_lifecycle_tps"
        ],
        "title": "AgentTps",
        "description": "Token-per-second metrics for one agent — two views of throughput.\n\n`lm_stage_tps` = output tokens / cumulative LLM-call wall-clock,\nisolating just the model's generation speed excluding execute_code and framework\noverhead. Denominator is the sum of all `turn_end.duration_seconds` over the window.\n\n`agent_lifecycle_tps` = output tokens / agent's cumulative alive\nwall-clock, covering the full agent runtime including non-LLM time.\nDenominator is the agent's total time in non-terminated status (sum of each\nspawn/resurrect → terminate interval, excluding gaps spent terminated)."
      },
      "AlertIngestResult": {
        "properties": {
          "processed": {
            "type": "integer",
            "title": "Processed"
          },
          "inserted": {
            "type": "integer",
            "title": "Inserted"
          },
          "updated": {
            "type": "integer",
            "title": "Updated"
          },
          "notified": {
            "type": "integer",
            "title": "Notified"
          }
        },
        "type": "object",
        "required": [
          "processed",
          "inserted",
          "updated",
          "notified"
        ],
        "title": "AlertIngestResult",
        "description": "What one ingest POST did — counts the UI/ops can log."
      },
      "AlertRow": {
        "properties": {
          "id": {
            "type": "integer",
            "title": "Id"
          },
          "status": {
            "type": "string",
            "enum": [
              "unresolved",
              "resolved"
            ],
            "title": "Status"
          },
          "severity": {
            "type": "string",
            "enum": [
              "critical",
              "warning",
              "error"
            ],
            "title": "Severity"
          },
          "alertname": {
            "type": "string",
            "title": "Alertname"
          },
          "labels": {
            "additionalProperties": {
              "type": "string"
            },
            "type": "object",
            "title": "Labels"
          },
          "annotations": {
            "additionalProperties": {
              "type": "string"
            },
            "type": "object",
            "title": "Annotations"
          },
          "starts_at": {
            "type": "string",
            "format": "date-time",
            "title": "Starts At"
          },
          "ends_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Ends At"
          },
          "fingerprint": {
            "type": "string",
            "title": "Fingerprint"
          },
          "generator_url": {
            "type": "string",
            "title": "Generator Url"
          },
          "source": {
            "type": "string",
            "title": "Source"
          },
          "notified_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Notified At"
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          },
          "updated_at": {
            "type": "string",
            "format": "date-time",
            "title": "Updated At"
          }
        },
        "type": "object",
        "required": [
          "id",
          "status",
          "severity",
          "alertname",
          "labels",
          "annotations",
          "starts_at",
          "ends_at",
          "fingerprint",
          "generator_url",
          "source",
          "notified_at",
          "created_at",
          "updated_at"
        ],
        "title": "AlertRow",
        "description": "One alerts row as the UI sees it (list + SSE frames)."
      },
      "AlertWebhookAlert": {
        "properties": {
          "status": {
            "type": "string",
            "enum": [
              "firing",
              "resolved"
            ],
            "title": "Status",
            "default": "firing"
          },
          "labels": {
            "additionalProperties": {
              "type": "string"
            },
            "type": "object",
            "title": "Labels"
          },
          "annotations": {
            "additionalProperties": {
              "type": "string"
            },
            "type": "object",
            "title": "Annotations"
          },
          "startsAt": {
            "type": "string",
            "title": "Startsat",
            "default": ""
          },
          "endsAt": {
            "type": "string",
            "title": "Endsat",
            "default": ""
          },
          "fingerprint": {
            "type": "string",
            "title": "Fingerprint",
            "default": ""
          },
          "generatorURL": {
            "type": "string",
            "title": "Generatorurl",
            "default": ""
          },
          "values": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "title": "Values"
          }
        },
        "type": "object",
        "title": "AlertWebhookAlert",
        "description": "One alert instance inside an Alertmanager webhook POST.\n\n``labels`` / ``annotations`` are free-form; extra per-alert fields\nAlertmanager adds over time (``silenceURL``, ``imageURL``, …) are\ntolerated, not rejected."
      },
      "AlertWebhookPayload": {
        "properties": {
          "source": {
            "type": "string",
            "title": "Source",
            "default": "grafana"
          },
          "status": {
            "anyOf": [
              {
                "type": "string",
                "enum": [
                  "firing",
                  "resolved"
                ]
              },
              {
                "type": "null"
              }
            ],
            "title": "Status"
          },
          "alerts": {
            "items": {
              "$ref": "#/components/schemas/AlertWebhookAlert"
            },
            "type": "array",
            "title": "Alerts"
          }
        },
        "type": "object",
        "title": "AlertWebhookPayload",
        "description": "Top level of an Alertmanager webhook POST.\n\nTolerates both the full Alertmanager v4 envelope (version/groupKey/\nreceiver/commonLabels/… — ignored here) and the slimmer Grafana-managed\nwebhook shape; only ``status`` + ``alerts[]`` matter to the store.\n``source`` tags the row's provenance: the webhook omits it (default\n``grafana``), while the health probe posts ``source=\"health-probe\"`` and\nthe liveness pass ``source=\"machine-probe\"``."
      },
      "AlertsListMeta": {
        "properties": {
          "window": {
            "type": "string",
            "title": "Window"
          },
          "total": {
            "type": "integer",
            "title": "Total"
          },
          "unresolved_count": {
            "type": "integer",
            "title": "Unresolved Count"
          }
        },
        "type": "object",
        "required": [
          "window",
          "total",
          "unresolved_count"
        ],
        "title": "AlertsListMeta",
        "description": "Query provenance of one GET /api/alerts response."
      },
      "AlertsListResponse": {
        "properties": {
          "alerts": {
            "items": {
              "$ref": "#/components/schemas/AlertRow"
            },
            "type": "array",
            "title": "Alerts"
          },
          "meta": {
            "$ref": "#/components/schemas/AlertsListMeta"
          }
        },
        "type": "object",
        "required": [
          "alerts",
          "meta"
        ],
        "title": "AlertsListResponse",
        "description": "Unresolved-first alert history + the count the UI badge shows."
      },
      "Body_upload_files_api_agents__agent_id__uploads_post": {
        "properties": {
          "files": {
            "items": {
              "type": "string",
              "contentMediaType": "application/octet-stream"
            },
            "type": "array",
            "title": "Files"
          }
        },
        "type": "object",
        "required": [
          "files"
        ],
        "title": "Body_upload_files_api_agents__agent_id__uploads_post"
      },
      "CancelRequest": {
        "properties": {
          "agent_id": {
            "type": "integer",
            "exclusiveMinimum": 0.0,
            "title": "Agent Id"
          }
        },
        "type": "object",
        "required": [
          "agent_id"
        ],
        "title": "CancelRequest",
        "description": "POST /api/cancel request body — pause/stop the agent, addressed by id."
      },
      "CancelRequested": {
        "properties": {
          "status": {
            "type": "string",
            "enum": [
              "enqueued",
              "already_terminated"
            ],
            "title": "Status"
          }
        },
        "type": "object",
        "required": [
          "status"
        ],
        "title": "CancelRequested",
        "description": "POST /api/cancel response.\n\n`enqueued`: a durable kind='cancel' inbound was INSERTed. The in-flight\n    llm/exec node interrupts on it if one is running; otherwise the next\n    claim pass halts the agent to idle. The process stays alive.\n`already_terminated`: agent is dead — nothing to pause."
      },
      "ClusterOpRequest": {
        "properties": {
          "origin": {
            "type": "string",
            "title": "Origin",
            "default": "user"
          },
          "mode": {
            "type": "string",
            "title": "Mode",
            "default": "smooth"
          },
          "force": {
            "type": "boolean",
            "title": "Force",
            "default": false
          },
          "dry_run": {
            "type": "boolean",
            "title": "Dry Run",
            "default": false
          }
        },
        "type": "object",
        "title": "ClusterOpRequest",
        "description": "POST /api/cluster/rollout | /api/cluster/restart request body — fully\noptional; a body-less POST uses defaults.\n\n`origin` names the trigger, same convention as `resurrected_by`:\ndefault \"user\" (the frontend buttons), SDK passes f\"agent:{my_id}\".\nIt heads the rollout/restart log and (rollout only) lands in the\ncluster pin's `updated_by`, so \"who moved the cluster\" is answerable\nfrom the log file or the pin row alone."
      },
      "ClusterPanel": {
        "properties": {
          "current_machine": {
            "type": "string",
            "title": "Current Machine"
          },
          "current_serve_gateway": {
            "type": "boolean",
            "title": "Current Serve Gateway"
          },
          "current_serve_agent_runner": {
            "type": "boolean",
            "title": "Current Serve Agent Runner"
          },
          "current_serve_observability_station": {
            "type": "boolean",
            "title": "Current Serve Observability Station",
            "default": false
          },
          "current_paused": {
            "type": "boolean",
            "title": "Current Paused"
          },
          "current_orchestration": {
            "anyOf": [
              {
                "type": "string",
                "enum": [
                  "rollout",
                  "restart",
                  "update"
                ]
              },
              {
                "type": "null"
              }
            ],
            "title": "Current Orchestration"
          },
          "machines": {
            "items": {
              "$ref": "#/components/schemas/MachineStatus"
            },
            "type": "array",
            "title": "Machines"
          },
          "cluster_target_sha": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Cluster Target Sha"
          },
          "last_update": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/LastUpdate"
              },
              {
                "type": "null"
              }
            ]
          },
          "cluster_last_known_good_sha": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Cluster Last Known Good Sha"
          }
        },
        "type": "object",
        "required": [
          "current_machine",
          "current_serve_gateway",
          "current_serve_agent_runner",
          "current_paused",
          "machines"
        ],
        "title": "ClusterPanel",
        "description": "GET /api/status cluster sub-section — multi-machine view.\n\n`current_*` is the perspective of the gateway that received this\nrequest; `machines` is every machine registered in DB + live probe\nresults."
      },
      "ClusterStatus": {
        "properties": {
          "machine_name": {
            "type": "string",
            "title": "Machine Name"
          },
          "serve_gateway": {
            "type": "boolean",
            "title": "Serve Gateway"
          },
          "serve_agent_runner": {
            "type": "boolean",
            "title": "Serve Agent Runner"
          },
          "serve_observability_station": {
            "type": "boolean",
            "title": "Serve Observability Station",
            "default": false
          },
          "paused": {
            "type": "boolean",
            "title": "Paused"
          },
          "current_orchestration": {
            "anyOf": [
              {
                "type": "string",
                "enum": [
                  "rollout",
                  "restart",
                  "update"
                ]
              },
              {
                "type": "null"
              }
            ],
            "title": "Current Orchestration"
          },
          "last_updater_outcome": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/UpdaterOutcome"
              },
              {
                "type": "null"
              }
            ]
          },
          "head_sha": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Head Sha"
          },
          "running_sha": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Running Sha"
          },
          "shell_count": {
            "type": "integer",
            "title": "Shell Count",
            "default": 0
          },
          "agent_host_online": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Agent Host Online"
          },
          "watchdog_online": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Watchdog Online"
          },
          "agent_count": {
            "type": "integer",
            "title": "Agent Count",
            "default": 0
          },
          "session_count": {
            "type": "integer",
            "title": "Session Count",
            "default": 0
          },
          "agent_groups": {
            "items": {
              "additionalProperties": true,
              "type": "object"
            },
            "type": "array",
            "title": "Agent Groups",
            "default": []
          },
          "resource": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/ResourceSample"
              },
              {
                "type": "null"
              }
            ]
          }
        },
        "type": "object",
        "required": [
          "machine_name",
          "serve_gateway",
          "serve_agent_runner",
          "paused"
        ],
        "title": "ClusterStatus",
        "description": "`GET /api/cluster/status` response body — this host's snapshot in the\nmulti-machine view.\n\n`ava cluster status` CLI and the future monitoring page consume the\nsame payload."
      },
      "ClusterTransitionPayload": {
        "properties": {
          "deploy_holder": {
            "type": "string",
            "minLength": 1,
            "title": "Deploy Holder"
          },
          "deploy_acquired_at": {
            "type": "string",
            "format": "date-time",
            "title": "Deploy Acquired At"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "deploy_holder",
          "deploy_acquired_at"
        ],
        "title": "ClusterTransitionPayload",
        "description": "Exact deploy-lease capability for one stop/resume generation.\n\nBoth fields are required. A delayed request from generation A must not be\nauthorized by whichever generation happens to own the lease when it lands."
      },
      "CommandItem": {
        "properties": {
          "name": {
            "type": "string",
            "title": "Name"
          },
          "description": {
            "type": "string",
            "title": "Description"
          },
          "instruction_hint": {
            "type": "string",
            "title": "Instruction Hint"
          }
        },
        "type": "object",
        "required": [
          "name",
          "description",
          "instruction_hint"
        ],
        "title": "CommandItem",
        "description": "One composer command, as surfaced in the `/`-autocomplete: name +\ndescription + `instruction_hint` (placeholder for the single natural-language\ninstruction typed after the name). The body is intentionally absent —\nexpansion happens server-side in the agent's claim node, so the template\nnever reaches the browser."
      },
      "CompactEnqueued": {
        "properties": {
          "mode": {
            "type": "string",
            "enum": [
              "framework",
              "agent"
            ],
            "title": "Mode"
          },
          "agent_id": {
            "type": "integer",
            "title": "Agent Id"
          },
          "status": {
            "type": "string",
            "const": "enqueued",
            "title": "Status"
          }
        },
        "type": "object",
        "required": [
          "mode",
          "agent_id",
          "status"
        ],
        "title": "CompactEnqueued",
        "description": "POST /api/agents/{id}/compact response — returns immediately after\npending insert, does not wait for the kernel loop to finish."
      },
      "ConfigFieldView": {
        "properties": {
          "name": {
            "type": "string",
            "title": "Name"
          },
          "field_type": {
            "type": "string",
            "title": "Field Type"
          },
          "current_value": {
            "title": "Current Value"
          },
          "default_value": {
            "title": "Default Value"
          },
          "description": {
            "type": "string",
            "title": "Description"
          },
          "group": {
            "type": "string",
            "title": "Group"
          },
          "capability": {
            "type": "string",
            "enum": [
              "gateway",
              "agent-runner",
              "common"
            ],
            "title": "Capability"
          },
          "restart_required": {
            "type": "string",
            "title": "Restart Required"
          },
          "writable": {
            "type": "boolean",
            "title": "Writable"
          },
          "sensitive": {
            "type": "boolean",
            "title": "Sensitive"
          },
          "env_var": {
            "type": "string",
            "title": "Env Var"
          },
          "scope": {
            "type": "string",
            "title": "Scope"
          },
          "per_agent": {
            "type": "boolean",
            "title": "Per Agent"
          },
          "remote_writable": {
            "type": "boolean",
            "title": "Remote Writable"
          },
          "choices": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Choices"
          },
          "can_enable": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Can Enable"
          },
          "reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Reason"
          }
        },
        "type": "object",
        "required": [
          "name",
          "field_type",
          "current_value",
          "default_value",
          "description",
          "group",
          "capability",
          "restart_required",
          "writable",
          "sensitive",
          "env_var",
          "scope",
          "per_agent",
          "remote_writable"
        ],
        "title": "ConfigFieldView",
        "description": "View of a single config field — server-side metadata + current value.\n\nThe server composes metadata fields and sends them down; the frontend\ndoes not do any \"infer config back\" logic — it displays + sends PUT\nback by name.\n\n`can_enable` / `reason` are a host-side read-time capability hint, only\npopulated for host-scope fields with a static precondition (today\n`browser_enabled`: can the host actually run a headed browser). Both stay\nNone for non-host fields and for host fields with no pre-grey gate (free\ntext / numbers validated only at write time)."
      },
      "ConfigFieldWriteResult": {
        "properties": {
          "ok": {
            "type": "boolean",
            "title": "Ok"
          },
          "reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Reason"
          }
        },
        "type": "object",
        "required": [
          "ok"
        ],
        "title": "ConfigFieldWriteResult",
        "description": "Per-field verdict of a PUT /api/config write — `ok` plus a human-readable\n`reason` when the field was rejected (capability / scope / unknown).\n\n`reason` is None iff ok. The host-side op is authoritative for host-scope\nfields (re-validates remote_writable + capability); cluster fields can't\nfail a capability check, so they don't appear here on a self PUT."
      },
      "ConfigView": {
        "properties": {
          "fields": {
            "items": {
              "$ref": "#/components/schemas/ConfigFieldView"
            },
            "type": "array",
            "title": "Fields"
          },
          "raw_overrides": {
            "additionalProperties": true,
            "type": "object",
            "title": "Raw Overrides"
          },
          "machine_capabilities": {
            "items": {
              "type": "string",
              "enum": [
                "gateway",
                "agent-runner",
                "observability-station"
              ]
            },
            "type": "array",
            "title": "Machine Capabilities"
          }
        },
        "type": "object",
        "required": [
          "fields",
          "raw_overrides",
          "machine_capabilities"
        ],
        "title": "ConfigView",
        "description": "GET /api/config response — grouped field list + raw_overrides (PUT body source).\n\nraw_overrides is config.json's current content — the frontend deltas\nagainst this and returns the result via PUT.\n\nmachine_capabilities is the target machine's capability set (`gateway` and/or\n`agent-runner` and/or `observability-station`) — the gateway's own for the\nCluster (self) view, the `?machine=`\nhost's for a remote view. The panel uses it to pick which capability sections to\nrender on a remote view: a pure agent-runner shows only its agent-runner + common\nsections, while a co-located gateway,agent-runner box shows the gateway section too\n(so its gateway-daemon toggles stay editable). The Cluster view always shows all\nsections regardless (it edits every capability's cluster fields)."
      },
      "ConfigWriteResult": {
        "properties": {
          "applied": {
            "type": "boolean",
            "title": "Applied"
          },
          "results": {
            "additionalProperties": {
              "$ref": "#/components/schemas/ConfigFieldWriteResult"
            },
            "type": "object",
            "title": "Results"
          },
          "restart_required": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Restart Required"
          }
        },
        "type": "object",
        "required": [
          "applied",
          "results",
          "restart_required"
        ],
        "title": "ConfigWriteResult",
        "description": "PUT /api/config response — per-field results + whether anything was applied.\n\n`applied` is True iff every field passed and the write committed (atomic:\none bad field -> nothing written). `restart_required` is the union of the\nwritten fields' restart targets (\"agent\" | \"ops\" | \"gateway\" | \"all\"), for\nthe per-machine \"needs restart\" banner."
      },
      "ContextBreakdownResponse": {
        "properties": {
          "total_input_tokens": {
            "type": "integer",
            "title": "Total Input Tokens"
          },
          "estimated_total": {
            "type": "integer",
            "title": "Estimated Total"
          },
          "max_input_tokens": {
            "type": "integer",
            "title": "Max Input Tokens",
            "default": 0
          },
          "soft_compact_tokens": {
            "type": "integer",
            "title": "Soft Compact Tokens",
            "default": 0
          },
          "hard_compact_tokens": {
            "type": "integer",
            "title": "Hard Compact Tokens",
            "default": 0
          },
          "sections": {
            "items": {
              "$ref": "#/components/schemas/ContextSection"
            },
            "type": "array",
            "title": "Sections"
          },
          "categories": {
            "items": {
              "$ref": "#/components/schemas/ContextCategory"
            },
            "type": "array",
            "title": "Categories"
          }
        },
        "type": "object",
        "required": [
          "total_input_tokens",
          "estimated_total",
          "sections",
          "categories"
        ],
        "title": "ContextBreakdownResponse",
        "description": "GET /api/agents/{id}/context-breakdown — how the agent's context window is\nspent, for the composer's breakdown panel (lazy-loaded when the panel opens).\n\nEvery token value is a chars/4 estimate proportionally normalized to\n`total_input_tokens` (the last LLM call's real input_tokens), so `categories`\nsum exactly to `total_input_tokens` while the distribution stays approximate —\nzero extra API cost, zero KV-cache impact. `sections` break down the\n`system_prompt` category. When no LLM call has run yet (`total_input_tokens ==\n0`) the values are the raw chars/4 estimates and `estimated_total` is their\nsum. `max_input_tokens` / `soft_compact_tokens` / `hard_compact_tokens` mirror\nthe token-usage endpoint (0 when the model window is unknown)."
      },
      "ContextCategory": {
        "properties": {
          "kind": {
            "type": "string",
            "title": "Kind"
          },
          "tokens": {
            "type": "integer",
            "title": "Tokens"
          }
        },
        "type": "object",
        "required": [
          "kind",
          "tokens"
        ],
        "title": "ContextCategory",
        "description": "One context bucket (system_prompt / compact_summary / cluster_memory /\nagent_memory / context_note / user_input / agent_messages / automation /\nreasoning / output / tool_call / tool_response) with its normalized token\nestimate. Inbound messages split by source: user_input (a human turn),\nagent_messages (a peer agent), automation (a machine/framework wakeup)."
      },
      "ContextSection": {
        "properties": {
          "name": {
            "type": "string",
            "title": "Name"
          },
          "tokens": {
            "type": "integer",
            "title": "Tokens"
          },
          "children": {
            "items": {
              "$ref": "#/components/schemas/ContextSection"
            },
            "type": "array",
            "title": "Children"
          }
        },
        "type": "object",
        "required": [
          "name",
          "tokens"
        ],
        "title": "ContextSection",
        "description": "One node of the system prompt's recursive section breakdown: a heading — or\nthe `(preamble)` / `(intro)` prose before the first sub-heading — with its\n(normalized) token estimate. A section over ~1000 tokens is drilled into its\nnext-level sub-headings as `children` (recursively); a smaller section, or one\nwith no deeper heading, is a leaf (`children == []`). When `children` is\nnon-empty their tokens sum to this node's `tokens`."
      },
      "DefaultModelView": {
        "properties": {
          "model": {
            "type": "string",
            "title": "Model"
          },
          "source": {
            "type": "string",
            "enum": [
              "cluster",
              "config"
            ],
            "title": "Source"
          }
        },
        "type": "object",
        "required": [
          "model",
          "source"
        ],
        "title": "DefaultModelView",
        "description": "GET/PUT /api/config/default-model — the model a new agent is born on.\n\n`source` names where `model` came from, because the two cases behave\ndifferently on the next `.env` edit: `cluster` is the DB row (a deliberate\nchoice through this endpoint, which outranks `.env`), `config` is the ordinary\nchain (`.env` AVA_MODEL, else the code default) showing through because no\ncluster choice has been made."
      },
      "DefaultModelWrite": {
        "properties": {
          "model": {
            "type": "string",
            "title": "Model"
          }
        },
        "type": "object",
        "required": [
          "model"
        ],
        "title": "DefaultModelWrite",
        "description": "PUT /api/config/default-model body. `model` must be a spawnable id from the\nregistry roster; anything else is rejected rather than stored."
      },
      "EscalationNoticeItem": {
        "properties": {
          "id": {
            "type": "integer",
            "title": "Id"
          },
          "title": {
            "type": "string",
            "title": "Title"
          },
          "priority": {
            "$ref": "#/components/schemas/Priority"
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          },
          "task_id": {
            "type": "integer",
            "title": "Task Id"
          },
          "task_title": {
            "type": "string",
            "title": "Task Title"
          },
          "task_status": {
            "type": "string",
            "title": "Task Status"
          },
          "owner_id": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Owner Id"
          },
          "owner_label": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Owner Label"
          },
          "reminder_count": {
            "type": "integer",
            "title": "Reminder Count"
          },
          "updated_at": {
            "type": "string",
            "format": "date-time",
            "title": "Updated At"
          }
        },
        "type": "object",
        "required": [
          "id",
          "title",
          "priority",
          "created_at",
          "task_id",
          "task_title",
          "task_status",
          "owner_id",
          "owner_label",
          "reminder_count",
          "updated_at"
        ],
        "title": "EscalationNoticeItem",
        "description": "One open task escalation for GET /api/notices/escalations.\n\nThe operator queue joins the escalation notice to its task and current\nowner, so its consumer can decide whether to reassign, cancel, or retain\nthe task without fetching another resource."
      },
      "EventResolutionCreate": {
        "properties": {
          "category": {
            "type": "string",
            "enum": [
              "telemetry",
              "log"
            ],
            "title": "Category"
          },
          "level": {
            "type": "string",
            "enum": [
              "warning",
              "error",
              "critical"
            ],
            "title": "Level"
          },
          "event_name": {
            "type": "string",
            "maxLength": 255,
            "minLength": 1,
            "title": "Event Name"
          },
          "source": {
            "type": "string",
            "maxLength": 255,
            "minLength": 1,
            "title": "Source"
          },
          "agent_id": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Agent Id"
          },
          "note": {
            "type": "string",
            "maxLength": 4000,
            "title": "Note",
            "default": ""
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "category",
          "level",
          "event_name",
          "source"
        ],
        "title": "EventResolutionCreate",
        "description": "One immutable event class to dismiss through the authenticated API."
      },
      "EventResolutionListResponse": {
        "properties": {
          "resolutions": {
            "items": {
              "$ref": "#/components/schemas/EventResolutionRow"
            },
            "type": "array",
            "title": "Resolutions"
          }
        },
        "type": "object",
        "required": [
          "resolutions"
        ],
        "title": "EventResolutionListResponse",
        "description": "Status-filtered resolution history for the ops agent's review cycle."
      },
      "EventResolutionRow": {
        "properties": {
          "id": {
            "type": "integer",
            "title": "Id"
          },
          "category": {
            "type": "string",
            "enum": [
              "telemetry",
              "log"
            ],
            "title": "Category"
          },
          "level": {
            "type": "string",
            "enum": [
              "warning",
              "error",
              "critical"
            ],
            "title": "Level"
          },
          "event_name": {
            "type": "string",
            "title": "Event Name"
          },
          "source": {
            "type": "string",
            "title": "Source"
          },
          "agent_id": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Agent Id"
          },
          "dismissed_by": {
            "type": "integer",
            "title": "Dismissed By"
          },
          "note": {
            "type": "string",
            "title": "Note"
          },
          "status": {
            "type": "string",
            "enum": [
              "dismissed",
              "reopened"
            ],
            "title": "Status"
          },
          "dismissed_at": {
            "type": "string",
            "format": "date-time",
            "title": "Dismissed At"
          },
          "reopened_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Reopened At"
          },
          "burst_count": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Burst Count"
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          },
          "updated_at": {
            "type": "string",
            "format": "date-time",
            "title": "Updated At"
          }
        },
        "type": "object",
        "required": [
          "id",
          "category",
          "level",
          "event_name",
          "source",
          "agent_id",
          "dismissed_by",
          "note",
          "status",
          "dismissed_at",
          "reopened_at",
          "burst_count",
          "created_at",
          "updated_at"
        ],
        "title": "EventResolutionRow",
        "description": "One persisted class dismissal, including reopened history metadata."
      },
      "EventRow": {
        "properties": {
          "id": {
            "type": "integer",
            "title": "Id"
          },
          "ts": {
            "type": "string",
            "format": "date-time",
            "title": "Ts"
          },
          "trace_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Trace Id"
          },
          "span_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Span Id"
          },
          "agent_id": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Agent Id"
          },
          "machine": {
            "type": "string",
            "title": "Machine"
          },
          "process": {
            "type": "string",
            "title": "Process"
          },
          "category": {
            "type": "string",
            "title": "Category"
          },
          "event_name": {
            "type": "string",
            "title": "Event Name"
          },
          "tier": {
            "type": "string",
            "enum": [
              "business",
              "anomaly",
              "observation",
              "noise"
            ],
            "title": "Tier"
          },
          "level": {
            "type": "string",
            "title": "Level"
          },
          "source": {
            "type": "string",
            "title": "Source"
          },
          "target_agent_id": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Target Agent Id"
          },
          "attributes": {
            "additionalProperties": true,
            "type": "object",
            "title": "Attributes"
          }
        },
        "type": "object",
        "required": [
          "id",
          "ts",
          "trace_id",
          "span_id",
          "agent_id",
          "machine",
          "process",
          "category",
          "event_name",
          "tier",
          "level",
          "source",
          "target_agent_id",
          "attributes"
        ],
        "title": "EventRow",
        "description": "One row from the unified `events` table — the single event stream.\n\nEvery signal shares this shape (event-system design doc §1): audit\n(legacy `event_log`), telemetry and log (formerly `agent_events`) all live\nhere, written through the unified emitter (`shared/telemetry.py`).\n`trace_id` is the correlation key — one turn = one trace id, every event\ninside it carries the same value. `agent_id` is None for service-level\nevents (gateway / daemons); `machine` is the host dimension. `level` is\nLOWERCASE (`debug`/`info`/`warning`/`error`/`critical`) — the retired\n`agent_events` mirror stored loguru's uppercase; the W9 read-path switch\nflipped API consumers to lowercase."
      },
      "EventsMeta": {
        "properties": {
          "total": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Total"
          },
          "window_from": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Window From"
          },
          "window_to": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Window To"
          },
          "limit": {
            "type": "integer",
            "title": "Limit"
          },
          "offset": {
            "type": "integer",
            "title": "Offset"
          },
          "has_more": {
            "type": "boolean",
            "title": "Has More"
          },
          "generated_at": {
            "type": "string",
            "title": "Generated At"
          }
        },
        "type": "object",
        "required": [
          "total",
          "window_from",
          "window_to",
          "limit",
          "offset",
          "has_more",
          "generated_at"
        ],
        "title": "EventsMeta",
        "description": "GET /api/events response header — effective window + pagination\nstate. `window_from`/`window_to` echo the applied time window\n(`None` when unbounded high; `window_from` is never `None` — the\ncomputed `now - hours` when the request used `hours`, the explicit\n`from` when one was given, and otherwise the default lower bound\n`now - 24h`). `has_more` (from the list fetch's +1 lookahead) tells a\npaging client whether another `offset` page exists. `total` is the exact\nfiltered row count before paging, computed only when the request asked\nfor it (`with_total=1`) — `None` otherwise (it costs a full-window count\naggregation)."
      },
      "EventsResponse": {
        "properties": {
          "meta": {
            "$ref": "#/components/schemas/EventsMeta"
          },
          "items": {
            "items": {
              "$ref": "#/components/schemas/EventRow"
            },
            "type": "array",
            "title": "Items"
          }
        },
        "type": "object",
        "required": [
          "meta",
          "items"
        ],
        "title": "EventsResponse",
        "description": "GET /api/events response — newest-first slice of the unified event\nstream (`ts DESC`, `id DESC` tiebreak so `limit`/`offset` paging is stable\nacross same-`ts` rows)."
      },
      "FleetGraphEdge": {
        "properties": {
          "from_agent": {
            "type": "integer",
            "title": "From Agent"
          },
          "to_agent": {
            "type": "integer",
            "title": "To Agent"
          },
          "event_type": {
            "type": "string",
            "title": "Event Type"
          },
          "weight": {
            "type": "number",
            "title": "Weight"
          },
          "event_count": {
            "type": "integer",
            "title": "Event Count"
          },
          "last_seen_at": {
            "type": "string",
            "title": "Last Seen At"
          }
        },
        "type": "object",
        "required": [
          "from_agent",
          "to_agent",
          "event_type",
          "weight",
          "event_count",
          "last_seen_at"
        ],
        "title": "FleetGraphEdge",
        "description": "One directed edge in the fleet graph: from_agent → to_agent."
      },
      "FleetGraphNode": {
        "properties": {
          "agent_id": {
            "type": "integer",
            "title": "Agent Id"
          },
          "label": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Label"
          },
          "status": {
            "$ref": "#/components/schemas/AgentStatus"
          },
          "liveness_state": {
            "type": "string",
            "enum": [
              "online",
              "offline",
              "unknown"
            ],
            "title": "Liveness State"
          },
          "spawner": {
            "type": "string",
            "title": "Spawner"
          },
          "machine": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Machine"
          },
          "node_score": {
            "type": "number",
            "title": "Node Score"
          },
          "total_tokens": {
            "type": "integer",
            "title": "Total Tokens"
          }
        },
        "type": "object",
        "required": [
          "agent_id",
          "label",
          "status",
          "liveness_state",
          "spawner",
          "machine",
          "node_score",
          "total_tokens"
        ],
        "title": "FleetGraphNode",
        "description": "One agent node in the fleet force-directed graph."
      },
      "FleetGraphResponse": {
        "properties": {
          "nodes": {
            "items": {
              "$ref": "#/components/schemas/FleetGraphNode"
            },
            "type": "array",
            "title": "Nodes"
          },
          "edges": {
            "items": {
              "$ref": "#/components/schemas/FleetGraphEdge"
            },
            "type": "array",
            "title": "Edges"
          },
          "stale": {
            "type": "boolean",
            "title": "Stale",
            "default": false
          },
          "truncated": {
            "type": "boolean",
            "title": "Truncated",
            "default": false
          },
          "telemetry_stale": {
            "type": "boolean",
            "title": "Telemetry Stale",
            "default": false
          },
          "snapshot_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Snapshot At"
          }
        },
        "type": "object",
        "required": [
          "nodes",
          "edges"
        ],
        "title": "FleetGraphResponse",
        "description": "GET /api/fleet/graph response.\n\n`stale` identifies data-level degradation: a fallback snapshot, truncated\nedge stream, or failed source response. `telemetry_stale` separately says\nthe heartbeat guard was old or missing while this otherwise fresh graph was\nread. `snapshot_at` records when a successful graph snapshot was produced;\na fallback retains that timestamp so clients can show its age."
      },
      "GuideDraftRequest": {
        "properties": {
          "nl": {
            "type": "string",
            "title": "Nl",
            "description": "Natural-language operations request for the guide agent."
          }
        },
        "type": "object",
        "required": [
          "nl"
        ],
        "title": "GuideDraftRequest"
      },
      "GuideDraftResponse": {
        "properties": {
          "agent_id": {
            "type": "integer",
            "title": "Agent Id"
          }
        },
        "type": "object",
        "required": [
          "agent_id"
        ],
        "title": "GuideDraftResponse"
      },
      "HTTPValidationError": {
        "properties": {
          "detail": {
            "items": {
              "$ref": "#/components/schemas/ValidationError"
            },
            "type": "array",
            "title": "Detail"
          }
        },
        "type": "object",
        "title": "HTTPValidationError"
      },
      "HeartbeatInfo": {
        "properties": {
          "interval_s": {
            "type": "integer",
            "title": "Interval S"
          },
          "next_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next At"
          },
          "paused_until": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Paused Until"
          },
          "heartbeat_pending": {
            "type": "boolean",
            "title": "Heartbeat Pending",
            "default": false
          },
          "last_pause": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/HeartbeatLastPause"
              },
              {
                "type": "null"
              }
            ]
          }
        },
        "type": "object",
        "required": [
          "interval_s"
        ],
        "title": "HeartbeatInfo",
        "description": "Idle check-in heartbeat state for one agent — mutually-exclusive display\nstates the panel renders:\n\n- idle-family (idling / restarting — the statuses the fleet view\n  projects to \"Idle\") & not paused & no fresh wake queued: `next_at` is\n  set — the daemon's projected check-in due time: the later of\n  `last_active_at + idle_threshold + (id mod JITTER_SPAN_S)` and\n  `last_heartbeat_at + interval_s` when a prior check-in exists. The daemon\n  dispatches the actual check-in at its first poll tick at/after that (at\n  most one 15s dispatch step later), so `next_at` is the earliest possible\n  check-in, and never later than what the daemon does. An overdue\n  projection renders as \"due\" in the frontend, never as a past time;\n  everything else off.\n- idle-family & not paused & a *fresh* wake already queued (created within\n  the daemon's 900s `STALE_PENDING_S` freshness window): `heartbeat_pending`\n  is True — the daemon suppresses check-ins while a fresh inbound is pending\n  (its `NOT EXISTS` guard, windowed by `STALE_PENDING_S`), so no future\n  check-in is projected. This is the state a stuck agent sits in (a\n  heartbeat check-in it never woke to process); rendering it honestly is\n  what stops the panel from projecting a nonsensical past `next_at`. A\n  pending inbound older than the window is stale — the daemon checks in on\n  the agent anyway, so the panel projects `next_at` instead.\n- paused: `paused_until` is set (the active suppression end); `next_at` None.\n- running or terminated: all off — an active agent never gets a check-in,\n  and a dead one never will.\n\n`interval_s` is the cluster heartbeat interval in seconds (context for the\n`next_at` projection). `last_pause` is the most recent pause from history,\nindependent of the current state."
      },
      "HeartbeatLastPause": {
        "properties": {
          "at": {
            "type": "string",
            "format": "date-time",
            "title": "At"
          },
          "duration_s": {
            "type": "number",
            "minimum": 0.0,
            "title": "Duration S"
          }
        },
        "type": "object",
        "required": [
          "at",
          "duration_s"
        ],
        "title": "HeartbeatLastPause",
        "description": "The agent's most recent heartbeat pause — when it last opted out of idle\ncheck-ins and the length it asked for. Sourced from the newest\n`heartbeat_paused` events row; None on AgentInspect when the agent has\nnever paused. `at` is when the pause was requested; `duration_s` is the\nrequested window in seconds (the agent's `pause_heartbeat(duration)` arg)."
      },
      "ImageUrlContentBlock": {
        "properties": {
          "type": {
            "type": "string",
            "const": "image_url",
            "title": "Type"
          },
          "image_url": {
            "$ref": "#/components/schemas/ImageUrlRef"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "type",
          "image_url"
        ],
        "title": "ImageUrlContentBlock",
        "description": "Image part of a multimodal chat message (OpenAI content-block shape).\n\n`image_url.url` must reference an upload of the target agent\n(`/api/agents/{id}/uploads/<name>`); the message endpoint validates the\nownership + that the file still exists on disk and 422s otherwise. Only a\nreference is carried on the wire, never base64 — the claim node inlines the\nbytes as native model content at delivery time."
      },
      "ImageUrlRef": {
        "properties": {
          "url": {
            "type": "string",
            "maxLength": 2048,
            "minLength": 1,
            "title": "Url"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "url"
        ],
        "title": "ImageUrlRef",
        "description": "The `image_url` object of an image content block."
      },
      "InventoryAggregate": {
        "properties": {
          "machines": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Machines"
          },
          "unreachable": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Unreachable"
          },
          "plugins": {
            "items": {
              "$ref": "#/components/schemas/InventoryItemAggregate"
            },
            "type": "array",
            "title": "Plugins"
          },
          "mcp_servers": {
            "items": {
              "$ref": "#/components/schemas/InventoryItemAggregate"
            },
            "type": "array",
            "title": "Mcp Servers"
          }
        },
        "type": "object",
        "required": [
          "machines",
          "unreachable",
          "plugins",
          "mcp_servers"
        ],
        "title": "InventoryAggregate",
        "description": "GET /api/inventory response — the cross-machine plugin + MCP matrix.\n\n`machines` is every machine name considered (the column set). `unreachable`\nis the subset whose inventory read timed out / failed (their cells are\nunknown). `plugins` / `mcp_servers` are the collapsed rows."
      },
      "InventoryItem": {
        "properties": {
          "name": {
            "type": "string",
            "title": "Name"
          },
          "kind": {
            "type": "string",
            "title": "Kind"
          },
          "enabled": {
            "type": "boolean",
            "title": "Enabled"
          },
          "can_enable": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Can Enable"
          },
          "reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Reason"
          },
          "description": {
            "type": "string",
            "title": "Description"
          }
        },
        "type": "object",
        "required": [
          "name",
          "kind",
          "enabled",
          "description"
        ],
        "title": "InventoryItem",
        "description": "One plugin/MCP server on a SINGLE host (the ?machine= view)."
      },
      "InventoryItemAggregate": {
        "properties": {
          "name": {
            "type": "string",
            "title": "Name"
          },
          "kind": {
            "type": "string",
            "title": "Kind"
          },
          "description": {
            "type": "string",
            "title": "Description"
          },
          "hosts": {
            "additionalProperties": {
              "$ref": "#/components/schemas/InventoryItemHostState"
            },
            "type": "object",
            "title": "Hosts"
          }
        },
        "type": "object",
        "required": [
          "name",
          "kind",
          "description",
          "hosts"
        ],
        "title": "InventoryItemAggregate",
        "description": "One plugin or MCP server, collapsed across every reachable host.\n\n`kind` is \"plugin\" | \"mcp\". `hosts` maps each reachable machine name to its\nper-host state (a machine lacking this item -> present=False)."
      },
      "InventoryItemHostState": {
        "properties": {
          "present": {
            "type": "boolean",
            "title": "Present"
          },
          "enabled": {
            "type": "boolean",
            "title": "Enabled"
          },
          "can_enable": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Can Enable"
          },
          "reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Reason"
          }
        },
        "type": "object",
        "required": [
          "present",
          "enabled"
        ],
        "title": "InventoryItemHostState",
        "description": "One plugin/MCP server's state on ONE host, in the cross-machine aggregate.\n\n`present` is False when the host doesn't have this plugin/server at all\n(installed elsewhere but not here) — the cell renders \"not installed\" and\noffers no toggle. When present, `enabled` is the effective state and\ncan_enable/reason are the host's read-time capability verdict (None = no gate)."
      },
      "InventoryItemWriteResult": {
        "properties": {
          "ok": {
            "type": "boolean",
            "title": "Ok"
          },
          "reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Reason"
          }
        },
        "type": "object",
        "required": [
          "ok"
        ],
        "title": "InventoryItemWriteResult",
        "description": "Per-item verdict of a PUT /api/inventory write (`ok` + reason when rejected)."
      },
      "InventoryMachineView": {
        "properties": {
          "machine": {
            "type": "string",
            "title": "Machine"
          },
          "plugins": {
            "items": {
              "$ref": "#/components/schemas/InventoryItem"
            },
            "type": "array",
            "title": "Plugins"
          },
          "mcp_servers": {
            "items": {
              "$ref": "#/components/schemas/InventoryItem"
            },
            "type": "array",
            "title": "Mcp Servers"
          }
        },
        "type": "object",
        "required": [
          "machine",
          "plugins",
          "mcp_servers"
        ],
        "title": "InventoryMachineView",
        "description": "GET /api/inventory?machine= response — one host's plugins + MCP servers."
      },
      "InventoryWriteRequest": {
        "properties": {
          "plugins": {
            "additionalProperties": {
              "type": "boolean"
            },
            "type": "object",
            "title": "Plugins",
            "default": {}
          },
          "mcp_servers": {
            "additionalProperties": {
              "type": "boolean"
            },
            "type": "object",
            "title": "Mcp Servers",
            "default": {}
          }
        },
        "type": "object",
        "title": "InventoryWriteRequest",
        "description": "PUT /api/inventory body. Each half is genuinely optional (the frontend may\ntoggle only a plugin or only an MCP server), so a missing half defaults to\nempty — not the contract-required-field fallback CLAUDE.md forbids. FastAPI\n422s a present-but-non-object half before the handler runs."
      },
      "InventoryWriteResult": {
        "properties": {
          "applied": {
            "type": "boolean",
            "title": "Applied"
          },
          "plugin_results": {
            "additionalProperties": {
              "$ref": "#/components/schemas/InventoryItemWriteResult"
            },
            "type": "object",
            "title": "Plugin Results"
          },
          "mcp_results": {
            "additionalProperties": {
              "$ref": "#/components/schemas/InventoryItemWriteResult"
            },
            "type": "object",
            "title": "Mcp Results"
          }
        },
        "type": "object",
        "required": [
          "applied",
          "plugin_results",
          "mcp_results"
        ],
        "title": "InventoryWriteResult",
        "description": "PUT /api/inventory response — per-item results + atomic `applied` flag."
      },
      "LabelPatchRequest": {
        "properties": {
          "label": {
            "type": "string",
            "maxLength": 64,
            "title": "Label"
          }
        },
        "type": "object",
        "required": [
          "label"
        ],
        "title": "LabelPatchRequest",
        "description": "PATCH /api/agents/{id} body — manually set / reset agent label.\n\n`label=\"\"` resets back to NULL; the frontend re-displays fallback\n`#N`. Non-empty strings are stripped; length 1-64 inclusive."
      },
      "LastMessageResponse": {
        "properties": {
          "text": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Text"
          }
        },
        "type": "object",
        "required": [
          "text"
        ],
        "title": "LastMessageResponse",
        "description": "GET /api/agents/{id}/last-message response — the text of the last\nAI message, or None when no AI message with text content exists yet."
      },
      "LastUpdate": {
        "properties": {
          "outcome": {
            "$ref": "#/components/schemas/UpdateOutcome"
          },
          "failed": {
            "type": "boolean",
            "title": "Failed"
          },
          "target_sha": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Target Sha"
          },
          "origin": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Origin"
          },
          "holder": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Holder"
          },
          "started_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Started At"
          },
          "ended_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Ended At"
          },
          "failing_step": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Failing Step"
          },
          "observed_by": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Observed By"
          },
          "pin_advanced": {
            "type": "boolean",
            "title": "Pin Advanced",
            "default": false
          },
          "log_path": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Log Path"
          }
        },
        "type": "object",
        "required": [
          "outcome",
          "failed"
        ],
        "title": "LastUpdate",
        "description": "The last cluster update, as served to `ava cluster status` and the frontend.\n\n`failed` is computed here rather than in each surface: two consumers deciding\nindependently what counts as a failure is how they end up disagreeing, and this\nis the value a banner is switched on."
      },
      "LlmBucket": {
        "properties": {
          "bucket": {
            "type": "integer",
            "title": "Bucket"
          },
          "calls": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Calls"
          },
          "latency_p50_ms": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Latency P50 Ms"
          },
          "latency_p95_ms": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Latency P95 Ms"
          },
          "latency_max_ms": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Latency Max Ms"
          },
          "tokens_in": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Tokens In"
          },
          "tokens_out": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Tokens Out"
          },
          "tps": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Tps"
          },
          "errors": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Errors"
          }
        },
        "type": "object",
        "required": [
          "bucket",
          "calls",
          "latency_p50_ms",
          "latency_p95_ms",
          "latency_max_ms",
          "tokens_in",
          "tokens_out",
          "tps",
          "errors"
        ],
        "title": "LlmBucket",
        "description": "One bucket's LLM call profile. Latency percentiles are over rows that\ncarry `latency_ms` (pre-instrumentation rows are NULL and ignored);\n`tps` = Σ(in+out+reasoning) tokens / Σ latency seconds (NULL when no\nlatency data). `errors` counts llm_provider_error / stream_stalled_retry /\nllm_turn_aborted events."
      },
      "LlmReport": {
        "properties": {
          "series": {
            "items": {
              "$ref": "#/components/schemas/LlmBucket"
            },
            "type": "array",
            "title": "Series"
          },
          "totals": {
            "$ref": "#/components/schemas/LlmTotals"
          }
        },
        "type": "object",
        "required": [
          "series",
          "totals"
        ],
        "title": "LlmReport"
      },
      "LlmTotals": {
        "properties": {
          "calls": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Calls"
          },
          "latency_p50_ms": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Latency P50 Ms"
          },
          "latency_p95_ms": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Latency P95 Ms"
          },
          "latency_max_ms": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Latency Max Ms"
          },
          "tokens_in": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Tokens In"
          },
          "tokens_out": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Tokens Out"
          },
          "tps": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Tps"
          },
          "errors": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Errors"
          }
        },
        "type": "object",
        "required": [
          "calls",
          "latency_p50_ms",
          "latency_p95_ms",
          "latency_max_ms",
          "tokens_in",
          "tokens_out",
          "tps",
          "errors"
        ],
        "title": "LlmTotals",
        "description": "Whole-window LLM profile — same fields as one LlmBucket, no bucket."
      },
      "LoginRequest": {
        "properties": {
          "password": {
            "type": "string",
            "title": "Password",
            "default": ""
          },
          "username": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Username"
          }
        },
        "type": "object",
        "title": "LoginRequest",
        "description": "POST /api/auth/login body. `password` is the cluster secret; a missing or\nempty one falls through to the 401 below rather than a 422. `username` is\naccepted for Chrome password-manager compatibility but never validated."
      },
      "MachineDeleteResponse": {
        "properties": {
          "deleted": {
            "type": "boolean",
            "title": "Deleted"
          }
        },
        "type": "object",
        "required": [
          "deleted"
        ],
        "title": "MachineDeleteResponse",
        "description": "DELETE /api/cluster/machines/{name} response."
      },
      "MachinePauseRequest": {
        "properties": {
          "reason": {
            "type": "string",
            "title": "Reason",
            "default": ""
          }
        },
        "type": "object",
        "title": "MachinePauseRequest",
        "description": "POST /api/cluster/machines/{name}/pause body.\n\n`reason` is free-text why the machine is being pulled out (e.g. \"a week off\")\n— recorded on the machines row as `pause_reason` for the resume checklist."
      },
      "MachinePauseResponse": {
        "properties": {
          "name": {
            "type": "string",
            "title": "Name"
          },
          "paused": {
            "type": "boolean",
            "title": "Paused"
          },
          "terminated_agents": {
            "type": "integer",
            "title": "Terminated Agents"
          },
          "force_marked_agents": {
            "type": "integer",
            "title": "Force Marked Agents"
          },
          "reassigned_tasks": {
            "type": "integer",
            "title": "Reassigned Tasks"
          },
          "paused_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Paused At"
          },
          "pause_reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Pause Reason"
          }
        },
        "type": "object",
        "required": [
          "name",
          "paused",
          "terminated_agents",
          "force_marked_agents",
          "reassigned_tasks"
        ],
        "title": "MachinePauseResponse",
        "description": "POST /api/cluster/machines/{name}/pause response — what the pause did.\n\nThe pause is the three-step operator act: drain (reassign in_progress\ntasks owned by the machine's agents to the drain owner), terminate every\nlive agent on the machine (graceful via its ops server; agents whose\ngraceful terminate could not be enqueued — machine already unreachable —\nare force-marked terminated in the shared DB), then set the pause latch.\n`paused_at`/`pause_reason` are the row values after the latch write.\nIdempotent: pausing an already-paused machine terminates nothing (its\nagents are already terminated) and returns the existing latch values."
      },
      "MachineResumeResponse": {
        "properties": {
          "name": {
            "type": "string",
            "title": "Name"
          },
          "resumed": {
            "type": "boolean",
            "title": "Resumed"
          }
        },
        "type": "object",
        "required": [
          "name",
          "resumed"
        ],
        "title": "MachineResumeResponse",
        "description": "POST /api/cluster/machines/{name}/resume response.\n\n`resumed` True = the pause latch was cleared (the machine is a normal\ncluster member again); False = it was not paused (idempotent no-op)."
      },
      "MachineStagingRequest": {
        "properties": {
          "is_staging": {
            "type": "boolean",
            "title": "Is Staging"
          }
        },
        "type": "object",
        "required": [
          "is_staging"
        ],
        "title": "MachineStagingRequest",
        "description": "Body for POST /api/cluster/machines/{name}/staging."
      },
      "MachineStatus": {
        "properties": {
          "name": {
            "type": "string",
            "title": "Name"
          },
          "serve_gateway": {
            "type": "boolean",
            "title": "Serve Gateway"
          },
          "serve_agent_runner": {
            "type": "boolean",
            "title": "Serve Agent Runner"
          },
          "serve_observability_station": {
            "type": "boolean",
            "title": "Serve Observability Station",
            "default": false
          },
          "gateway_url": {
            "type": "string",
            "title": "Gateway Url"
          },
          "up_since_at": {
            "type": "string",
            "format": "date-time",
            "title": "Up Since At"
          },
          "online": {
            "type": "boolean",
            "title": "Online"
          },
          "paused": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Paused"
          },
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Description"
          },
          "stopped_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Stopped At"
          },
          "is_staging": {
            "type": "boolean",
            "title": "Is Staging",
            "default": false
          },
          "head_sha": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Head Sha"
          },
          "on_pin": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "On Pin"
          },
          "running_sha": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Running Sha"
          },
          "deploy_hold": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Deploy Hold"
          },
          "last_update": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/LastUpdate"
              },
              {
                "type": "null"
              }
            ]
          },
          "cluster_last_known_good_sha": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Cluster Last Known Good Sha"
          },
          "settle_waited_on": {
            "type": "boolean",
            "title": "Settle Waited On",
            "default": false
          },
          "identity_mismatch": {
            "type": "boolean",
            "title": "Identity Mismatch",
            "default": false
          },
          "shell_count": {
            "type": "integer",
            "title": "Shell Count",
            "default": 0
          },
          "agent_host_online": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Agent Host Online"
          },
          "watchdog_online": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Watchdog Online"
          },
          "agent_count": {
            "type": "integer",
            "title": "Agent Count",
            "default": 0
          },
          "session_count": {
            "type": "integer",
            "title": "Session Count",
            "default": 0
          },
          "agent_groups": {
            "items": {
              "additionalProperties": true,
              "type": "object"
            },
            "type": "array",
            "title": "Agent Groups",
            "default": []
          },
          "resource": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/ResourceSample"
              },
              {
                "type": "null"
              }
            ]
          }
        },
        "type": "object",
        "required": [
          "name",
          "serve_gateway",
          "serve_agent_runner",
          "gateway_url",
          "up_since_at",
          "online",
          "paused"
        ],
        "title": "MachineStatus",
        "description": "A machines-table row state — augmented with live probe results.\n\n`online` / `paused` come from each machine's ops `status_probe` within\nthe configured roster deadline:\n- online=True + paused has a value: probe succeeded\n- online=False + paused=None: probe failed (network unreachable /\n  gateway down)\n- online=True + paused=None: ops server responded, but the operation failed\n  or its response did not match ClusterStatus (abnormal)"
      },
      "McpClientCreate": {
        "properties": {
          "name": {
            "type": "string",
            "title": "Name"
          },
          "scope": {
            "type": "string",
            "enum": [
              "read",
              "write"
            ],
            "title": "Scope",
            "default": "read"
          }
        },
        "type": "object",
        "required": [
          "name"
        ],
        "title": "McpClientCreate"
      },
      "McpClientCreated": {
        "properties": {
          "id": {
            "type": "integer",
            "title": "Id"
          },
          "name": {
            "type": "string",
            "title": "Name"
          },
          "scope": {
            "type": "string",
            "enum": [
              "read",
              "write"
            ],
            "title": "Scope"
          },
          "token": {
            "type": "string",
            "title": "Token"
          }
        },
        "type": "object",
        "required": [
          "id",
          "name",
          "scope",
          "token"
        ],
        "title": "McpClientCreated"
      },
      "McpClientRevoked": {
        "properties": {
          "ok": {
            "type": "boolean",
            "title": "Ok"
          }
        },
        "type": "object",
        "required": [
          "ok"
        ],
        "title": "McpClientRevoked"
      },
      "McpClientView": {
        "properties": {
          "id": {
            "type": "integer",
            "title": "Id"
          },
          "name": {
            "type": "string",
            "title": "Name"
          },
          "scope": {
            "type": "string",
            "enum": [
              "read",
              "write"
            ],
            "title": "Scope"
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          },
          "revoked_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Revoked At"
          },
          "last_used_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Used At"
          }
        },
        "type": "object",
        "required": [
          "id",
          "name",
          "scope",
          "created_at",
          "revoked_at",
          "last_used_at"
        ],
        "title": "McpClientView"
      },
      "MemoryGraphEdge": {
        "properties": {
          "source": {
            "type": "string",
            "title": "Source"
          },
          "target": {
            "type": "string",
            "title": "Target"
          },
          "kind": {
            "type": "string",
            "enum": [
              "containment",
              "reference"
            ],
            "title": "Kind"
          }
        },
        "type": "object",
        "required": [
          "source",
          "target",
          "kind"
        ],
        "title": "MemoryGraphEdge",
        "description": "One directed edge in the OKF memory graph.\n\n`kind` separates the two edge families the frontend renders differently:\n`containment` (note → folder, folder → parent folder — the main\nstructure) and `reference` (a markdown cross-link between two notes)."
      },
      "MemoryGraphNode": {
        "properties": {
          "id": {
            "type": "string",
            "title": "Id"
          },
          "path": {
            "type": "string",
            "title": "Path"
          },
          "title": {
            "type": "string",
            "title": "Title"
          },
          "kind": {
            "type": "string",
            "enum": [
              "note",
              "folder"
            ],
            "title": "Kind"
          },
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Description"
          },
          "tags": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Tags"
          },
          "primary_tag": {
            "type": "string",
            "title": "Primary Tag"
          },
          "timestamp": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Timestamp"
          },
          "ava_agent": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Ava Agent"
          },
          "ava_machine": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Ava Machine"
          }
        },
        "type": "object",
        "required": [
          "id",
          "path",
          "title",
          "kind",
          "description",
          "tags",
          "primary_tag",
          "timestamp",
          "ava_agent",
          "ava_machine"
        ],
        "title": "MemoryGraphNode",
        "description": "One node in the OKF memory graph — a concept note, or a folder pseudo\nnode (the graph's structural skeleton)."
      },
      "MemoryGraphResponse": {
        "properties": {
          "nodes": {
            "items": {
              "$ref": "#/components/schemas/MemoryGraphNode"
            },
            "type": "array",
            "title": "Nodes"
          },
          "edges": {
            "items": {
              "$ref": "#/components/schemas/MemoryGraphEdge"
            },
            "type": "array",
            "title": "Edges"
          },
          "warnings": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Warnings"
          }
        },
        "type": "object",
        "required": [
          "nodes",
          "edges"
        ],
        "title": "MemoryGraphResponse",
        "description": "GET /api/memory/graph response."
      },
      "MemoryNoteResponse": {
        "properties": {
          "path": {
            "type": "string",
            "title": "Path"
          },
          "title": {
            "type": "string",
            "title": "Title"
          },
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Description"
          },
          "tags": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Tags"
          },
          "timestamp": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Timestamp"
          },
          "ava_agent": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Ava Agent"
          },
          "ava_machine": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Ava Machine"
          },
          "body": {
            "type": "string",
            "title": "Body"
          }
        },
        "type": "object",
        "required": [
          "path",
          "title",
          "description",
          "tags",
          "timestamp",
          "ava_agent",
          "ava_machine",
          "body"
        ],
        "title": "MemoryNoteResponse",
        "description": "GET /api/memory/note response — one parsed memory note.\n\nMirrors MemoryGraphNode plus the parsed markdown body. The body is the\nmarkdown with the YAML frontmatter removed (shared.parse_note's body), so\nthe frontend renders the note itself rather than re-parsing frontmatter\n(frontmatter values arrive as structured fields: title / description /\ntags / timestamp / ava_agent / ava_machine)."
      },
      "MemoryRefreshResponse": {
        "properties": {
          "head": {
            "type": "string",
            "title": "Head"
          }
        },
        "type": "object",
        "required": [
          "head"
        ],
        "title": "MemoryRefreshResponse",
        "description": "POST /api/memory/refresh response."
      },
      "MemorySearchRequest": {
        "properties": {
          "query": {
            "type": "string",
            "minLength": 1,
            "title": "Query",
            "description": "natural-language query"
          },
          "k": {
            "type": "integer",
            "maximum": 100.0,
            "minimum": 1.0,
            "title": "K",
            "description": "top-k result count",
            "default": 5
          }
        },
        "type": "object",
        "required": [
          "query"
        ],
        "title": "MemorySearchRequest",
        "description": "POST /api/memory/search request — semantic search query."
      },
      "MemorySearchResponse": {
        "properties": {
          "paths": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Paths"
          },
          "results": {
            "items": {
              "$ref": "#/components/schemas/MemorySearchResultItem"
            },
            "type": "array",
            "title": "Results"
          }
        },
        "type": "object",
        "title": "MemorySearchResponse",
        "description": "POST /api/memory/search response — RELATIVE paths from memory pool root.\n\nCaller (SDK `ava.memory.search`) uses `ava.memory.PATH / p` to\nreconstruct absolute paths. fs-neutral makes mismatched gateway\n(e.g. /Users/x) and agent-runner (/home/y) filesystems work.\n\n`results` carries path + description for each match; `paths` is the\nbare list of relative paths (backward-compat for existing consumers)."
      },
      "MemorySearchResultItem": {
        "properties": {
          "path": {
            "type": "string",
            "title": "Path",
            "description": "relative path from memory pool root"
          },
          "description": {
            "type": "string",
            "title": "Description",
            "description": "frontmatter description field, or empty",
            "default": ""
          },
          "tags": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Tags",
            "description": "frontmatter tags, including the note's `type/<x>`, or empty"
          }
        },
        "type": "object",
        "required": [
          "path"
        ],
        "title": "MemorySearchResultItem",
        "description": "A single search result: file path + the frontmatter a caller can judge the\nhit by without opening it."
      },
      "MetricPoint": {
        "properties": {
          "ts": {
            "type": "string",
            "format": "date-time",
            "title": "Ts"
          },
          "value": {
            "type": "number",
            "title": "Value"
          }
        },
        "type": "object",
        "required": [
          "ts",
          "value"
        ],
        "title": "MetricPoint",
        "description": "One sample of a plugin metric's inspector series — the bucket start\n(`ts`) and the aggregated `value` for that bucket. Series come back\nchronologically (the metric templates GROUP BY 1 ORDER BY 1); the frontend\ntakes the last point as the \"current\" value."
      },
      "MetricsMeta": {
        "properties": {
          "window_days": {
            "type": "integer",
            "title": "Window Days"
          },
          "agent_filter": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Agent Filter"
          },
          "generated_at": {
            "type": "string",
            "title": "Generated At"
          },
          "total_events": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Total Events"
          },
          "distinct_agents": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Distinct Agents"
          },
          "since_compact": {
            "type": "boolean",
            "title": "Since Compact",
            "default": false
          }
        },
        "type": "object",
        "required": [
          "window_days",
          "agent_filter",
          "generated_at",
          "total_events",
          "distinct_agents"
        ],
        "title": "MetricsMeta",
        "description": "`/api/metrics` / `/api/metrics/agents` report header — window +\nprovenance. `since_compact` echoes the request's filter: True = each\nagent's events were narrowed to those at or after its latest compact\nhalt before aggregating."
      },
      "MetricsReport": {
        "properties": {
          "meta": {
            "$ref": "#/components/schemas/MetricsMeta"
          },
          "metrics": {
            "additionalProperties": true,
            "type": "object",
            "title": "Metrics"
          }
        },
        "type": "object",
        "required": [
          "meta",
          "metrics"
        ],
        "title": "MetricsReport",
        "description": "GET /api/metrics response — aggregate report over events for the\nsettings Metrics tab.\n\n`metrics` is intentionally a free-form map (one key per registered metric\nunit, e.g. \"syntax_fix\" / \"exec\" / \"llm_turns\" / \"agent_activity\"). Typing\neach unit's shape here would couple the schema to the metric registry and\ndefeat the \"add a metric = one function\" extensibility, so the frontend\ncarries the per-unit data shapes in hand-written TypeScript instead of\ngenerated types."
      },
      "ModelInfo": {
        "properties": {
          "provider": {
            "type": "string",
            "title": "Provider"
          },
          "context_window": {
            "type": "integer",
            "title": "Context Window"
          },
          "pricing": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/ModelPricing"
              },
              {
                "type": "null"
              }
            ]
          },
          "reasoning_effort_options": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Reasoning Effort Options"
          },
          "reasoning_effort_default": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Reasoning Effort Default"
          },
          "superseded_by": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Superseded By"
          }
        },
        "type": "object",
        "required": [
          "provider",
          "context_window"
        ],
        "title": "ModelInfo",
        "description": "Per-model metadata surfaced in the spawn picker."
      },
      "ModelPricing": {
        "properties": {
          "input": {
            "type": "number",
            "title": "Input"
          },
          "cache_read": {
            "type": "number",
            "title": "Cache Read"
          },
          "output": {
            "type": "number",
            "title": "Output"
          }
        },
        "type": "object",
        "required": [
          "input",
          "cache_read",
          "output"
        ],
        "title": "ModelPricing",
        "description": "Per-million-token pricing for one model (USD)."
      },
      "ModelsResponse": {
        "properties": {
          "providers": {
            "additionalProperties": {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "type": "object",
            "title": "Providers"
          },
          "models": {
            "additionalProperties": {
              "$ref": "#/components/schemas/ModelInfo"
            },
            "type": "object",
            "title": "Models"
          },
          "default": {
            "type": "string",
            "title": "Default"
          }
        },
        "type": "object",
        "required": [
          "providers",
          "models",
          "default"
        ],
        "title": "ModelsResponse",
        "description": "GET /api/models — model picker source for the spawn UI."
      },
      "NeighborRow": {
        "properties": {
          "agent_id": {
            "type": "integer",
            "title": "Agent Id"
          },
          "label": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Label"
          },
          "status": {
            "type": "string",
            "title": "Status"
          },
          "depth": {
            "type": "integer",
            "title": "Depth"
          },
          "score": {
            "type": "number",
            "title": "Score"
          }
        },
        "type": "object",
        "required": [
          "agent_id",
          "label",
          "status",
          "depth",
          "score"
        ],
        "title": "NeighborRow",
        "description": "One row in the GET /api/agents/{id}/neighbors result — a neighbor in\n`neighbors` or an ancestor in `ancestors`."
      },
      "NeighborsResponse": {
        "properties": {
          "neighbors": {
            "items": {
              "$ref": "#/components/schemas/NeighborRow"
            },
            "type": "array",
            "title": "Neighbors"
          },
          "ancestors": {
            "items": {
              "$ref": "#/components/schemas/NeighborRow"
            },
            "type": "array",
            "title": "Ancestors"
          },
          "degraded": {
            "type": "boolean",
            "title": "Degraded",
            "default": false
          }
        },
        "type": "object",
        "required": [
          "neighbors",
          "ancestors"
        ],
        "title": "NeighborsResponse",
        "description": "GET /api/agents/{id}/neighbors response."
      },
      "NoticeCreateIn": {
        "properties": {
          "title": {
            "type": "string",
            "title": "Title"
          },
          "content": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Content"
          },
          "priority": {
            "$ref": "#/components/schemas/Priority",
            "default": "P2"
          },
          "require_response": {
            "type": "boolean",
            "title": "Require Response",
            "default": false
          },
          "blocking": {
            "type": "boolean",
            "title": "Blocking",
            "default": false
          },
          "task_id": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Task Id"
          }
        },
        "type": "object",
        "required": [
          "title"
        ],
        "title": "NoticeCreateIn",
        "description": "POST /api/agents/{id}/notices request body — the unified notice write API\n(R3 door ④).\n\nMirrors the SDK ava.ui.notify() contract: `priority` P0-P3, `blocking`\nmeaningful only when `require_response` is true (an FYI never stalls).\n`task_id` optionally groups the notice under a task in the user's queue."
      },
      "NoticeEditIn": {
        "properties": {
          "title": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Title"
          },
          "content": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Content"
          },
          "priority": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Priority"
              },
              {
                "type": "null"
              }
            ]
          },
          "blocking": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Blocking"
          }
        },
        "type": "object",
        "title": "NoticeEditIn",
        "description": "PATCH /api/agents/{id}/notices/current request body — revise the agent's\nopen notice (the SDK ava.ui.edit_notice contract). All fields optional;\npass only what changes. `require_response` cannot be changed (turn an FYI\ninto a question by dismissing and posting fresh)."
      },
      "NoticeItem": {
        "properties": {
          "id": {
            "type": "integer",
            "title": "Id"
          },
          "agent_id": {
            "type": "integer",
            "title": "Agent Id"
          },
          "agent_label": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Agent Label"
          },
          "title": {
            "type": "string",
            "title": "Title"
          },
          "content": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Content"
          },
          "priority": {
            "$ref": "#/components/schemas/Priority"
          },
          "require_response": {
            "type": "boolean",
            "title": "Require Response"
          },
          "blocking": {
            "type": "boolean",
            "title": "Blocking"
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          },
          "updated_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Updated At"
          },
          "resolved_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Resolved At"
          },
          "resolution": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Resolution"
          },
          "reply": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Reply"
          },
          "task_id": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Task Id"
          }
        },
        "type": "object",
        "required": [
          "id",
          "agent_id",
          "agent_label",
          "title",
          "content",
          "priority",
          "require_response",
          "blocking",
          "created_at"
        ],
        "title": "NoticeItem",
        "description": "One agent_notices row — element of GET /api/notices/open (the FYI feed:\nrequire_response false, resolved_at None) and GET /api/notices/resolved (the\ncross-fleet resolution history). Joined to the agent label.\n\nServed as an independent feed kept off the agent snapshot — the snapshot\ncarries the open require_response notices inline + an unread FYI count, so a\nlarge FYI backlog never bloats the fleet-wide broadcast.\n\n`resolution` is NULL while open, else one of answered / dismissed / read /\nwithdrawn / superseded; `reply` is the cached user reply text (NULL when none)."
      },
      "NoticesCursor": {
        "properties": {
          "before_at": {
            "type": "string",
            "format": "date-time",
            "title": "Before At"
          },
          "before_id": {
            "type": "integer",
            "title": "Before Id"
          }
        },
        "type": "object",
        "required": [
          "before_at",
          "before_id"
        ],
        "title": "NoticesCursor",
        "description": "Keyset cursor for the resolved-history page of GET /api/notices —\nmirror of the (resolved_at, id) cursor the standalone\n/api/notices/resolved endpoint accepts. Supplied together or not at all;\n`before_at` is the last row's resolution time, `before_id` its id."
      },
      "NoticesFeed": {
        "properties": {
          "open": {
            "items": {
              "$ref": "#/components/schemas/NoticeItem"
            },
            "type": "array",
            "title": "Open"
          },
          "awaiting": {
            "items": {
              "$ref": "#/components/schemas/NoticeItem"
            },
            "type": "array",
            "title": "Awaiting"
          },
          "resolved_page": {
            "items": {
              "$ref": "#/components/schemas/NoticeItem"
            },
            "type": "array",
            "title": "Resolved Page"
          },
          "next_cursor": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/NoticesCursor"
              },
              {
                "type": "null"
              }
            ]
          }
        },
        "type": "object",
        "required": [
          "open",
          "awaiting",
          "resolved_page",
          "next_cursor"
        ],
        "title": "NoticesFeed",
        "description": "GET /api/notices — the unified inbox feed (Task #1024, R4 layer 2,\ndecision Q1=A). One request carries the whole Inbox panel: the OPEN\nqueue split by kind (open = FYI notices that need no response, awaiting\n= require_response notices that ride the agent snapshot today) plus one\nkeyset page of the RESOLVED history.\n\nThe contract shape was chosen so a panel = one request = one hook:\nthe client no longer merges three independent pipes (agent snapshot +\n/api/notices/open + /api/notices/resolved). `next_cursor` is None when\n`resolved_page` is the last page (short page or exhausted); pass it back\nas before_at/before_id for the next strictly-older page."
      },
      "OpenNotice": {
        "properties": {
          "id": {
            "type": "integer",
            "title": "Id"
          },
          "title": {
            "type": "string",
            "title": "Title"
          },
          "content": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Content"
          },
          "priority": {
            "$ref": "#/components/schemas/Priority"
          },
          "require_response": {
            "type": "boolean",
            "title": "Require Response",
            "default": true
          },
          "blocking": {
            "type": "boolean",
            "title": "Blocking"
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          },
          "task_id": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Task Id"
          }
        },
        "type": "object",
        "required": [
          "id",
          "title",
          "content",
          "priority",
          "blocking",
          "created_at"
        ],
        "title": "OpenNotice",
        "description": "One open notice from ava.ui.notify() (migration 0053).\n\nThe agent snapshot's `notices_awaiting_response` carries only the require_response\nsubset (the \"needs response\" worklist); this model also serves the per-agent\ninspector panel where the single open notice (since #152) may be either kind.\n\n`title` is the one-line headline; `content` is the optional detail\nbody (possibly long). `priority` is P0..P3 (the stakes axis) and `blocking`\nsays whether the agent is stalled waiting on the answer (the urgency axis) —\nthe two triage axes (color + sort). `require_response` is True when the agent\nneeds a user decision, False for a one-way FYI; defaults to True for backward\ncompat with the snapshot's notices_awaiting_response subquery (which only\nselects require_response rows)."
      },
      "OpsMonitorMeta": {
        "properties": {
          "window": {
            "type": "string",
            "title": "Window"
          },
          "bucket_seconds": {
            "type": "integer",
            "minimum": 1.0,
            "title": "Bucket Seconds"
          },
          "generated_at": {
            "type": "string",
            "title": "Generated At"
          },
          "bucket_starts": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Bucket Starts"
          }
        },
        "type": "object",
        "required": [
          "window",
          "bucket_seconds",
          "generated_at",
          "bucket_starts"
        ],
        "title": "OpsMonitorMeta",
        "description": "Window + provenance of one `/api/ops/monitor` response."
      },
      "OpsMonitorReport": {
        "properties": {
          "meta": {
            "$ref": "#/components/schemas/OpsMonitorMeta"
          },
          "sse": {
            "$ref": "#/components/schemas/SseReport"
          },
          "llm": {
            "$ref": "#/components/schemas/LlmReport"
          },
          "restarts": {
            "$ref": "#/components/schemas/RestartReport"
          }
        },
        "type": "object",
        "required": [
          "meta",
          "sse",
          "llm",
          "restarts"
        ],
        "title": "OpsMonitorReport",
        "description": "GET /api/ops/monitor response — the whole panel in one round trip."
      },
      "PackageDraftRequest": {
        "properties": {
          "kind": {
            "type": "string",
            "enum": [
              "skill",
              "plugin",
              "mcp"
            ],
            "title": "Kind",
            "description": "Which package surface the request came from."
          },
          "nl": {
            "type": "string",
            "title": "Nl",
            "description": "Natural-language description of the capability wanted."
          }
        },
        "type": "object",
        "required": [
          "kind",
          "nl"
        ],
        "title": "PackageDraftRequest"
      },
      "PackageDraftResponse": {
        "properties": {
          "agent_id": {
            "type": "integer",
            "title": "Agent Id"
          }
        },
        "type": "object",
        "required": [
          "agent_id"
        ],
        "title": "PackageDraftResponse"
      },
      "PageRegisterRequest": {
        "properties": {
          "name": {
            "type": "string",
            "maxLength": 64,
            "minLength": 1,
            "pattern": "^[a-zA-Z0-9_-]+$",
            "title": "Name"
          },
          "host": {
            "type": "string",
            "maxLength": 255,
            "minLength": 1,
            "title": "Host"
          },
          "port": {
            "type": "integer",
            "exclusiveMaximum": 65536.0,
            "exclusiveMinimum": 0.0,
            "title": "Port"
          },
          "title": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 200
              },
              {
                "type": "null"
              }
            ],
            "title": "Title"
          },
          "serve_dir": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 4096
              },
              {
                "type": "null"
              }
            ],
            "title": "Serve Dir"
          },
          "ttl_seconds": {
            "anyOf": [
              {
                "type": "integer",
                "exclusiveMinimum": 0.0
              },
              {
                "type": "null"
              }
            ],
            "title": "Ttl Seconds"
          }
        },
        "type": "object",
        "required": [
          "name",
          "host",
          "port"
        ],
        "title": "PageRegisterRequest",
        "description": "POST /api/agents/{aid}/pages request body — SDK ava.ui.show / .serve call.\n\n`host`: the agent-runner's reachable address (the agent self-reports\n    it) — where the gateway's reverse proxy dials the page server\n    (loopback on a single box). Never appears in any URL.\n`port`: port that the agent process's HTTP server is listening on\n    (bound on the runner's reachable host, never 0.0.0.0). 1-65535.\n`title`: optional human-readable title shown in the frontend\n    tab/header; falls back to name when not given.\n`serve_dir`: directory the page server serves, recorded by\n    ava.ui.serve() so agent boot can re-serve a dead\n    page server after resurrect/restart; absent for ava.ui.show()\n    pages (the agent manages those servers itself)."
      },
      "PageRow": {
        "properties": {
          "id": {
            "type": "integer",
            "title": "Id"
          },
          "agent_id": {
            "type": "integer",
            "title": "Agent Id"
          },
          "name": {
            "type": "string",
            "title": "Name"
          },
          "port": {
            "type": "integer",
            "title": "Port"
          },
          "title": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Title"
          },
          "serve_dir": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Serve Dir"
          },
          "url": {
            "type": "string",
            "title": "Url"
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          },
          "closed_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Closed At"
          }
        },
        "type": "object",
        "required": [
          "id",
          "agent_id",
          "name",
          "port",
          "title",
          "serve_dir",
          "url",
          "created_at",
          "closed_at"
        ],
        "title": "PageRow",
        "description": "Single agent_pages row view — element of GET /api/agents/{aid}/pages +\nreturn of register/close endpoints.\n\n`url`: absolute gateway reverse-proxy URL\n(`http://<gateway>/pages/<id>-<name>/`) — the gateway serves\nthe page content, so this is the only address the browser needs; the\npage server's host:port stays inside the gateway."
      },
      "PendingInbound": {
        "properties": {
          "id": {
            "type": "integer",
            "title": "Id"
          },
          "source": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Source"
          },
          "content": {
            "type": "string",
            "title": "Content"
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          }
        },
        "type": "object",
        "required": [
          "id",
          "source",
          "content",
          "created_at"
        ],
        "title": "PendingInbound",
        "description": "One queued inbound (status='pending', kind='chat') — element of\nGET /api/agents/{id}/pending.\n\nThese have not been claimed by the agent yet, so they are absent from\nthe timeline snapshot; the web UI shows them as a compact strip above\nthe composer. `source` disambiguates origin (user / agent:N /\nwatcher:N / ...) so the UI can label who queued it."
      },
      "PluginMetricResult": {
        "properties": {
          "name": {
            "type": "string",
            "title": "Name"
          },
          "title": {
            "type": "string",
            "title": "Title"
          },
          "description": {
            "type": "string",
            "title": "Description",
            "default": ""
          },
          "plugin": {
            "type": "string",
            "title": "Plugin",
            "default": ""
          },
          "unit": {
            "type": "string",
            "title": "Unit",
            "default": "short"
          },
          "panel": {
            "type": "string",
            "enum": [
              "timeseries",
              "stat",
              "barchart",
              "table"
            ],
            "title": "Panel"
          },
          "error": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Error"
          },
          "value": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Value"
          },
          "series": {
            "items": {
              "$ref": "#/components/schemas/MetricPoint"
            },
            "type": "array",
            "title": "Series"
          }
        },
        "type": "object",
        "required": [
          "name",
          "title",
          "panel"
        ],
        "title": "PluginMetricResult",
        "description": "One plugin metric rendered for the inspector surface — an element of\nGET /api/agents/{id}/inspect/metrics.\n\nMirrors the registered MetricSpec (see `shared/plugin_metrics.py`):\n`panel` selects the payload — `timeseries` / `barchart` metrics carry\n`series` (a bounded recent window, 24h in 1h buckets by default, so at\nmost a couple of dozen points), `stat` metrics carry `value` (the single\naggregate). `error` is set (with `series`/`value` empty) when the metric's\nquery failed at execution time — the panel shows the rest of the metrics\nand surfaces the failure inline instead of 500ing the whole request;\nregistry-level problems (missing/malformed/tampered file, a template that\nfails the safety re-validation) are HTTP errors instead."
      },
      "PresetCreate": {
        "properties": {
          "name": {
            "type": "string",
            "maxLength": 64,
            "minLength": 1,
            "title": "Name"
          },
          "label": {
            "type": "string",
            "maxLength": 128,
            "minLength": 1,
            "title": "Label"
          },
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Description"
          },
          "config": {
            "additionalProperties": true,
            "type": "object",
            "title": "Config"
          }
        },
        "type": "object",
        "required": [
          "name",
          "label"
        ],
        "title": "PresetCreate"
      },
      "PresetUpdate": {
        "properties": {
          "name": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 64,
                "minLength": 1
              },
              {
                "type": "null"
              }
            ],
            "title": "Name"
          },
          "label": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 128,
                "minLength": 1
              },
              {
                "type": "null"
              }
            ],
            "title": "Label"
          },
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Description"
          },
          "config": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "title": "Config"
          }
        },
        "type": "object",
        "title": "PresetUpdate"
      },
      "PresetView": {
        "properties": {
          "id": {
            "type": "integer",
            "title": "Id"
          },
          "name": {
            "type": "string",
            "title": "Name"
          },
          "label": {
            "type": "string",
            "title": "Label"
          },
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Description"
          },
          "config": {
            "additionalProperties": true,
            "type": "object",
            "title": "Config"
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          },
          "updated_at": {
            "type": "string",
            "format": "date-time",
            "title": "Updated At"
          }
        },
        "type": "object",
        "required": [
          "id",
          "name",
          "label",
          "description",
          "config",
          "created_at",
          "updated_at"
        ],
        "title": "PresetView"
      },
      "Priority": {
        "type": "string",
        "enum": [
          "P0",
          "P1",
          "P2",
          "P3"
        ],
        "title": "Priority",
        "description": "Stakes rung: P0 (highest) through P3 (lowest)."
      },
      "ResolveNoticeIn": {
        "properties": {
          "action": {
            "type": "string",
            "enum": [
              "answer",
              "dismiss",
              "read"
            ],
            "title": "Action"
          },
          "reply": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 1000000,
                "minLength": 1
              },
              {
                "type": "null"
              }
            ],
            "title": "Reply"
          }
        },
        "type": "object",
        "required": [
          "action"
        ],
        "title": "ResolveNoticeIn",
        "description": "POST /api/agents/{id}/notices/{notice_id}/resolve request body.\n\n`action` is the explicit close verb (never inferred from whether `reply` is\npresent): `answer` / `dismiss` apply to a require_response notice, `read`\napplies to an FYI notice. `reply` is the user's free-text reply — required\nfor `answer`, optional for `dismiss` and `read`. When present it is cached on\nthe notice row and delivered back to the agent as a normal chat inbound."
      },
      "ResolvedConfigView": {
        "properties": {
          "model": {
            "type": "string",
            "title": "Model"
          },
          "registered": {
            "type": "boolean",
            "title": "Registered"
          },
          "fields": {
            "items": {
              "$ref": "#/components/schemas/ResolvedFieldView"
            },
            "type": "array",
            "title": "Fields"
          }
        },
        "type": "object",
        "required": [
          "model",
          "registered",
          "fields"
        ],
        "title": "ResolvedConfigView",
        "description": "GET /api/config/resolved response — every tunable resolved for one model.\n\n`model` echoes the model actually resolved against (the cluster's own\n`llm_model` when the request omitted it). `registered` is False for a model\nid absent from the registry: resolution still works (it simply has no\nper-model layer, exactly as at runtime), and the flag lets the panel say so\ninstead of presenting shared defaults as if the model had chosen them."
      },
      "ResolvedFieldView": {
        "properties": {
          "name": {
            "type": "string",
            "title": "Name"
          },
          "env_var": {
            "type": "string",
            "title": "Env Var"
          },
          "description": {
            "type": "string",
            "title": "Description"
          },
          "field_type": {
            "type": "string",
            "title": "Field Type"
          },
          "choices": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Choices"
          },
          "group": {
            "type": "string",
            "title": "Group"
          },
          "effective_value": {
            "title": "Effective Value"
          },
          "source": {
            "type": "string",
            "enum": [
              "explicit",
              "model-default",
              "shared-default"
            ],
            "title": "Source"
          },
          "explicit_value": {
            "title": "Explicit Value"
          },
          "model_default": {
            "title": "Model Default"
          },
          "shared_default": {
            "title": "Shared Default"
          },
          "per_agent": {
            "type": "boolean",
            "title": "Per Agent"
          },
          "restart_required": {
            "type": "string",
            "title": "Restart Required"
          }
        },
        "type": "object",
        "required": [
          "name",
          "env_var",
          "description",
          "field_type",
          "group",
          "effective_value",
          "source",
          "explicit_value",
          "model_default",
          "shared_default",
          "per_agent",
          "restart_required"
        ],
        "title": "ResolvedFieldView",
        "description": "One per-model-defaultable setting resolved for a specific model.\n\nThe read-only mirror of `shared/lm/registry.py:explain_setting`: the value an\nagent on this model boots with, plus every candidate layer and the name of\nthe one that won. There is no write path here — an explicit value is edited\nas the normal config field of the same `name`, so the panel links back to\nthat row rather than growing a second store."
      },
      "ResourceSample": {
        "properties": {
          "ts": {
            "type": "number",
            "title": "Ts"
          },
          "cpu_pct": {
            "type": "number",
            "title": "Cpu Pct"
          },
          "mem_used_gb": {
            "type": "number",
            "title": "Mem Used Gb"
          },
          "mem_total_gb": {
            "type": "number",
            "title": "Mem Total Gb"
          },
          "mem_pct": {
            "type": "number",
            "title": "Mem Pct"
          },
          "disk_used_gb": {
            "type": "number",
            "title": "Disk Used Gb"
          },
          "disk_total_gb": {
            "type": "number",
            "title": "Disk Total Gb"
          },
          "disk_pct": {
            "type": "number",
            "title": "Disk Pct"
          }
        },
        "type": "object",
        "required": [
          "ts",
          "cpu_pct",
          "mem_used_gb",
          "mem_total_gb",
          "mem_pct",
          "disk_used_gb",
          "disk_total_gb",
          "disk_pct"
        ],
        "title": "ResourceSample",
        "description": "This machine's CPU / memory / disk at the moment it was read."
      },
      "RestartAgentRequest": {
        "properties": {
          "source": {
            "type": "string",
            "maxLength": 64,
            "minLength": 1,
            "title": "Source",
            "default": "user"
          },
          "config_overlay": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "title": "Config Overlay"
          }
        },
        "type": "object",
        "title": "RestartAgentRequest",
        "description": "POST /api/agents/{id}/restart request body — fully optional.\n\n`source` default \"user\" — frontend restart button does not need to\npass it; SDK paths pass f\"agent:{my_id}\". Written into the lifecycle\n'restart' inbound's source; the claim-side dispatch composes it into\nthe marker `[system ts] You have been restarted by {source}` so the\nagent knows who restarted it.\n\n`config_overlay`, when nonempty, is validated before it reaches either\ngateway or runner writes, merged into the persistent agent overlay, and\ncarried in the restart inbound payload for the completion marker."
      },
      "RestartAgentResponse": {
        "properties": {
          "status": {
            "type": "string",
            "enum": [
              "enqueued",
              "already_terminated"
            ],
            "title": "Status"
          }
        },
        "type": "object",
        "required": [
          "status"
        ],
        "title": "RestartAgentResponse",
        "description": "POST /api/agents/{id}/restart response.\n\n`enqueued`: native restart is durable; the host completes claim and\n    checkpoint settlement before admitting the next incarnation.\n`already_terminated`: agent is dead; restart does not apply — use\n    resurrect."
      },
      "RestartBucket": {
        "properties": {
          "bucket": {
            "type": "integer",
            "title": "Bucket"
          },
          "agent_restarts": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Agent Restarts"
          },
          "service_starts": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Service Starts"
          }
        },
        "type": "object",
        "required": [
          "bucket",
          "agent_restarts",
          "service_starts"
        ],
        "title": "RestartBucket",
        "description": "One bucket's process-restart counts: agent processes (agent_restarted)\nand gateway-side services (service_started)."
      },
      "RestartReport": {
        "properties": {
          "series": {
            "items": {
              "$ref": "#/components/schemas/RestartBucket"
            },
            "type": "array",
            "title": "Series"
          },
          "services": {
            "items": {
              "$ref": "#/components/schemas/ServiceRestartRow"
            },
            "type": "array",
            "title": "Services"
          },
          "agents": {
            "items": {
              "$ref": "#/components/schemas/AgentRestartRow"
            },
            "type": "array",
            "title": "Agents"
          },
          "totals": {
            "$ref": "#/components/schemas/RestartTotals"
          }
        },
        "type": "object",
        "required": [
          "series",
          "services",
          "agents",
          "totals"
        ],
        "title": "RestartReport"
      },
      "RestartTotals": {
        "properties": {
          "agent_restarts": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Agent Restarts"
          },
          "service_starts": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Service Starts"
          }
        },
        "type": "object",
        "required": [
          "agent_restarts",
          "service_starts"
        ],
        "title": "RestartTotals"
      },
      "ResurrectAgentRequest": {
        "properties": {
          "resurrected_by": {
            "type": "string",
            "maxLength": 64,
            "minLength": 1,
            "title": "Resurrected By",
            "default": "user"
          },
          "prompt": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 1000000,
                "minLength": 1
              },
              {
                "type": "null"
              }
            ],
            "title": "Prompt"
          }
        },
        "type": "object",
        "title": "ResurrectAgentRequest",
        "description": "Resurrect agent request body.\n\nThe public `POST /api/agents/{id}/resurrect` endpoint uses this for an\nexplicit lifecycle wake with no work guard. The internal versioned\npending-work path also carries it alongside the exact chat or compact\nrequest id and kind; controller recovery calls the lower lifecycle helper\nwith its own exact death claim.\n\n`resurrected_by` default \"user\" — frontend resurrect button does not\nneed to pass it; SDK paths pass f\"agent:{my_id}\"; pending-work and\ncontroller auto-resurrect pass \"system\". Written into the\nlifecycle 'resurrect' inbound's source; the claim-side dispatch\ncomposes it into the marker `[system ts] You have been resurrected\nby {resurrected_by}` so the agent knows who resurrected it.\n\nThe value must pass `shared.envelope.validate_source` (same check as\n`AgentMessageIn.source`): the same value becomes the prompt chat\ninbound's source, and the claim node's envelope wrap raises on\nanything outside the whitelist — killing the freshly resurrected\nprocess on its first claim. Validating here turns that process-fatal\nvalue into a 422 at the HTTP boundary (the agent-240 incident).\n\n`prompt` is **optional at this HTTP boundary**. The frontend resurrect\nbutton is a pure lifecycle event — there is no message to deliver, so it\nsends no prompt and the agent just gets the \"you have been resurrected\"\nmarker. Peer agents no longer have a dedicated resurrect API — they send\na chat message (`ava.agents.send_message`) and auto-resurrect handles the\nrest. When a prompt is given it is INSERTed as a chat inbound in **the same\ntransaction** as the lifecycle 'resurrect' inbound. The session may be\ncreated before commit, but its child blocks on the agent row and cannot\nclaim or process either inbound until both are committed."
      },
      "ResurrectAgentResponse": {
        "properties": {
          "status": {
            "type": "string",
            "enum": [
              "spawned",
              "already_alive"
            ],
            "title": "Status"
          }
        },
        "type": "object",
        "required": [
          "status"
        ],
        "title": "ResurrectAgentResponse",
        "description": "Resurrect agent response — returned by `POST /api/agents/{id}/resurrect`\n(the frontend resurrect button) and the internal auto-resurrect op.\n\n`spawned`: agent was dead; UPDATEd 'terminated' -> 'idling' +\n    started a fresh process attached to the same agent_id\n    (LangGraph state preserved; agent wakes up from where it left\n    off).\n`already_alive`: agent is still alive\n    (running/idling/restarting); resurrect does\n    not apply."
      },
      "RunTimelineBoundaries": {
        "properties": {
          "initialize_turn": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Initialize Turn"
          },
          "last_before_compact_turn": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Before Compact Turn"
          },
          "post_window_turns": {
            "type": "integer",
            "title": "Post Window Turns"
          },
          "has_activity_after_window": {
            "type": "boolean",
            "title": "Has Activity After Window"
          }
        },
        "type": "object",
        "required": [
          "initialize_turn",
          "last_before_compact_turn",
          "post_window_turns",
          "has_activity_after_window"
        ],
        "title": "RunTimelineBoundaries",
        "description": "Turn rows that anchor the initialized-context-to-compact session."
      },
      "RunTimelineEvent": {
        "properties": {
          "ts": {
            "type": "string",
            "format": "date-time",
            "title": "Ts"
          },
          "kind": {
            "type": "string",
            "title": "Kind"
          },
          "trace_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Trace Id"
          },
          "label": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Label"
          }
        },
        "type": "object",
        "required": [
          "ts",
          "kind",
          "trace_id",
          "label"
        ],
        "title": "RunTimelineEvent",
        "description": "An event-rail marker relevant to the selected session window."
      },
      "RunTimelineExec": {
        "properties": {
          "tool": {
            "type": "string",
            "title": "Tool"
          },
          "dur_s": {
            "type": "number",
            "title": "Dur S"
          },
          "ok": {
            "type": "boolean",
            "title": "Ok"
          }
        },
        "type": "object",
        "required": [
          "tool",
          "dur_s",
          "ok"
        ],
        "title": "RunTimelineExec",
        "description": "One execution event attached to the containing turn."
      },
      "RunTimelineLlm": {
        "properties": {
          "calls": {
            "type": "integer",
            "title": "Calls"
          },
          "in_total": {
            "type": "integer",
            "title": "In Total"
          },
          "cache_read": {
            "type": "integer",
            "title": "Cache Read"
          },
          "out_total": {
            "type": "integer",
            "title": "Out Total"
          },
          "reasoning": {
            "type": "integer",
            "title": "Reasoning"
          },
          "latency_ms": {
            "type": "number",
            "title": "Latency Ms"
          },
          "cost_usd": {
            "type": "number",
            "title": "Cost Usd"
          },
          "model": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Model"
          }
        },
        "type": "object",
        "required": [
          "calls",
          "in_total",
          "cache_read",
          "out_total",
          "reasoning",
          "latency_ms",
          "cost_usd",
          "model"
        ],
        "title": "RunTimelineLlm",
        "description": "Absolute LLM usage for a turn or an aggregated time bucket."
      },
      "RunTimelineMeta": {
        "properties": {
          "n_turns": {
            "type": "integer",
            "title": "N Turns"
          },
          "wall_span_s": {
            "type": "number",
            "title": "Wall Span S"
          },
          "active_s": {
            "type": "number",
            "title": "Active S"
          },
          "tokens_in": {
            "type": "integer",
            "title": "Tokens In"
          },
          "tokens_out": {
            "type": "integer",
            "title": "Tokens Out"
          },
          "cost_usd": {
            "type": "number",
            "title": "Cost Usd"
          },
          "n_exec_failed": {
            "type": "integer",
            "title": "N Exec Failed"
          },
          "n_compact": {
            "type": "integer",
            "title": "N Compact"
          },
          "n_restart": {
            "type": "integer",
            "title": "N Restart"
          },
          "fallback_turns": {
            "type": "integer",
            "title": "Fallback Turns"
          },
          "unmatched_turns": {
            "type": "integer",
            "title": "Unmatched Turns"
          }
        },
        "type": "object",
        "required": [
          "n_turns",
          "wall_span_s",
          "active_s",
          "tokens_in",
          "tokens_out",
          "cost_usd",
          "n_exec_failed",
          "n_compact",
          "n_restart",
          "fallback_turns",
          "unmatched_turns"
        ],
        "title": "RunTimelineMeta",
        "description": "Run-level totals derived from the rows in a timeline window."
      },
      "RunTimelineResponse": {
        "properties": {
          "agent_id": {
            "type": "integer",
            "title": "Agent Id"
          },
          "window": {
            "$ref": "#/components/schemas/RunTimelineWindow"
          },
          "meta": {
            "$ref": "#/components/schemas/RunTimelineMeta"
          },
          "rows": {
            "items": {
              "$ref": "#/components/schemas/RunTimelineRow"
            },
            "type": "array",
            "title": "Rows"
          },
          "events": {
            "items": {
              "$ref": "#/components/schemas/RunTimelineEvent"
            },
            "type": "array",
            "title": "Events"
          },
          "boundaries": {
            "$ref": "#/components/schemas/RunTimelineBoundaries"
          }
        },
        "type": "object",
        "required": [
          "agent_id",
          "window",
          "meta",
          "rows",
          "events",
          "boundaries"
        ],
        "title": "RunTimelineResponse",
        "description": "GET /api/agents/{agent_id}/run-timeline response."
      },
      "RunTimelineRow": {
        "properties": {
          "turn": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Turn"
          },
          "n_turns": {
            "type": "integer",
            "title": "N Turns"
          },
          "start": {
            "type": "string",
            "format": "date-time",
            "title": "Start"
          },
          "end": {
            "type": "string",
            "format": "date-time",
            "title": "End"
          },
          "active_s": {
            "type": "number",
            "title": "Active S"
          },
          "trace_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Trace Id"
          },
          "checkpoint_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Checkpoint Id"
          },
          "ok": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Ok"
          },
          "llm": {
            "$ref": "#/components/schemas/RunTimelineLlm"
          },
          "execs": {
            "items": {
              "$ref": "#/components/schemas/RunTimelineExec"
            },
            "type": "array",
            "title": "Execs"
          },
          "anomalies": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Anomalies"
          },
          "tags": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Tags"
          }
        },
        "type": "object",
        "required": [
          "turn",
          "n_turns",
          "start",
          "end",
          "active_s",
          "trace_id",
          "checkpoint_id",
          "ok",
          "llm",
          "execs",
          "anomalies",
          "tags"
        ],
        "title": "RunTimelineRow",
        "description": "A completed turn, or a bucket made from contiguous completed turns."
      },
      "RunTimelineWindow": {
        "properties": {
          "from": {
            "type": "string",
            "format": "date-time",
            "title": "From"
          },
          "to": {
            "type": "string",
            "format": "date-time",
            "title": "To"
          }
        },
        "type": "object",
        "required": [
          "from",
          "to"
        ],
        "title": "RunTimelineWindow",
        "description": "The inclusive Loki window used to derive one timeline."
      },
      "ScheduleCreate": {
        "properties": {
          "name": {
            "type": "string",
            "title": "Name"
          },
          "script": {
            "type": "string",
            "title": "Script"
          },
          "command": {
            "type": "string",
            "title": "Command",
            "default": "python schedule.py"
          },
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Description"
          },
          "enabled": {
            "type": "boolean",
            "title": "Enabled",
            "default": true
          }
        },
        "type": "object",
        "required": [
          "name",
          "script"
        ],
        "title": "ScheduleCreate"
      },
      "ScheduleDraftRequest": {
        "properties": {
          "nl": {
            "type": "string",
            "title": "Nl",
            "description": "Natural-language description of the scheduled task."
          }
        },
        "type": "object",
        "required": [
          "nl"
        ],
        "title": "ScheduleDraftRequest"
      },
      "ScheduleDraftResponse": {
        "properties": {
          "agent_id": {
            "type": "integer",
            "title": "Agent Id"
          }
        },
        "type": "object",
        "required": [
          "agent_id"
        ],
        "title": "ScheduleDraftResponse"
      },
      "ScheduleLogsView": {
        "properties": {
          "source": {
            "type": "string",
            "title": "Source"
          },
          "lines": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Lines"
          }
        },
        "type": "object",
        "required": [
          "source",
          "lines"
        ],
        "title": "ScheduleLogsView"
      },
      "ScheduleRunView": {
        "properties": {
          "id": {
            "type": "integer",
            "title": "Id"
          },
          "ran_at": {
            "type": "string",
            "format": "date-time",
            "title": "Ran At"
          },
          "ok": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Ok"
          },
          "agent_id": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Agent Id"
          },
          "note": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Note"
          }
        },
        "type": "object",
        "required": [
          "id",
          "ran_at",
          "ok",
          "agent_id",
          "note"
        ],
        "title": "ScheduleRunView"
      },
      "ScheduleSummary": {
        "properties": {
          "id": {
            "type": "integer",
            "title": "Id"
          },
          "name": {
            "type": "string",
            "title": "Name"
          },
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Description"
          },
          "command": {
            "type": "string",
            "title": "Command"
          },
          "enabled": {
            "type": "boolean",
            "title": "Enabled"
          },
          "status": {
            "type": "string",
            "title": "Status"
          },
          "last_error": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Error"
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          },
          "updated_at": {
            "type": "string",
            "format": "date-time",
            "title": "Updated At"
          }
        },
        "type": "object",
        "required": [
          "id",
          "name",
          "description",
          "command",
          "enabled",
          "status",
          "last_error",
          "created_at",
          "updated_at"
        ],
        "title": "ScheduleSummary"
      },
      "ScheduleUpdate": {
        "properties": {
          "name": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Name"
          },
          "script": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Script"
          },
          "command": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Command"
          },
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Description"
          },
          "enabled": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Enabled"
          }
        },
        "type": "object",
        "title": "ScheduleUpdate"
      },
      "ScheduleView": {
        "properties": {
          "id": {
            "type": "integer",
            "title": "Id"
          },
          "name": {
            "type": "string",
            "title": "Name"
          },
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Description"
          },
          "command": {
            "type": "string",
            "title": "Command"
          },
          "enabled": {
            "type": "boolean",
            "title": "Enabled"
          },
          "status": {
            "type": "string",
            "title": "Status"
          },
          "last_error": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Error"
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          },
          "updated_at": {
            "type": "string",
            "format": "date-time",
            "title": "Updated At"
          },
          "script": {
            "type": "string",
            "title": "Script"
          }
        },
        "type": "object",
        "required": [
          "id",
          "name",
          "description",
          "command",
          "enabled",
          "status",
          "last_error",
          "created_at",
          "updated_at",
          "script"
        ],
        "title": "ScheduleView"
      },
      "SdkCall": {
        "properties": {
          "method": {
            "type": "string",
            "title": "Method"
          },
          "count": {
            "type": "integer",
            "title": "Count"
          }
        },
        "type": "object",
        "required": [
          "method",
          "count"
        ],
        "title": "SdkCall",
        "description": "One SDK method's call count in an agent_code block, e.g.\n``SdkCall(method=\"files.read\", count=3)`` for ``ava.files.read(...)`` x3."
      },
      "ServiceItem": {
        "properties": {
          "name": {
            "type": "string",
            "title": "Name"
          },
          "label": {
            "type": "string",
            "title": "Label"
          },
          "online": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Online"
          },
          "pid": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Pid"
          },
          "detail": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Detail"
          }
        },
        "type": "object",
        "required": [
          "name",
          "label",
          "online",
          "pid",
          "detail"
        ],
        "title": "ServiceItem",
        "description": "Online status of a single daemon."
      },
      "ServiceRestartRow": {
        "properties": {
          "name": {
            "type": "string",
            "title": "Name"
          },
          "starts": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Starts"
          },
          "last_start": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Start"
          }
        },
        "type": "object",
        "required": [
          "name",
          "starts",
          "last_start"
        ],
        "title": "ServiceRestartRow",
        "description": "One service's boot count within the window — `name` is the daemon\nidentity passed to `init_gateway_process` (gateway / agent-host / watchdog /\ndelivery_watchdog / labeler / memory_indexer / heartbeat / ...)."
      },
      "ServicesStatus": {
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/ServiceItem"
            },
            "type": "array",
            "title": "Items"
          }
        },
        "type": "object",
        "required": [
          "items"
        ],
        "title": "ServicesStatus",
        "description": "GET /api/status services sub-section."
      },
      "ShellCaptureResponse": {
        "properties": {
          "agent_id": {
            "type": "integer",
            "title": "Agent Id"
          },
          "session_id": {
            "type": "integer",
            "title": "Session Id"
          },
          "session_name": {
            "type": "string",
            "title": "Session Name"
          },
          "lines": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Lines"
          },
          "created_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Created At"
          },
          "uptime_seconds": {
            "type": "integer",
            "title": "Uptime Seconds",
            "default": 0
          },
          "expires_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Expires At"
          }
        },
        "type": "object",
        "required": [
          "agent_id",
          "session_id",
          "session_name",
          "lines"
        ],
        "title": "ShellCaptureResponse",
        "description": "GET /api/agents/{id}/shell/{sid} response — a recent tail of one\npersistent-shell session's terminal output.\n\n`lines` is the session's most recent output (up to the last 200 lines,\nhistory that has scrolled past included), one string per line with no\ntrailing newline. `session_name` is the resolved session name the\ncapture came from. Backs the shell monitor page, which polls it while open;\nthe session must be live (404 otherwise).\n\n`created_at` / `uptime_seconds` come from the runner's session record\n(the launch epoch + probe-time uptime); `expires_at` is the gateway-owned\nTTL deadline from `agent_shell_ttls` — None when the session has no TTL.\nTogether they let the monitor page's title bar render runtime + TTL\nwithout a second probe."
      },
      "ShellInfo": {
        "properties": {
          "id": {
            "type": "integer",
            "title": "Id"
          },
          "name": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Name"
          },
          "created_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Created At"
          },
          "uptime_seconds": {
            "type": "integer",
            "title": "Uptime Seconds",
            "default": 0
          },
          "expires_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Expires At"
          }
        },
        "type": "object",
        "required": [
          "id"
        ],
        "title": "ShellInfo",
        "description": "One live persistent-shell session of an agent — a session named\n`…-agent-<id>-shell-<sid>[-<name>]`. `id` is the agent-local session id\n(the int the agent uses to drive it); `name` is the optional slug label\n(a watcher carries the conventional name \"watcher\"), None when unnamed.\nThe agent's own process session has no `-shell-` segment and is excluded.\n\n`created_at` / `uptime_seconds` come from the runner's session record\n(the launch epoch, resolved to the cluster timezone); `expires_at` is\nthe gateway-owned TTL deadline from `agent_shell_ttls` — None when the\nsession has no TTL (watcher sessions and legacy pre-TTL shells)."
      },
      "SkillEnableUpdate": {
        "properties": {
          "name": {
            "type": "string",
            "title": "Name"
          },
          "enabled": {
            "type": "boolean",
            "title": "Enabled"
          }
        },
        "type": "object",
        "required": [
          "name",
          "enabled"
        ],
        "title": "SkillEnableUpdate",
        "description": "Request body for PUT /api/skills — toggle one skill's enabled flag."
      },
      "SkillView": {
        "properties": {
          "name": {
            "type": "string",
            "title": "Name"
          },
          "layer": {
            "type": "string",
            "enum": [
              "core",
              "plugin",
              "machine",
              "untracked"
            ],
            "title": "Layer"
          },
          "enabled": {
            "type": "boolean",
            "title": "Enabled"
          },
          "modified_locally": {
            "type": "boolean",
            "title": "Modified Locally"
          },
          "origin_path": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Origin Path"
          }
        },
        "type": "object",
        "required": [
          "name",
          "layer",
          "enabled",
          "modified_locally"
        ],
        "title": "SkillView",
        "description": "One skill in the load dir, correlated with its registry entry.\n\n`modified_locally` is the converge \"was hand-edited\" signal — the on-disk\ncopy's tree hash no longer matches the `content_hash` converge last wrote\n(always False for machine/untracked entries, which carry no managed hash).\n`origin_path` is the source tree a converge-managed copy derives from."
      },
      "SkillsView": {
        "properties": {
          "skills": {
            "items": {
              "$ref": "#/components/schemas/SkillView"
            },
            "type": "array",
            "title": "Skills"
          }
        },
        "type": "object",
        "required": [
          "skills"
        ],
        "title": "SkillsView",
        "description": "Every top-level directory in this host's skills load dir."
      },
      "SpawnAgentRequest": {
        "properties": {
          "prompt": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 1000000,
                "minLength": 1
              },
              {
                "type": "null"
              }
            ],
            "title": "Prompt"
          },
          "spawner": {
            "type": "string",
            "maxLength": 64,
            "minLength": 1,
            "title": "Spawner",
            "default": "user"
          },
          "fork_from": {
            "anyOf": [
              {
                "type": "integer",
                "exclusiveMinimum": 0.0
              },
              {
                "type": "null"
              }
            ],
            "title": "Fork From"
          },
          "prompt_source": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 64,
                "minLength": 1
              },
              {
                "type": "null"
              }
            ],
            "title": "Prompt Source"
          },
          "machine": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 64,
                "minLength": 1
              },
              {
                "type": "null"
              }
            ],
            "title": "Machine"
          },
          "config": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "title": "Config"
          },
          "preset": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 64
              },
              {
                "type": "null"
              }
            ],
            "title": "Preset"
          },
          "label": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 64
              },
              {
                "type": "null"
              }
            ],
            "title": "Label"
          }
        },
        "type": "object",
        "title": "SpawnAgentRequest",
        "description": "POST /api/agents request body — for frontend / SDK; frontend does\nnot need to know checkpoint ids.\n\nIf `fork_from` is given, the gateway resolves the latest checkpoint\ninternally and passes an explicit id to the underlying create_agent_row\n(consistent with SDK `ava.agents.spawn(fork_from=N)` logic).\n\n`spawner` default \"user\" — frontend spawn button does not need to\npass it; external callers (claude-code / SDK paths `agent:N` etc.)\npass their own identifier so the frontend sidebar groups them under\na separate root section by spawner.\n\n`prompt_source` is **required only when prompt is given** —\nINSERTed into inbound_messages.source; the envelope wrap marks who\nthe message came from. Frontend passes \"user\"; SDK paths pass\nf\"agent:{my_id}\". This differs from spawner: spawner is \"who\ncreated the agent\", source is \"from whom the prompt arrived\".\n\nThe schema does not give prompt_source a default — to avoid the\n\"caller forgot to pass it and got silently tagged user,\ncontaminating envelope source\" anti-pattern (one of the\n`or default` forms CLAUDE.md prohibits); both kinds of callers must\nexplicitly identify themselves."
      },
      "SpawnedAgent": {
        "properties": {
          "id": {
            "type": "integer",
            "title": "Id"
          }
        },
        "type": "object",
        "required": [
          "id"
        ],
        "title": "SpawnedAgent",
        "description": "POST /api/agents response — new agent_id (== agent_id)."
      },
      "SseDropBucket": {
        "properties": {
          "bucket": {
            "type": "integer",
            "title": "Bucket"
          },
          "queue_full": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Queue Full"
          },
          "publish_error": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Publish Error"
          },
          "event_log_drop": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Event Log Drop"
          }
        },
        "type": "object",
        "required": [
          "bucket",
          "queue_full",
          "publish_error",
          "event_log_drop"
        ],
        "title": "SseDropBucket",
        "description": "One bucket's SSE/event-log backlog footprint — dropped records by cause."
      },
      "SseReport": {
        "properties": {
          "series": {
            "items": {
              "$ref": "#/components/schemas/SseDropBucket"
            },
            "type": "array",
            "title": "Series"
          },
          "totals": {
            "$ref": "#/components/schemas/SseTotals"
          }
        },
        "type": "object",
        "required": [
          "series",
          "totals"
        ],
        "title": "SseReport"
      },
      "SseTotals": {
        "properties": {
          "queue_full": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Queue Full"
          },
          "publish_error": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Publish Error"
          },
          "event_log_drop": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Event Log Drop"
          }
        },
        "type": "object",
        "required": [
          "queue_full",
          "publish_error",
          "event_log_drop"
        ],
        "title": "SseTotals"
      },
      "StatsDashboard": {
        "properties": {
          "live_count": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Live Count"
          },
          "window_hours": {
            "$ref": "#/components/schemas/StatsWindowHours"
          },
          "applied_window_hours": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Applied Window Hours"
          },
          "tokens": {
            "$ref": "#/components/schemas/StatsTokens"
          },
          "cost_usd": {
            "type": "number",
            "minimum": 0.0,
            "title": "Cost Usd"
          },
          "avg_turn_seconds": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Avg Turn Seconds"
          },
          "warnings": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Warnings"
          },
          "errors": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Errors"
          },
          "warnings_dismissed": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Warnings Dismissed"
          },
          "warnings_net": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Warnings Net"
          },
          "errors_dismissed": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Errors Dismissed"
          },
          "errors_net": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Errors Net"
          },
          "total_events": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Total Events"
          }
        },
        "type": "object",
        "required": [
          "live_count",
          "window_hours",
          "tokens",
          "cost_usd",
          "avg_turn_seconds",
          "warnings",
          "errors",
          "warnings_dismissed",
          "warnings_net",
          "errors_dismissed",
          "errors_net",
          "total_events"
        ],
        "title": "StatsDashboard",
        "description": "GET /api/stats/dashboard response — sidebar-top stats card data pulled in one shot.\n\nAll windowed fields (`tokens` / `cost_usd` / `avg_turn_seconds` /\n`warnings` / `errors`) aggregate over the `applied_window_hours` horizon.\n`window_hours` echoes the selected value — `0` means five minutes; all\nother values are hours.\n`applied_window_hours` is the actually served window in hours, no greater\nthan `window_hours` and clamped to the Loki retention horizon.\n\n- `live_count`: current non-terminated count (from agents_meta, not\n  events; not windowed)\n- `tokens`: windowed telemetry LLM token usage (cached for at most 60s)\n- `cost_usd`: windowed LLM spend in USD, summed from the usage-time\n  `cost_usd` snapshots carried by telemetry Loki `llm_usage` events;\n  events that pre-date the snapshot field contribute 0 (cached for at\n  most 60s with `tokens`)\n- `avg_turn_seconds`: windowed avg LLM call wall time\n  (event=turn_end + ok=true)\n- `warnings` / `errors`: raw level totals over the window (critical\n  folds into error). Agent trial-and-error (exec_failed) logs at INFO\n  and is deliberately NOT counted — these numbers are operator-facing\n  alerts, not agent activity.\n- `warnings_dismissed` / `warnings_net` / `errors_dismissed` /\n  `errors_net`: the three-way resolution split (task #1935). The\n  arithmetic is the events-maintenance daemon's class subtraction\n  (`services.events_maintenance.resolution.level_splits`) applied to the\n  SELECTED window instead of the daemon's fixed six hours: events whose\n  (category, level, event_name, source) class has an active dismissal in\n  `event_dismissals` count as dismissed, the rest as net, and\n  dismissed + net == the raw total by construction.\n- `total_events`: archived event row count (frozen — the PG events copy\n  stopped growing at the LGTM cutover; not a live gauge)\n\n`avg_turn_seconds` None = zero turns in the window (new DB / no\nactivity); frontend renders \"—\"."
      },
      "StatsTokens": {
        "properties": {
          "input": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Input"
          },
          "output": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Output"
          },
          "cache_read": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Cache Read"
          },
          "cache_hit_pct": {
            "type": "number",
            "maximum": 100.0,
            "minimum": 0.0,
            "title": "Cache Hit Pct"
          }
        },
        "type": "object",
        "required": [
          "input",
          "output",
          "cache_read",
          "cache_hit_pct"
        ],
        "title": "StatsTokens",
        "description": "`/api/stats/dashboard` token sub-section — windowed LLM usage aggregation.\n\n`cache_hit_pct` = cache_read / input * 100, rounded to 2 decimals;\ninput=0 degrades to 0 to avoid div-by-zero."
      },
      "StatsWindowHours": {
        "type": "integer",
        "enum": [
          0,
          1,
          6,
          24,
          72,
          168
        ],
        "title": "StatsWindowHours",
        "description": "Whitelisted `?hours=` windows for stats, inspect, and fleet — 0 = last\n5 minutes / 1h / 6h / 24h / 3d / 7d. An enum (not `int` + range check)\nso FastAPI 422s any other value instead of silently aggregating an arbitrary window.\n(A `Literal[1, 6, ...]` won't do: query params arrive as strings and\nint-literal validation doesn't coerce them, 422ing even valid values.)"
      },
      "SystemNoteIn": {
        "properties": {
          "content": {
            "type": "string",
            "minLength": 1,
            "title": "Content"
          },
          "source": {
            "type": "string",
            "maxLength": 64,
            "minLength": 1,
            "title": "Source",
            "default": "system"
          },
          "note_tag": {
            "type": "string",
            "title": "Note Tag",
            "default": "task"
          },
          "task_id": {
            "anyOf": [
              {
                "type": "integer",
                "exclusiveMinimum": 0.0
              },
              {
                "type": "null"
              }
            ],
            "title": "Task Id"
          },
          "resurrect": {
            "type": "boolean",
            "title": "Resurrect",
            "default": true
          }
        },
        "type": "object",
        "required": [
          "content"
        ],
        "title": "SystemNoteIn",
        "description": "POST /api/agents/{id}/system-note request body — a framework system\nnotification delivered to the agent as a system note (system_marker in\nthe timeline), never a peer chat message.\n\n`content` is the note text. `source` follows the same envelope whitelist\nas chat messages (system / agent:N / user / ...), defaulting to 'system';\nit is recorded for audit but the note itself renders without a sender\nprefix. `note_tag` picks the timeline chip — must be a NoteTag value\n(closed set); `task` is the task-notification family. `resurrect`\ncontrols whether a terminated target is woken to receive the note: task\nassignment (a delegator direction) resurrects, plain update notices do\nnot (user ruling 2026-08-27 — notifications never resurrect an owner).\n`task_id` is optional but requires `note_tag='task'`; it is the explicit\nLLM-usage attribution for the turn this note drives."
      },
      "SystemStatus": {
        "properties": {
          "services": {
            "$ref": "#/components/schemas/ServicesStatus"
          },
          "cluster": {
            "$ref": "#/components/schemas/ClusterPanel"
          }
        },
        "type": "object",
        "required": [
          "services",
          "cluster"
        ],
        "title": "SystemStatus",
        "description": "GET /api/status response — System Status panel data all in one go."
      },
      "TaskListResponse": {
        "properties": {
          "tasks": {
            "items": {
              "anyOf": [
                {
                  "$ref": "#/components/schemas/TaskRow"
                },
                {
                  "$ref": "#/components/schemas/TaskSummaryRow"
                }
              ]
            },
            "type": "array",
            "title": "Tasks"
          }
        },
        "type": "object",
        "required": [
          "tasks"
        ],
        "title": "TaskListResponse",
        "description": "GET /api/tasks response — full rows or compact list summaries."
      },
      "TaskRow": {
        "properties": {
          "id": {
            "type": "integer",
            "title": "Id"
          },
          "parent_id": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Parent Id"
          },
          "title": {
            "type": "string",
            "title": "Title"
          },
          "description": {
            "type": "string",
            "title": "Description"
          },
          "results": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Results"
          },
          "status": {
            "type": "string",
            "title": "Status"
          },
          "priority": {
            "$ref": "#/components/schemas/Priority"
          },
          "owner": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Owner"
          },
          "owner_label": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Owner Label"
          },
          "created_by": {
            "type": "string",
            "title": "Created By"
          },
          "created_at": {
            "type": "string",
            "title": "Created At"
          },
          "updated_at": {
            "type": "string",
            "title": "Updated At"
          },
          "remind_interval_seconds": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Remind Interval Seconds"
          },
          "last_reminded_at": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Reminded At"
          },
          "reminder_count": {
            "type": "integer",
            "title": "Reminder Count",
            "default": 0
          },
          "token_budget": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Token Budget"
          },
          "usd_budget": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Usd Budget"
          },
          "token_used": {
            "type": "integer",
            "title": "Token Used",
            "default": 0
          },
          "usd_used": {
            "type": "number",
            "title": "Usd Used",
            "default": 0.0
          },
          "ghost": {
            "type": "boolean",
            "title": "Ghost",
            "default": false
          }
        },
        "type": "object",
        "required": [
          "id",
          "parent_id",
          "title",
          "description",
          "results",
          "status",
          "priority",
          "owner",
          "created_by",
          "created_at",
          "updated_at"
        ],
        "title": "TaskRow",
        "description": "One full task row for PATCH and GET /api/tasks?fields=full."
      },
      "TaskSummaryRow": {
        "properties": {
          "id": {
            "type": "integer",
            "title": "Id"
          },
          "parent_id": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Parent Id"
          },
          "title": {
            "type": "string",
            "title": "Title"
          },
          "status": {
            "type": "string",
            "title": "Status"
          },
          "owner": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Owner"
          },
          "owner_label": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Owner Label"
          },
          "created_by": {
            "type": "string",
            "title": "Created By"
          },
          "created_at": {
            "type": "string",
            "title": "Created At"
          },
          "updated_at": {
            "type": "string",
            "title": "Updated At"
          },
          "remind_interval_seconds": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Remind Interval Seconds"
          },
          "last_reminded_at": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Reminded At"
          },
          "reminder_count": {
            "type": "integer",
            "title": "Reminder Count",
            "default": 0
          },
          "priority": {
            "$ref": "#/components/schemas/Priority"
          },
          "token_budget": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Token Budget"
          },
          "usd_budget": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Usd Budget"
          },
          "token_used": {
            "type": "integer",
            "title": "Token Used",
            "default": 0
          },
          "usd_used": {
            "type": "number",
            "title": "Usd Used",
            "default": 0.0
          },
          "ghost": {
            "type": "boolean",
            "title": "Ghost",
            "default": false
          }
        },
        "type": "object",
        "required": [
          "id",
          "parent_id",
          "title",
          "status",
          "owner",
          "created_by",
          "created_at",
          "updated_at",
          "priority"
        ],
        "title": "TaskSummaryRow",
        "description": "One metadata-only task-list row."
      },
      "TaskUpdateRequest": {
        "properties": {
          "status": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Status"
          },
          "title": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Title"
          },
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Description"
          },
          "results": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Results"
          },
          "remind_interval_seconds": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Remind Interval Seconds"
          },
          "owner": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Owner"
          },
          "priority": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Priority"
              },
              {
                "type": "null"
              }
            ]
          },
          "parent_id": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Parent Id"
          }
        },
        "type": "object",
        "title": "TaskUpdateRequest",
        "description": "PATCH /api/tasks/{id} — partial update; omitted fields stay unchanged.\n\nstatus, title, description, and results are taken when non-null. owner and\nremind_interval_seconds are taken when present (model_fields_set), but an explicit\nnull is rejected for both: a task cannot be released (every non-root task\nhas an owner) and reminders cannot be disabled (interval capped at 24h).\nparent_id is taken when present (model_fields_set): an explicit null moves\nthe task under the system root, an int reparents it (the parent must exist\nand the move must not create a cycle). Owner changes through this endpoint\nsend the SDK-equivalent task system notes to affected agents."
      },
      "TerminateAgentRequest": {
        "properties": {
          "force": {
            "type": "boolean",
            "title": "Force",
            "default": false
          },
          "source": {
            "type": "string",
            "maxLength": 64,
            "minLength": 1,
            "title": "Source",
            "default": "user"
          },
          "message": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 1000000,
                "minLength": 1
              },
              {
                "type": "null"
              }
            ],
            "title": "Message"
          }
        },
        "type": "object",
        "title": "TerminateAgentRequest",
        "description": "POST /api/agents/{id}/terminate request body — fully optional.\n\n`force` defaults to False for graceful termination. True directly kills the\ndetached process and force-updates status when the agent cannot reach claim.\n\n`source` defaults to \"user\"; SDK paths pass f\"agent:{my_id}\". Claim\nincludes this source in the lifecycle marker shown to the agent.\n\n`message`, when present, is queued as chat immediately before the terminate\ninbound. Lifecycle acceptance claims only the terminate row, so the chat\nremains pending for the next resurrection without another LLM turn."
      },
      "TerminateAgentResponse": {
        "properties": {
          "status": {
            "type": "string",
            "enum": [
              "enqueued",
              "already_terminated"
            ],
            "title": "Status"
          }
        },
        "type": "object",
        "required": [
          "status"
        ],
        "title": "TerminateAgentResponse",
        "description": "POST /api/agents/{id}/terminate response.\n\n`enqueued`: termination accepted, including hosted force. Actual work may\n    still be draining; this result does not prove exit.\n`already_terminated`: agent was already dead. Graceful termination is a\n    no-op. Hosted force instead returns enqueued until its exact original\n    host can prove quiescence; metadata status alone is not exit evidence."
      },
      "TextContentBlock": {
        "properties": {
          "type": {
            "type": "string",
            "const": "text",
            "title": "Type"
          },
          "text": {
            "type": "string",
            "title": "Text"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "type",
          "text"
        ],
        "title": "TextContentBlock",
        "description": "Text part of a multimodal chat message (OpenAI content-block shape)."
      },
      "TimelineItem": {
        "properties": {
          "item_id": {
            "type": "string",
            "title": "Item Id"
          },
          "kind": {
            "type": "string",
            "enum": [
              "inbound_chat",
              "inbound_compact_summary",
              "inbound_compact_request",
              "attach",
              "agent_chat",
              "agent_code",
              "agent_reasoning",
              "code_output",
              "system_prompt",
              "system_marker"
            ],
            "title": "Kind"
          },
          "source": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Source"
          },
          "payload": {
            "type": "string",
            "title": "Payload"
          },
          "created_at": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Created At"
          },
          "inbound_id": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Inbound Id"
          },
          "reasoning_ms": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Reasoning Ms"
          },
          "reasoning_tokens": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Reasoning Tokens"
          },
          "exec_ms": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Exec Ms"
          },
          "show_timestamp": {
            "type": "boolean",
            "title": "Show Timestamp",
            "default": true
          },
          "images": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Images"
          },
          "image_captions": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Image Captions"
          },
          "sdk_calls": {
            "anyOf": [
              {
                "items": {
                  "$ref": "#/components/schemas/SdkCall"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Sdk Calls"
          },
          "code_elapsed_ms": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Code Elapsed Ms"
          }
        },
        "type": "object",
        "required": [
          "item_id",
          "kind",
          "payload"
        ],
        "title": "TimelineItem",
        "description": "One element of a timeline view of LangGraph state.messages, using the\ninbound_messages table only as ts anchor.\n\n`item_id` is the stable key coordinating frontend timeline with\nstreaming SSE; the current segment uses `f\"{msg_idx}.{block_idx}\"`\n(msg_idx = position in state.messages; for other message types\nblock_idx=0). Cold-loaded compact history prefixes that local position\nwith `s<rank>.<boundary_checkpoint_id>.`; it never enters the live SSE\nmerge path. For an\nAIMessage, text/thinking are content blocks (block_idx = their content\nposition) and each tool call gets `block_idx = <number of text/thinking\ncontent blocks> + <ordinal in msg.tool_calls>` — a provider-agnostic\nrule derived from langchain's normalized `tool_calls` view, so it holds\nwhether the provider carries tool calls inside `content` (anthropic\n`tool_use` blocks) or only in the separate `tool_calls` list\n(gemini / openai). During streaming, the frontend uses `*_start`\nevent's `item_id` to create items; after commit, the same rule\nrecomputes id, and matching ids on both sides means the same logical\nitem. On merge, the snapshot version wins, avoiding race bugs from\nts-heuristic matching."
      },
      "TimelineResponse": {
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/TimelineItem"
            },
            "type": "array",
            "title": "Items"
          },
          "msg_count": {
            "type": "integer",
            "title": "Msg Count"
          },
          "has_more": {
            "type": "boolean",
            "title": "Has More"
          }
        },
        "type": "object",
        "required": [
          "items",
          "msg_count",
          "has_more"
        ],
        "title": "TimelineResponse",
        "description": "Cold-load timeline payload: one window of rendered items + the\nauthoritative `msg_count` (len(state.messages)) + `has_more`.\n\n`msg_count` is surfaced (not inferred frontend-side from max rendered\nmsg_idx) so the merge that preserves the single streaming \"future\npartial\" uses the exact boundary — a trailing message that renders to\nnothing would otherwise make an inferred count too low.\n\nThe endpoint returns only a tail window (newest `limit` items); `before`\npages further back for scroll-up history. `has_more` reports whether\nolder items exist before the returned window."
      },
      "TokenUsageResponse": {
        "properties": {
          "input_tokens": {
            "type": "integer",
            "title": "Input Tokens"
          },
          "output_tokens": {
            "type": "integer",
            "title": "Output Tokens"
          },
          "reasoning_tokens": {
            "type": "integer",
            "title": "Reasoning Tokens",
            "default": 0
          },
          "max_input_tokens": {
            "type": "integer",
            "title": "Max Input Tokens",
            "default": 0
          },
          "soft_compact_tokens": {
            "type": "integer",
            "title": "Soft Compact Tokens",
            "default": 0
          },
          "hard_compact_tokens": {
            "type": "integer",
            "title": "Hard Compact Tokens",
            "default": 0
          }
        },
        "type": "object",
        "required": [
          "input_tokens",
          "output_tokens"
        ],
        "title": "TokenUsageResponse",
        "description": "GET /api/agents/{id}/token-usage response — token usage of the\nmost recent LLM call. `input_tokens` is context-window occupancy.\n\nReal-time deltas rely on the SSE `token_usage` event; when\nswitching back to an existing agent, the frontend uses this endpoint\nto fetch the historical last value to initialize the UI (SSE is\nfire-and-forget with no persistence). Returns `(0, 0, 0)` if no\nAIMessage in state.messages carries usage_metadata (new agent /\nhas not run any LLM call yet). `reasoning_tokens` is the\nreasoning/thinking portion of output (gemini/openai reasoning\nmodels + Anthropic thinking); defaults to 0 when absent.\n\n`soft_compact_tokens` / `hard_compact_tokens` are the agent model's\nwind-down-reminder and force-compact thresholds (a fraction of\n`max_input_tokens`, resolved per agent), so the composer can render the two\nmarks against current occupancy without a second request. Both default to 0\nwhen the agent's model has no known context window."
      },
      "TraceCheckpointMessagesResponse": {
        "properties": {
          "trace_id": {
            "type": "string",
            "title": "Trace Id"
          },
          "agent_id": {
            "type": "integer",
            "title": "Agent Id"
          },
          "checkpoint_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Checkpoint Id"
          },
          "pruned": {
            "type": "boolean",
            "title": "Pruned"
          },
          "messages": {
            "items": {
              "additionalProperties": true,
              "type": "object"
            },
            "type": "array",
            "title": "Messages"
          }
        },
        "type": "object",
        "required": [
          "trace_id",
          "agent_id",
          "checkpoint_id",
          "pruned",
          "messages"
        ],
        "title": "TraceCheckpointMessagesResponse",
        "description": "GET /api/agents/{id}/traces/{trace_id}/messages response — the full\nmessage list (incl. the system prompt) of the turn that produced one OTel\ntrace, resolved on demand from the checkpoints table.\n\nSpans are metadata-only (trace v2): this endpoint is how content comes\nback. `pruned` distinguishes the two absence shapes:\n- pruned=true, checkpoint_id=None — the trace's checkpoint was dropped by\n  compact/checkpoint trim (retention is the latest K checkpoints), so the\n  content is gone; the span metadata in the mirror / events table still\n  exists. The frontend renders this as \"trimmed\" rather than an error.\n- pruned=false — the checkpoint exists; messages is its `messages` channel\n  (the full conversation at that point, system prompt included).\n\n`messages[i]` is one LangChain BaseMessage `model_dump()` — the same raw\nshape as GET /api/agents/{id}/messages."
      },
      "UiContributionsResponse": {
        "properties": {
          "themes": {
            "items": {
              "$ref": "#/components/schemas/UiThemeContribution"
            },
            "type": "array",
            "title": "Themes"
          },
          "nav": {
            "items": {
              "$ref": "#/components/schemas/UiNavContribution"
            },
            "type": "array",
            "title": "Nav"
          }
        },
        "type": "object",
        "required": [
          "themes",
          "nav"
        ],
        "title": "UiContributionsResponse",
        "description": "Every console contribution the cluster's enabled plugins declare."
      },
      "UiNavContribution": {
        "properties": {
          "plugin": {
            "type": "string",
            "title": "Plugin"
          },
          "location": {
            "type": "string",
            "title": "Location"
          },
          "label": {
            "type": "string",
            "title": "Label"
          },
          "icon": {
            "type": "string",
            "title": "Icon"
          },
          "page": {
            "type": "string",
            "title": "Page"
          }
        },
        "type": "object",
        "required": [
          "plugin",
          "location",
          "label",
          "icon",
          "page"
        ],
        "title": "UiNavContribution",
        "description": "One nav entry opening a plugin-served page.\n\n`page` is a path under the plugin's own mount (`/api/plugin-ui/<plugin>/`),\n`icon` a lucide icon name from the closed set the console imports, and\n`location` names which of the console's nav surfaces carries the entry —\nall three validated at manifest load."
      },
      "UiThemeContribution": {
        "properties": {
          "plugin": {
            "type": "string",
            "title": "Plugin"
          },
          "name": {
            "type": "string",
            "title": "Name"
          },
          "tokens": {
            "additionalProperties": {
              "type": "string"
            },
            "type": "object",
            "title": "Tokens"
          },
          "dark_tokens": {
            "anyOf": [
              {
                "additionalProperties": {
                  "type": "string"
                },
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "title": "Dark Tokens"
          }
        },
        "type": "object",
        "required": [
          "plugin",
          "name",
          "tokens"
        ],
        "title": "UiThemeContribution",
        "description": "One named token pack a plugin offers the theme picker.\n\n`tokens` is a partial map of the console's own `:root` custom properties to\ncolor literals — validated at manifest load, so the console applies the\nvalues as given. Unset tokens keep the console default, which is why a pack\nthat names three colors is a legitimate skin rather than a broken one.\n\n`dark_tokens` is the pack's dark-mode half, and `null` is a MEANINGFUL\nvalue rather than a missing one: it declares that the pack deliberately\npins both modes to `tokens`. That distinction has to survive onto the wire\nbecause it is what the picker tells the user — a pack applied over both\npalettes silently disables the light/dark toggle for every color it sets,\nand a skin and a mode that fight is the failure this field exists to make\nvisible. With it set, `tokens` is the light half and the two stay\northogonal."
      },
      "UpdateCheck": {
        "properties": {
          "behind": {
            "type": "integer",
            "title": "Behind"
          },
          "frontend_changed": {
            "type": "boolean",
            "title": "Frontend Changed"
          },
          "backend_changed": {
            "type": "boolean",
            "title": "Backend Changed"
          },
          "needs_replay": {
            "type": "boolean",
            "title": "Needs Replay"
          }
        },
        "type": "object",
        "required": [
          "behind",
          "frontend_changed",
          "backend_changed",
          "needs_replay"
        ],
        "title": "UpdateCheck",
        "description": "`GET /api/cluster/update-check` response — how far the gateway's\ncheckout is behind its track target, and which side a pull would restart.\n\n`behind` is the commit count from the last fully installed commit to the\ntrack target. `needs_replay` names the exceptional state where the fully\ninstalled commit is ahead of the running-commit bookmark: a rollout was\ninterrupted between them, so zero commits behind is not an up-to-date\nverdict. A running commit ahead of installation is the normal fast-path\nstate and does not need replay.\n\n`frontend_changed` / `backend_changed` mirror `ava cluster update`'s own\nclassification so the UI can tell the user what a rollout would actually\nrestart (docs-only diffs count as neither — a pull with nothing to restart)."
      },
      "UpdateOutcome": {
        "type": "string",
        "enum": [
          "clean",
          "recovered",
          "incomplete",
          "aborted",
          "running",
          "orphaned"
        ],
        "title": "UpdateOutcome",
        "description": "How the last update ended, as the surfaces need to say it.\n\n`CLEAN` / `INCOMPLETE` / `ABORTED` mirror\n`cli.commands._update_recover.RolloutOutcome` — the orchestration's own\nthree-way verdict, persisted rather than re-derived, so the banner cannot\ndisagree with the rollout log.\n\n`RECOVERED` is the fourth terminal state, and it is a distinct one rather than a\nshade of `ABORTED` because it answers a different operator question: the attempt\nfailed AND the cluster is already back on a commit that works, so there is\nnothing to repair — only something to know. It is reached three ways, and all\nare real recoveries: the orchestration's own gateway leg rolling back to\nlast-known-good (written by `finish_update`), a *later* `ava cluster rollback`\ncleaning up after an orchestration that died (derived by `read_last_update` from\nthe observation that rollback left behind), and an incomplete rollout whose\ntarget becomes last-known-good after the cluster self-heals. Collapsing it into\n`ABORTED` was what made the 2026-07-30 recovery read as an open incident.\n\n`RUNNING` and `ORPHANED` are the two readings of a row with no recorded end, and\nthey are told apart by the deploy lease rather than by a timeout: a rollout runs\nfor as long as it runs (the lease is renewed while it does), so elapsed time\nproves nothing, while the holder having stopped renewing does."
      },
      "UpdaterOutcome": {
        "properties": {
          "kind": {
            "type": "string",
            "title": "Kind",
            "default": "unknown"
          },
          "rc": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Rc"
          },
          "detail": {
            "type": "string",
            "title": "Detail",
            "default": ""
          },
          "log": {
            "type": "string",
            "title": "Log",
            "default": ""
          },
          "stages": {
            "additionalProperties": {
              "type": "number"
            },
            "type": "object",
            "title": "Stages"
          },
          "total_s": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Total S"
          },
          "current_stage": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Current Stage"
          },
          "current_stage_s": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Current Stage S"
          }
        },
        "type": "object",
        "title": "UpdaterOutcome",
        "description": "How this host's last updater session ended, as far as its log can say.\n\n`kind` is the operator-facing distinction:\n\n- `declined` — the restart's validate-before-kill preflight refused. **Nothing\n  was stopped**: the host is serving its old code, intact, and the next action\n  is to fix what the preflight named (usually gateway reachability) and re-run.\n- `exited` — the session ran to its end and reported `rc`. Non-zero means the\n  self-update failed after the stop; the host may be half-transitioned.\n- `unknown` — the log is this update's, and carries neither marker. The\n  session died mid-flight: every terminal branch on both platforms writes one\n  (`native_exit_line`), so reaching an end and saying nothing is no longer a\n  thing a healthy run does.\n\n`rc` is None whenever no exit line was found. It is a fact about the run, not\nabout the platform: a Windows run that got as far as any branch of its ladder\nreports one."
      },
      "UploadedBatch": {
        "properties": {
          "files": {
            "items": {
              "$ref": "#/components/schemas/UploadedFile"
            },
            "type": "array",
            "title": "Files"
          }
        },
        "type": "object",
        "required": [
          "files"
        ],
        "title": "UploadedBatch",
        "description": "Result of one upload request — the files saved in a single batch."
      },
      "UploadedFile": {
        "properties": {
          "filename": {
            "type": "string",
            "title": "Filename"
          },
          "path": {
            "type": "string",
            "title": "Path"
          },
          "url": {
            "type": "string",
            "title": "Url"
          },
          "size": {
            "type": "integer",
            "title": "Size"
          },
          "content_type": {
            "type": "string",
            "title": "Content Type"
          }
        },
        "type": "object",
        "required": [
          "filename",
          "path",
          "url",
          "size",
          "content_type"
        ],
        "title": "UploadedFile",
        "description": "Single file upload result.\n\n`url` is the HTTP path the saved file is served back at\n(`/api/agents/{id}/uploads/<name>`) — the frontend uses it as an image\nthumbnail src and, for a native image attachment, as the `image_url.url`\nreference it sends in the next multimodal message."
      },
      "UserSettingListResponse": {
        "properties": {
          "settings": {
            "items": {
              "$ref": "#/components/schemas/UserSettingRow"
            },
            "type": "array",
            "title": "Settings"
          }
        },
        "type": "object",
        "required": [
          "settings"
        ],
        "title": "UserSettingListResponse",
        "description": "GET /api/settings response — every user_settings row."
      },
      "UserSettingRow": {
        "properties": {
          "key": {
            "type": "string",
            "title": "Key"
          },
          "value": {
            "title": "Value"
          },
          "updated_at": {
            "type": "string",
            "title": "Updated At"
          }
        },
        "type": "object",
        "required": [
          "key",
          "value",
          "updated_at"
        ],
        "title": "UserSettingRow",
        "description": "One row from the user_settings table (key-value store for frontend preferences)."
      },
      "UserSettingUpdateRequest": {
        "properties": {
          "value": {
            "title": "Value"
          }
        },
        "type": "object",
        "required": [
          "value"
        ],
        "title": "UserSettingUpdateRequest",
        "description": "PUT /api/settings/{key} request body."
      },
      "ValidationError": {
        "properties": {
          "loc": {
            "items": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "integer"
                }
              ]
            },
            "type": "array",
            "title": "Location"
          },
          "msg": {
            "type": "string",
            "title": "Message"
          },
          "type": {
            "type": "string",
            "title": "Error Type"
          },
          "input": {
            "title": "Input"
          },
          "ctx": {
            "type": "object",
            "title": "Context"
          }
        },
        "type": "object",
        "required": [
          "loc",
          "msg",
          "type"
        ],
        "title": "ValidationError"
      },
      "WorkFailedIn": {
        "properties": {
          "repo": {
            "type": "string",
            "maxLength": 200,
            "minLength": 1,
            "title": "Repo"
          },
          "ref": {
            "type": "string",
            "maxLength": 255,
            "minLength": 1,
            "title": "Ref"
          },
          "commit_sha": {
            "type": "string",
            "maxLength": 64,
            "minLength": 1,
            "title": "Commit Sha"
          },
          "stage": {
            "type": "string",
            "enum": [
              "ci",
              "qa",
              "merge"
            ],
            "title": "Stage"
          },
          "summary": {
            "type": "string",
            "maxLength": 2000,
            "minLength": 1,
            "title": "Summary"
          },
          "author_agent_id": {
            "type": "integer",
            "exclusiveMinimum": 0.0,
            "title": "Author Agent Id"
          },
          "dedup_key": {
            "type": "string",
            "maxLength": 255,
            "minLength": 1,
            "title": "Dedup Key"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "repo",
          "ref",
          "commit_sha",
          "stage",
          "summary",
          "author_agent_id",
          "dedup_key"
        ],
        "title": "WorkFailedIn",
        "description": "One producer-owned failure fact; identity is supplied by commit parsing."
      },
      "WorkFailedResult": {
        "properties": {
          "event_id": {
            "type": "integer",
            "title": "Event Id"
          },
          "status": {
            "type": "string",
            "enum": [
              "delivered",
              "task_alerted",
              "duplicate"
            ],
            "title": "Status"
          },
          "delivered_to": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Delivered To"
          },
          "delivery_kind": {
            "anyOf": [
              {
                "type": "string",
                "enum": [
                  "author",
                  "author_resurrected",
                  "delegator",
                  "task_alert"
                ]
              },
              {
                "type": "null"
              }
            ],
            "title": "Delivery Kind"
          }
        },
        "type": "object",
        "required": [
          "event_id",
          "status",
          "delivered_to",
          "delivery_kind"
        ],
        "title": "WorkFailedResult",
        "description": "The durable event identity and the one routing outcome it reached."
      }
    }
  }
}
